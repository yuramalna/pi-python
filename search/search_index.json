{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pi-python","text":"<p>Unified LLM provider abstraction and agent framework for Python.</p>"},{"location":"#packages","title":"Packages","text":""},{"location":"#pi-llm","title":"pi-llm","text":"<p>Streaming LLM API with model discovery, tool calling, token tracking, and cost calculation.</p> <ul> <li>Stream or await LLM responses with typed events</li> <li>Define tools with JSON Schema, validate arguments automatically</li> <li>Track token usage and dollar costs</li> <li>Support for extended thinking / reasoning</li> </ul>"},{"location":"#pi-llm-agent","title":"pi-llm-agent","text":"<p>Stateful agent framework with tool execution, event streaming, and lifecycle management.</p> <ul> <li>Multi-turn agent loop: prompt \u2192 LLM \u2192 tool calls \u2192 repeat</li> <li>Parallel or sequential tool execution</li> <li>Pre/post tool call hooks for access control</li> <li>Steering and follow-up message queues</li> <li>Cooperative cancellation</li> </ul>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Getting Started \u2014 Install and run your first example</li> <li>pi-llm Quickstart \u2014 Streaming, completing, and tool calls</li> <li>pi-llm-agent Quickstart \u2014 Build an agent with tools</li> <li>Build a Research Agent \u2014 End-to-end tutorial</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<p>Install both packages:</p> <pre><code>pip install pi-llm pi-llm-agent\n</code></pre>"},{"location":"getting-started/#set-up-your-api-key","title":"Set up your API key","text":"<p>pi-python uses OpenAI's API. Export your key:</p> <pre><code>export OPENAI_API_KEY=sk-...\n</code></pre>"},{"location":"getting-started/#your-first-llm-call","title":"Your first LLM call","text":"<pre><code>import asyncio\nfrom pi_llm import get_model, complete_simple, Context, UserMessage\nfrom pi_llm.providers import register_builtin_providers\n\n# Register providers (call once at startup)\nregister_builtin_providers()\n\nasync def main():\n    model = get_model(\"openai\", \"gpt-4o\")\n    context = Context(\n        system_prompt=\"You are a helpful assistant.\",\n        messages=[UserMessage(content=\"Hello!\", timestamp=0)],\n    )\n    message = await complete_simple(model, context)\n    print(message.content[0].text)\n\nasyncio.run(main())\n</code></pre>"},{"location":"getting-started/#your-first-agent","title":"Your first agent","text":"<pre><code>import asyncio\nfrom pi_llm_agent import Agent, AgentOptions, InitialAgentState, AgentTool, AgentToolResult\nfrom pi_llm import get_model, TextContent, stream_simple\nfrom pi_llm.providers import register_builtin_providers\n\nregister_builtin_providers()\n\n\nclass GreetTool(AgentTool):\n    def __init__(self):\n        super().__init__(\n            name=\"greet\",\n            label=\"Greet\",\n            description=\"Greet someone by name\",\n            parameters={\n                \"type\": \"object\",\n                \"properties\": {\"name\": {\"type\": \"string\"}},\n                \"required\": [\"name\"],\n            },\n        )\n\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        return AgentToolResult(\n            content=[TextContent(text=f\"Hello, {params['name']}!\")]\n        )\n\n\nasync def main():\n    agent = Agent(AgentOptions(\n        initial_state=InitialAgentState(\n            model=get_model(\"openai\", \"gpt-4o\"),\n            system_prompt=\"You have a greet tool. Use it when asked to greet someone.\",\n            tools=[GreetTool()],\n        ),\n        stream_fn=stream_simple,\n        get_api_key=lambda _: None,  # uses OPENAI_API_KEY env var\n    ))\n\n    # Print events as they happen\n    def on_event(event, cancellation):\n        if event.type == \"message_update\":\n            if hasattr(event.assistant_message_event, \"delta\"):\n                print(event.assistant_message_event.delta, end=\"\", flush=True)\n        elif event.type == \"tool_execution_start\":\n            print(f\"\\n  [calling {event.tool_name}]\")\n        elif event.type == \"agent_end\":\n            print()\n\n    agent.subscribe(on_event)\n    await agent.prompt(\"Please greet Alice and Bob\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li>pi-llm Quickstart \u2014 Deeper dive into streaming, events, and tools</li> <li>pi-llm-agent Quickstart \u2014 Agent lifecycle, hooks, and state management</li> <li>Build a Research Agent \u2014 Full tutorial with web search</li> </ul>"},{"location":"pi_llm/","title":"pi-llm","text":"<p>Unified LLM provider abstraction for Python.</p> <p>pi-llm provides a clean, typed interface for making LLM API calls with streaming events, tool calling, extended thinking, and cost tracking.</p>"},{"location":"pi_llm/#key-concepts","title":"Key Concepts","text":"<ul> <li>Models &amp; Providers \u2014 Model catalog and provider registry</li> <li>Streaming \u2014 <code>stream_simple()</code>, <code>complete_simple()</code>, and <code>EventStream</code></li> <li>Events \u2014 The 12 streaming event types</li> <li>Messages &amp; Context \u2014 Message types and conversation context</li> <li>Tools \u2014 Tool definition, JSON Schema, and validation</li> <li>Thinking / Reasoning \u2014 Extended thinking levels</li> <li>Cost Tracking \u2014 Token usage and dollar cost calculation</li> </ul>"},{"location":"pi_llm/#how-to-guides","title":"How-to Guides","text":"<ul> <li>Handle Tool Calls</li> <li>Add Image Input</li> <li>Handle Errors</li> </ul>"},{"location":"pi_llm/#api-reference","title":"API Reference","text":"<p>See the auto-generated API Reference for complete documentation of all public types and functions.</p>"},{"location":"pi_llm/quickstart/","title":"pi-llm Quickstart","text":"<p>This guide walks you through your first LLM call with pi-llm: streaming text, collecting a complete response, and handling tool calls.</p>"},{"location":"pi_llm/quickstart/#installation","title":"Installation","text":"<pre><code>pip install pi-llm\n</code></pre>"},{"location":"pi_llm/quickstart/#basic-streaming","title":"Basic streaming","text":"<p>The simplest way to get started is <code>stream_simple</code>. It returns an async iterable of events that you consume with <code>async for</code>.</p> <pre><code>import asyncio\n\nfrom pi_llm import (\n    Context,\n    SimpleStreamOptions,\n    TextDeltaEvent,\n    get_model,\n    stream_simple,\n)\nfrom pi_llm.providers import register_builtin_providers\n\n# Register the built-in providers (OpenAI, etc.) once at startup\nregister_builtin_providers()\n\n\nasync def main():\n    model = get_model(\"openai\", \"gpt-4o\")\n    context = Context(\n        system_prompt=\"You are a helpful assistant.\",\n        messages=[],\n    )\n\n    event_stream = stream_simple(model, context, SimpleStreamOptions())\n\n    async for event in event_stream:\n        if isinstance(event, TextDeltaEvent):\n            print(event.delta, end=\"\", flush=True)\n\n    print()  # newline after streaming finishes\n\n\nasyncio.run(main())\n</code></pre> <p>Note</p> <p><code>register_builtin_providers()</code> must be called once before any streaming function works. It registers the OpenAI Responses API provider (and any other built-in providers).</p>"},{"location":"pi_llm/quickstart/#getting-the-complete-message","title":"Getting the complete message","text":"<p>If you do not need real-time streaming, use <code>complete_simple</code> to get the finished <code>AssistantMessage</code> directly:</p> <pre><code>import asyncio\n\nfrom pi_llm import (\n    Context,\n    SimpleStreamOptions,\n    complete_simple,\n    get_model,\n)\nfrom pi_llm.providers import register_builtin_providers\n\nregister_builtin_providers()\n\n\nasync def main():\n    model = get_model(\"openai\", \"gpt-4o\")\n    context = Context(\n        system_prompt=\"You are a helpful assistant.\",\n        messages=[],\n    )\n\n    message = await complete_simple(model, context, SimpleStreamOptions())\n\n    # The message contains content blocks\n    for block in message.content:\n        if block.type == \"text\":\n            print(block.text)\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"pi_llm/quickstart/#building-a-conversation","title":"Building a conversation","text":"<p>Add messages to the context to build a multi-turn conversation:</p> <pre><code>import asyncio\nimport time\n\nfrom pi_llm import (\n    AssistantMessage,\n    Context,\n    SimpleStreamOptions,\n    TextContent,\n    UserMessage,\n    complete_simple,\n    get_model,\n)\nfrom pi_llm.providers import register_builtin_providers\n\nregister_builtin_providers()\n\n\nasync def main():\n    model = get_model(\"openai\", \"gpt-4o\")\n    messages = []\n\n    # Turn 1\n    messages.append(\n        UserMessage(\n            content=\"What is the capital of France?\",\n            timestamp=int(time.time() * 1000),\n        )\n    )\n    context = Context(system_prompt=\"Answer concisely.\", messages=messages)\n    reply = await complete_simple(model, context, SimpleStreamOptions())\n    messages.append(reply)\n\n    # Turn 2\n    messages.append(\n        UserMessage(\n            content=\"And what about Germany?\",\n            timestamp=int(time.time() * 1000),\n        )\n    )\n    context = Context(system_prompt=\"Answer concisely.\", messages=messages)\n    reply = await complete_simple(model, context, SimpleStreamOptions())\n\n    for block in reply.content:\n        if block.type == \"text\":\n            print(block.text)\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"pi_llm/quickstart/#streaming-with-tool-calls","title":"Streaming with tool calls","text":"<p>pi-llm supports tool use. Define a <code>Tool</code> with a JSON Schema, then handle <code>ToolCallEndEvent</code> in the event stream.</p> <pre><code>import asyncio\nimport time\n\nfrom pi_llm import (\n    Context,\n    DoneEvent,\n    SimpleStreamOptions,\n    TextContent,\n    TextDeltaEvent,\n    Tool,\n    ToolCallEndEvent,\n    ToolResultMessage,\n    get_model,\n    stream_simple,\n)\nfrom pi_llm.providers import register_builtin_providers\n\nregister_builtin_providers()\n\nweather_tool = Tool(\n    name=\"get_weather\",\n    description=\"Get current weather for a city\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"city\": {\"type\": \"string\", \"description\": \"City name\"},\n        },\n        \"required\": [\"city\"],\n    },\n)\n\n\nasync def main():\n    model = get_model(\"openai\", \"gpt-4o\")\n    messages = []\n    from pi_llm import UserMessage\n\n    messages.append(\n        UserMessage(\n            content=\"What is the weather in Paris?\",\n            timestamp=int(time.time() * 1000),\n        )\n    )\n\n    context = Context(\n        system_prompt=\"Use the get_weather tool to answer weather questions.\",\n        messages=messages,\n        tools=[weather_tool],\n    )\n\n    event_stream = stream_simple(model, context, SimpleStreamOptions())\n\n    async for event in event_stream:\n        if isinstance(event, TextDeltaEvent):\n            print(event.delta, end=\"\", flush=True)\n        elif isinstance(event, ToolCallEndEvent):\n            tool_call = event.tool_call\n            print(f\"\\n[Tool call: {tool_call.name}({tool_call.arguments})]\")\n\n    # Get the assistant message that contains the tool call\n    assistant_msg = await event_stream.result()\n    messages.append(assistant_msg)\n\n    # Simulate executing the tool and returning a result\n    for block in assistant_msg.content:\n        if block.type == \"toolCall\":\n            messages.append(\n                ToolResultMessage(\n                    tool_call_id=block.id,\n                    tool_name=block.name,\n                    content=[TextContent(text='{\"temp\": \"18C\", \"condition\": \"Sunny\"}')],\n                    timestamp=int(time.time() * 1000),\n                )\n            )\n\n    # Continue the conversation with the tool result\n    context = Context(\n        system_prompt=\"Use the get_weather tool to answer weather questions.\",\n        messages=messages,\n        tools=[weather_tool],\n    )\n    event_stream = stream_simple(model, context, SimpleStreamOptions())\n\n    async for event in event_stream:\n        if isinstance(event, TextDeltaEvent):\n            print(event.delta, end=\"\", flush=True)\n\n    print()\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"pi_llm/quickstart/#enabling-extended-thinking","title":"Enabling extended thinking","text":"<p>Models that support reasoning can be enabled with the <code>reasoning</code> option:</p> <pre><code>from pi_llm import SimpleStreamOptions, ThinkingDeltaEvent\n\noptions = SimpleStreamOptions(reasoning=\"medium\")\n\n# In the event loop, you can observe thinking events:\n# ThinkingStartEvent, ThinkingDeltaEvent, ThinkingEndEvent\n</code></pre> <p>See Thinking / Reasoning for details on thinking levels.</p>"},{"location":"pi_llm/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Streaming -- The four streaming functions and EventStream</li> <li>Events -- All 12 event types explained</li> <li>Tools -- Tool definitions and JSON Schema</li> <li>Messages &amp; Context -- Message types and context serialization</li> <li>Handle Tool Calls -- Step-by-step tool call handling</li> </ul>"},{"location":"pi_llm/concepts/cost-tracking/","title":"Cost Tracking","text":"<p>pi-llm tracks token usage and calculates dollar costs for every LLM request. The <code>Usage</code> and <code>CostBreakdown</code> types provide detailed breakdowns of input, output, and cache tokens.</p>"},{"location":"pi_llm/concepts/cost-tracking/#key-types","title":"Key types","text":"<ul> <li><code>Usage</code> -- Token counts for a single request (input, output, cache read, cache write, total).</li> <li><code>CostBreakdown</code> -- Dollar costs for each token category (input, output, cache read, cache write, total).</li> <li><code>ModelCost</code> -- Pricing per million tokens for a model.</li> <li><code>calculate_cost(usage, model)</code> -- Compute the dollar cost from usage and model pricing.</li> </ul>"},{"location":"pi_llm/concepts/cost-tracking/#usage","title":"Usage","text":"<p>Every <code>AssistantMessage</code> includes a <code>usage</code> field with token counts:</p> <pre><code>message = await complete_simple(model, context, options)\nprint(f\"Input tokens: {message.usage.input}\")\nprint(f\"Output tokens: {message.usage.output}\")\nprint(f\"Total tokens: {message.usage.total_tokens}\")\n</code></pre>"},{"location":"pi_llm/concepts/cost-tracking/#costbreakdown","title":"CostBreakdown","text":"<p>The <code>usage.cost</code> field contains the dollar breakdown:</p> <pre><code>cost = message.usage.cost\nprint(f\"Input cost:  ${cost.input:.6f}\")\nprint(f\"Output cost: ${cost.output:.6f}\")\nprint(f\"Total cost:  ${cost.total:.6f}\")\n</code></pre>"},{"location":"pi_llm/concepts/cost-tracking/#calculate_cost","title":"calculate_cost","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/concepts/cost-tracking/#next-steps","title":"Next steps","text":"<ul> <li>Models &amp; Providers -- Model pricing data</li> <li>Streaming -- Getting usage from streamed responses</li> </ul>"},{"location":"pi_llm/concepts/events/","title":"Events","text":"<p>When you stream a response with <code>stream_simple</code> or <code>stream</code>, pi-llm emits a sequence of typed events. There are 12 event types in total, grouped into four families: lifecycle, text, thinking, and tool calls.</p>"},{"location":"pi_llm/concepts/events/#event-type-overview","title":"Event type overview","text":"Event Type string Description <code>StartEvent</code> <code>\"start\"</code> Streaming has begun <code>TextStartEvent</code> <code>\"text_start\"</code> A new text block started <code>TextDeltaEvent</code> <code>\"text_delta\"</code> An incremental chunk of text <code>TextEndEvent</code> <code>\"text_end\"</code> A text block completed <code>ThinkingStartEvent</code> <code>\"thinking_start\"</code> A reasoning block started <code>ThinkingDeltaEvent</code> <code>\"thinking_delta\"</code> An incremental chunk of reasoning <code>ThinkingEndEvent</code> <code>\"thinking_end\"</code> A reasoning block completed <code>ToolCallStartEvent</code> <code>\"toolcall_start\"</code> A tool call started <code>ToolCallDeltaEvent</code> <code>\"toolcall_delta\"</code> Incremental tool call arguments <code>ToolCallEndEvent</code> <code>\"toolcall_end\"</code> A tool call completed <code>DoneEvent</code> <code>\"done\"</code> Streaming completed successfully <code>ErrorEvent</code> <code>\"error\"</code> Streaming ended with an error"},{"location":"pi_llm/concepts/events/#common-fields","title":"Common fields","text":"<p>Every event has a <code>type</code> field (a string) and most events carry a <code>partial</code> field -- the <code>AssistantMessage</code> being built incrementally. The <code>partial</code> message is updated in place as events arrive, so by the time you receive <code>DoneEvent</code>, <code>partial</code> contains the complete message.</p> <p>Events that belong to a content block also have a <code>content_index</code> indicating which block within <code>partial.content</code> the event pertains to.</p>"},{"location":"pi_llm/concepts/events/#lifecycle-events","title":"Lifecycle events","text":""},{"location":"pi_llm/concepts/events/#startevent","title":"StartEvent","text":"<p>Emitted once at the beginning of every stream.</p> <pre><code>@dataclass\nclass StartEvent:\n    partial: AssistantMessage\n    type: str = \"start\"\n</code></pre>"},{"location":"pi_llm/concepts/events/#doneevent","title":"DoneEvent","text":"<p>Emitted when streaming completes successfully.</p> <pre><code>@dataclass\nclass DoneEvent:\n    reason: StopReason  # \"stop\", \"length\", \"toolUse\", etc.\n    message: AssistantMessage  # The complete message\n    type: str = \"done\"\n</code></pre>"},{"location":"pi_llm/concepts/events/#errorevent","title":"ErrorEvent","text":"<p>Emitted when streaming ends due to an error or cancellation.</p> <pre><code>@dataclass\nclass ErrorEvent:\n    reason: StopReason  # \"error\" or \"aborted\"\n    error: AssistantMessage  # Contains error_message\n    type: str = \"error\"\n</code></pre>"},{"location":"pi_llm/concepts/events/#text-events","title":"Text events","text":"<p>Text events track the incremental construction of a <code>TextContent</code> block.</p> <pre><code>from pi_llm import TextDeltaEvent, TextEndEvent\n\nasync for event in event_stream:\n    if isinstance(event, TextDeltaEvent):\n        # event.delta is a string chunk\n        print(event.delta, end=\"\", flush=True)\n    elif isinstance(event, TextEndEvent):\n        # event.content is the full text of this block\n        print(f\"\\n[Complete text: {len(event.content)} chars]\")\n</code></pre>"},{"location":"pi_llm/concepts/events/#textstartevent","title":"TextStartEvent","text":"<p>Signals the start of a new text content block. The <code>content_index</code> tells you which position in <code>partial.content</code> will hold the completed <code>TextContent</code>.</p>"},{"location":"pi_llm/concepts/events/#textdeltaevent","title":"TextDeltaEvent","text":"<p>Contains an incremental <code>delta</code> string. Concatenating all deltas for a given <code>content_index</code> produces the full text.</p>"},{"location":"pi_llm/concepts/events/#textendevent","title":"TextEndEvent","text":"<p>Contains the complete <code>content</code> string for this block.</p>"},{"location":"pi_llm/concepts/events/#thinking-events","title":"Thinking events","text":"<p>Thinking events mirror text events but track <code>ThinkingContent</code> blocks. They only appear when reasoning is enabled.</p> <pre><code>from pi_llm import ThinkingDeltaEvent, SimpleStreamOptions\n\noptions = SimpleStreamOptions(reasoning=\"medium\")\nevent_stream = stream_simple(model, context, options)\n\nasync for event in event_stream:\n    if isinstance(event, ThinkingDeltaEvent):\n        print(f\"[thinking] {event.delta}\", end=\"\")\n</code></pre>"},{"location":"pi_llm/concepts/events/#thinkingstartevent-thinkingdeltaevent-thinkingendevent","title":"ThinkingStartEvent / ThinkingDeltaEvent / ThinkingEndEvent","text":"<p>Same structure as the text family: <code>content_index</code>, <code>delta</code> (for the delta event), and <code>content</code> (for the end event). The <code>ThinkingContent</code> block may have a <code>thinking_signature</code> used for cross-provider handoff.</p>"},{"location":"pi_llm/concepts/events/#tool-call-events","title":"Tool call events","text":"<p>Tool call events track the incremental construction of a <code>ToolCall</code> block. The arguments are streamed as JSON string chunks.</p> <pre><code>from pi_llm import ToolCallStartEvent, ToolCallDeltaEvent, ToolCallEndEvent\n\nasync for event in event_stream:\n    if isinstance(event, ToolCallStartEvent):\n        print(f\"[Tool call starting at index {event.content_index}]\")\n    elif isinstance(event, ToolCallDeltaEvent):\n        # event.delta is a chunk of the JSON arguments string\n        pass\n    elif isinstance(event, ToolCallEndEvent):\n        # event.tool_call is the complete ToolCall with parsed arguments\n        tc = event.tool_call\n        print(f\"[Tool: {tc.name}({tc.arguments})]\")\n</code></pre>"},{"location":"pi_llm/concepts/events/#toolcallstartevent","title":"ToolCallStartEvent","text":"<p>Signals the start of a new tool call block.</p>"},{"location":"pi_llm/concepts/events/#toolcalldeltaevent","title":"ToolCallDeltaEvent","text":"<p>Contains a <code>delta</code> string -- an incremental chunk of the JSON-serialized arguments.</p>"},{"location":"pi_llm/concepts/events/#toolcallendevent","title":"ToolCallEndEvent","text":"<p>Contains the fully parsed <code>ToolCall</code> object with <code>id</code>, <code>name</code>, and <code>arguments</code> (a dict).</p>"},{"location":"pi_llm/concepts/events/#event-sequence","title":"Event sequence","text":"<p>A typical text-only response produces:</p> <pre><code>StartEvent -&gt; TextStartEvent -&gt; TextDeltaEvent* -&gt; TextEndEvent -&gt; DoneEvent\n</code></pre> <p>A response with thinking enabled:</p> <pre><code>StartEvent -&gt; ThinkingStartEvent -&gt; ThinkingDeltaEvent* -&gt; ThinkingEndEvent\n           -&gt; TextStartEvent -&gt; TextDeltaEvent* -&gt; TextEndEvent -&gt; DoneEvent\n</code></pre> <p>A response that invokes a tool:</p> <pre><code>StartEvent -&gt; ToolCallStartEvent -&gt; ToolCallDeltaEvent* -&gt; ToolCallEndEvent -&gt; DoneEvent\n</code></pre> <p>Multiple content blocks can appear in a single response. For example, the model might emit text, then a tool call, or multiple tool calls in sequence.</p>"},{"location":"pi_llm/concepts/events/#the-assistantmessageevent-union","title":"The AssistantMessageEvent union","text":"<p>All 12 event types are members of the <code>AssistantMessageEvent</code> union:</p> <pre><code>from pi_llm import AssistantMessageEvent\n\n# Type-safe matching with isinstance:\nasync for event in event_stream:\n    if isinstance(event, TextDeltaEvent):\n        ...\n    elif isinstance(event, DoneEvent):\n        ...\n\n# Or match on the type string:\nasync for event in event_stream:\n    match event.type:\n        case \"text_delta\":\n            ...\n        case \"done\":\n            ...\n</code></pre>"},{"location":"pi_llm/concepts/events/#next-steps","title":"Next steps","text":"<ul> <li>Streaming -- The streaming functions and EventStream</li> <li>Tools -- Defining tools for tool call events</li> <li>Messages &amp; Context -- The AssistantMessage that events build</li> </ul>"},{"location":"pi_llm/concepts/messages-and-context/","title":"Messages &amp; Context","text":"<p>pi-llm uses typed message objects to represent a conversation. The <code>Context</code> object bundles messages with a system prompt and tool definitions, forming the complete input for an LLM call.</p>"},{"location":"pi_llm/concepts/messages-and-context/#message-types","title":"Message types","text":"<p>There are three message types, discriminated by their <code>role</code> field:</p> <ul> <li><code>UserMessage</code> (<code>role=\"user\"</code>) -- A message from the user. Content can be a plain string or a list of <code>TextContent</code> / <code>ImageContent</code> blocks.</li> <li><code>AssistantMessage</code> (<code>role=\"assistant\"</code>) -- A response from the LLM. Content is a list of <code>TextContent</code>, <code>ThinkingContent</code>, and/or <code>ToolCall</code> blocks.</li> <li><code>ToolResultMessage</code> (<code>role=\"toolResult\"</code>) -- The result of executing a tool, sent back to the LLM.</li> </ul> <p>The <code>Message</code> union type covers all three: <code>UserMessage | AssistantMessage | ToolResultMessage</code>.</p>"},{"location":"pi_llm/concepts/messages-and-context/#context","title":"Context","text":"<p>The <code>Context</code> class bundles everything needed for an LLM call:</p> Field Type Description <code>system_prompt</code> <code>str</code> System-level instructions <code>messages</code> <code>list[Message]</code> Conversation history <code>tools</code> <code>list[Tool]</code> Available tools"},{"location":"pi_llm/concepts/messages-and-context/#content-blocks","title":"Content blocks","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/concepts/messages-and-context/#serialization","title":"Serialization","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/concepts/messages-and-context/#next-steps","title":"Next steps","text":"<ul> <li>Events -- How messages are built incrementally via events</li> <li>Tools -- The Tool definitions in Context</li> </ul>"},{"location":"pi_llm/concepts/models-and-providers/","title":"Models &amp; Providers","text":"<p>pi-llm uses a registry-based architecture to support multiple LLM providers through a unified interface. Models are descriptors that carry all the metadata needed to make API calls, and providers are the adapters that translate pi-llm's streaming protocol into provider-specific API calls.</p>"},{"location":"pi_llm/concepts/models-and-providers/#key-concepts","title":"Key concepts","text":"<ul> <li>Model -- A data object describing an LLM endpoint (id, provider, pricing, capabilities).</li> <li>Provider -- An API adapter registered in the global registry.</li> <li><code>get_model(provider, model_id)</code> -- Look up a model from the built-in catalog.</li> <li><code>register_builtin_providers()</code> -- Register the built-in provider adapters (OpenAI Responses, etc.).</li> <li><code>register_api_provider()</code> -- Register a custom provider adapter.</li> </ul>"},{"location":"pi_llm/concepts/models-and-providers/#the-model-class","title":"The Model class","text":"<p>The <code>Model</code> class carries:</p> Field Description <code>id</code> Model identifier (e.g., <code>\"gpt-4o\"</code>) <code>name</code> Display name <code>api</code> API backend identifier (e.g., <code>\"openai-responses\"</code>) <code>provider</code> Provider name (e.g., <code>\"openai\"</code>) <code>base_url</code> API base URL <code>reasoning</code> Whether the model supports extended thinking <code>input_types</code> Supported input modalities (<code>[\"text\"]</code>, <code>[\"text\", \"image\"]</code>) <code>cost</code> Pricing per million tokens (<code>ModelCost</code>) <code>context_window</code> Maximum context size in tokens <code>max_tokens</code> Maximum output tokens per request"},{"location":"pi_llm/concepts/models-and-providers/#provider-registry","title":"Provider registry","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/concepts/models-and-providers/#custom-providers","title":"Custom providers","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/concepts/models-and-providers/#next-steps","title":"Next steps","text":"<ul> <li>Streaming -- Making LLM calls with models</li> <li>Cost Tracking -- Calculating costs from Usage data</li> </ul>"},{"location":"pi_llm/concepts/streaming/","title":"Streaming","text":"<p>pi-llm provides four top-level functions for making LLM calls. Two are \"simple\" (handle reasoning automatically) and two are \"raw\" (accept provider-specific options). Each pair has a streaming variant and a non-streaming convenience wrapper.</p>"},{"location":"pi_llm/concepts/streaming/#the-four-functions","title":"The four functions","text":"Function Returns Use case <code>stream_simple(model, context, options)</code> <code>AssistantMessageEventStream</code> Recommended for most use cases <code>complete_simple(model, context, options)</code> <code>AssistantMessage</code> When you only need the final result <code>stream(model, context, options)</code> <code>AssistantMessageEventStream</code> When you need provider-specific options <code>complete(model, context, options)</code> <code>AssistantMessage</code> Non-streaming with provider-specific options"},{"location":"pi_llm/concepts/streaming/#stream_simple","title":"stream_simple","text":"<p>The recommended entry point. Accepts <code>SimpleStreamOptions</code> which includes reasoning level support.</p> <pre><code>from pi_llm import Context, SimpleStreamOptions, get_model, stream_simple\nfrom pi_llm.providers import register_builtin_providers\n\nregister_builtin_providers()\n\nmodel = get_model(\"openai\", \"gpt-4o\")\ncontext = Context(system_prompt=\"You are helpful.\", messages=[])\noptions = SimpleStreamOptions(reasoning=\"medium\")\n\nevent_stream = stream_simple(model, context, options)\n</code></pre>"},{"location":"pi_llm/concepts/streaming/#complete_simple","title":"complete_simple","text":"<p>A convenience wrapper that internally calls <code>stream_simple</code> and awaits the final result:</p> <pre><code>from pi_llm import Context, SimpleStreamOptions, complete_simple, get_model\n\nmessage = await complete_simple(model, context, SimpleStreamOptions())\nprint(message.content[0].text)\n</code></pre>"},{"location":"pi_llm/concepts/streaming/#stream-complete","title":"stream / complete","text":"<p>The lower-level pair. These accept <code>StreamOptions</code> (without reasoning level) and are useful when you need to pass provider-specific options like <code>cache_retention</code> or <code>session_id</code> without the reasoning abstraction.</p> <pre><code>from pi_llm import Context, StreamOptions, get_model, stream\n\noptions = StreamOptions(\n    temperature=0.7,\n    max_tokens=1024,\n    cache_retention=\"long\",\n)\n\nevent_stream = stream(model, context, options)\n</code></pre>"},{"location":"pi_llm/concepts/streaming/#eventstream","title":"EventStream","text":"<p>All streaming functions return an <code>AssistantMessageEventStream</code>, which is a specialization of the generic <code>EventStream[T, R]</code>.</p>"},{"location":"pi_llm/concepts/streaming/#async-iteration","title":"Async iteration","text":"<p>Consume events as they arrive:</p> <pre><code>from pi_llm import TextDeltaEvent, DoneEvent, ErrorEvent\n\nevent_stream = stream_simple(model, context, SimpleStreamOptions())\n\nasync for event in event_stream:\n    match event.type:\n        case \"text_delta\":\n            print(event.delta, end=\"\", flush=True)\n        case \"done\":\n            print(f\"\\nFinished: {event.reason}\")\n        case \"error\":\n            print(f\"\\nError: {event.error.error_message}\")\n</code></pre>"},{"location":"pi_llm/concepts/streaming/#getting-the-final-result","title":"Getting the final result","text":"<p>Use <code>.result()</code> to await the completed <code>AssistantMessage</code>. This works whether or not you consumed the events:</p> <pre><code>event_stream = stream_simple(model, context, SimpleStreamOptions())\nmessage = await event_stream.result()\n\n# message is an AssistantMessage with all content blocks\nfor block in message.content:\n    if block.type == \"text\":\n        print(block.text)\n</code></pre>"},{"location":"pi_llm/concepts/streaming/#producer-api","title":"Producer API","text":"<p>The <code>EventStream</code> is push-based. Providers use <code>push()</code> and <code>end()</code> to emit events:</p> <pre><code>from pi_llm import AssistantMessageEventStream\n\n# This is what providers do internally:\nevent_stream = AssistantMessageEventStream()\nevent_stream.push(start_event)\nevent_stream.push(text_delta_event)\n# ...\nevent_stream.push(done_event)  # Triggers result resolution\nevent_stream.end()             # Signals no more events\n</code></pre> <p>The <code>push()</code> method detects terminal events (<code>DoneEvent</code> or <code>ErrorEvent</code>) and resolves the result future automatically. After <code>end()</code>, further <code>push()</code> calls are silently dropped.</p>"},{"location":"pi_llm/concepts/streaming/#simplestreamoptions","title":"SimpleStreamOptions","text":"<p>Options for the simple API:</p> Field Type Default Description <code>reasoning</code> <code>ThinkingLevel \\| None</code> <code>None</code> Extended thinking level <code>thinking_budgets</code> <code>dict[str, int] \\| None</code> <code>None</code> Custom token budgets per level <code>temperature</code> <code>float \\| None</code> <code>None</code> Sampling temperature <code>max_tokens</code> <code>int \\| None</code> <code>None</code> Maximum output tokens <code>api_key</code> <code>str \\| None</code> <code>None</code> API key override <code>cancel_event</code> <code>asyncio.Event \\| None</code> <code>None</code> Cancellation signal <code>cache_retention</code> <code>CacheRetention \\| None</code> <code>None</code> Prompt caching preference <code>session_id</code> <code>str \\| None</code> <code>None</code> Session ID for caching <code>headers</code> <code>dict[str, str] \\| None</code> <code>None</code> Additional HTTP headers <code>metadata</code> <code>dict[str, Any] \\| None</code> <code>None</code> Arbitrary metadata for the provider"},{"location":"pi_llm/concepts/streaming/#streamoptions","title":"StreamOptions","text":"<p>The base options class (used by <code>stream</code> / <code>complete</code>). Identical to <code>SimpleStreamOptions</code> but without <code>reasoning</code> or <code>thinking_budgets</code>.</p>"},{"location":"pi_llm/concepts/streaming/#cancellation","title":"Cancellation","text":"<p>Pass an <code>asyncio.Event</code> to cancel a streaming request:</p> <pre><code>import asyncio\n\ncancel = asyncio.Event()\noptions = SimpleStreamOptions(cancel_event=cancel)\nevent_stream = stream_simple(model, context, options)\n\n# In another coroutine:\ncancel.set()  # Cancels the in-flight request\n</code></pre>"},{"location":"pi_llm/concepts/streaming/#next-steps","title":"Next steps","text":"<ul> <li>Events -- The 12 event types emitted during streaming</li> <li>Tools -- Adding tools to the context</li> <li>Thinking / Reasoning -- Reasoning levels and thinking events</li> </ul>"},{"location":"pi_llm/concepts/thinking/","title":"Thinking / Reasoning","text":"<p>Some LLM models support extended thinking (also called reasoning), where the model explicitly works through a problem before producing its final answer. pi-llm provides first-class support for controlling reasoning levels and observing thinking events.</p>"},{"location":"pi_llm/concepts/thinking/#thinkinglevel","title":"ThinkingLevel","text":"<p>The <code>ThinkingLevel</code> type defines five levels of reasoning effort:</p> Level Description <code>\"minimal\"</code> Minimal reasoning <code>\"low\"</code> Low reasoning effort <code>\"medium\"</code> Moderate reasoning effort <code>\"high\"</code> High reasoning effort <code>\"xhigh\"</code> Maximum reasoning effort (not supported by all models) <p>Pass the level via <code>SimpleStreamOptions</code>:</p> <pre><code>from pi_llm import SimpleStreamOptions\n\noptions = SimpleStreamOptions(reasoning=\"medium\")\n</code></pre>"},{"location":"pi_llm/concepts/thinking/#thinking-events","title":"Thinking events","text":"<p>When reasoning is enabled, the event stream includes <code>ThinkingStartEvent</code>, <code>ThinkingDeltaEvent</code>, and <code>ThinkingEndEvent</code> events alongside the regular text events.</p>"},{"location":"pi_llm/concepts/thinking/#thinkingcontent","title":"ThinkingContent","text":"<p>Reasoning output is stored as <code>ThinkingContent</code> blocks in the <code>AssistantMessage.content</code> list, alongside <code>TextContent</code> and <code>ToolCall</code> blocks.</p>"},{"location":"pi_llm/concepts/thinking/#thinking-budgets","title":"Thinking budgets","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/concepts/thinking/#next-steps","title":"Next steps","text":"<ul> <li>Streaming -- Passing reasoning options</li> <li>Events -- The thinking event family</li> <li>Cost Tracking -- Reasoning tokens and cost</li> </ul>"},{"location":"pi_llm/concepts/tools/","title":"Tools","text":"<p>Tools enable LLMs to interact with external systems by calling named functions with structured arguments. In pi-llm, tools are defined with a name, description, and a JSON Schema for their parameters.</p>"},{"location":"pi_llm/concepts/tools/#defining-a-tool","title":"Defining a tool","text":"<p>The <code>Tool</code> class takes three fields:</p> <pre><code>from pi_llm import Tool\n\nweather_tool = Tool(\n    name=\"get_weather\",\n    description=\"Get current weather for a city\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"city\": {\n                \"type\": \"string\",\n                \"description\": \"City name\",\n            },\n            \"units\": {\n                \"type\": \"string\",\n                \"enum\": [\"celsius\", \"fahrenheit\"],\n                \"description\": \"Temperature units\",\n            },\n        },\n        \"required\": [\"city\"],\n    },\n)\n</code></pre> <ul> <li>name -- A unique identifier the LLM uses to invoke the tool.</li> <li>description -- A human-readable string the LLM uses to decide when to call the tool.</li> <li>parameters -- A JSON Schema object describing the tool's input parameters.</li> </ul>"},{"location":"pi_llm/concepts/tools/#creating-tools-from-pydantic-models","title":"Creating tools from Pydantic models","text":"<p>If you already have a Pydantic model for your tool's parameters, use <code>Tool.from_pydantic()</code> to generate the schema automatically:</p> <pre><code>from pydantic import BaseModel, Field\nfrom pi_llm import Tool\n\n\nclass WeatherParams(BaseModel):\n    city: str = Field(description=\"City name\")\n    units: str = Field(\n        default=\"celsius\",\n        description=\"Temperature units\",\n        json_schema_extra={\"enum\": [\"celsius\", \"fahrenheit\"]},\n    )\n\n\nweather_tool = Tool.from_pydantic(\n    name=\"get_weather\",\n    description=\"Get current weather for a city\",\n    model_class=WeatherParams,\n)\n</code></pre>"},{"location":"pi_llm/concepts/tools/#adding-tools-to-the-context","title":"Adding tools to the context","text":"<p>Pass tools in the <code>Context</code> to make them available to the LLM:</p> <pre><code>from pi_llm import Context\n\ncontext = Context(\n    system_prompt=\"Use the available tools to help the user.\",\n    messages=[...],\n    tools=[weather_tool],\n)\n</code></pre> <p>The LLM will decide whether to call a tool based on the user's request and the tool descriptions.</p>"},{"location":"pi_llm/concepts/tools/#handling-tool-call-events","title":"Handling tool call events","text":"<p>When the LLM decides to call a tool, the event stream emits <code>ToolCallStartEvent</code>, zero or more <code>ToolCallDeltaEvent</code>s, and finally <code>ToolCallEndEvent</code> with the parsed arguments.</p> <pre><code>from pi_llm import (\n    ToolCallEndEvent,\n    ToolResultMessage,\n    TextContent,\n    stream_simple,\n    SimpleStreamOptions,\n)\nimport time\n\nevent_stream = stream_simple(model, context, SimpleStreamOptions())\n\nasync for event in event_stream:\n    if isinstance(event, ToolCallEndEvent):\n        tc = event.tool_call\n        print(f\"Tool call: {tc.name}(id={tc.id})\")\n        print(f\"Arguments: {tc.arguments}\")\n\n# After consuming events, get the complete message\nassistant_msg = await event_stream.result()\n</code></pre>"},{"location":"pi_llm/concepts/tools/#returning-tool-results","title":"Returning tool results","text":"<p>After executing the tool, send back a <code>ToolResultMessage</code> to continue the conversation:</p> <pre><code>import time\nfrom pi_llm import ToolResultMessage, TextContent\n\nresult = ToolResultMessage(\n    tool_call_id=tc.id,       # Must match the ToolCall.id\n    tool_name=tc.name,         # Must match the ToolCall.name\n    content=[\n        TextContent(text='{\"temp\": \"18C\", \"condition\": \"Sunny\"}'),\n    ],\n    timestamp=int(time.time() * 1000),\n)\n</code></pre> <p>Add both the assistant message (containing the tool call) and the tool result to the context for the next LLM call:</p> <pre><code>messages.append(assistant_msg)\nmessages.append(result)\n\ncontext = Context(\n    system_prompt=\"Use the available tools to help the user.\",\n    messages=messages,\n    tools=[weather_tool],\n)\n\n# Continue streaming -- the LLM will incorporate the tool result\nevent_stream = stream_simple(model, context, SimpleStreamOptions())\n</code></pre>"},{"location":"pi_llm/concepts/tools/#toolcall-structure","title":"ToolCall structure","text":"<p>The <code>ToolCall</code> object returned in <code>ToolCallEndEvent.tool_call</code> has these fields:</p> Field Type Description <code>type</code> <code>Literal[\"toolCall\"]</code> Always <code>\"toolCall\"</code> <code>id</code> <code>str</code> Unique ID for this invocation <code>name</code> <code>str</code> Tool name (matches <code>Tool.name</code>) <code>arguments</code> <code>dict[str, Any]</code> Parsed arguments matching the JSON Schema"},{"location":"pi_llm/concepts/tools/#validating-tool-arguments","title":"Validating tool arguments","text":"<p>pi-llm provides utilities to validate tool call arguments against the schema:</p> <pre><code>from pi_llm import validate_tool_call, validate_tool_arguments\n\n# Validate a ToolCall against a list of Tools\nerrors = validate_tool_call(tool_call, [weather_tool])\n\n# Validate raw arguments against a JSON Schema\nerrors = validate_tool_arguments(tool_call.arguments, weather_tool.parameters)\n</code></pre> <p>Both return a list of validation error strings (empty list means valid).</p>"},{"location":"pi_llm/concepts/tools/#error-results","title":"Error results","text":"<p>If tool execution fails, set <code>is_error=True</code> on the result message:</p> <pre><code>error_result = ToolResultMessage(\n    tool_call_id=tc.id,\n    tool_name=tc.name,\n    content=[TextContent(text=\"City not found\")],\n    is_error=True,\n    timestamp=int(time.time() * 1000),\n)\n</code></pre> <p>The LLM will see the error and can retry or inform the user.</p>"},{"location":"pi_llm/concepts/tools/#next-steps","title":"Next steps","text":"<ul> <li>Handle Tool Calls -- Step-by-step guide</li> <li>Events -- The tool call event family</li> <li>Messages &amp; Context -- ToolResultMessage details</li> </ul>"},{"location":"pi_llm/howto/add-image-input/","title":"Add Image Input","text":"<p>This guide shows how to include images in user messages using <code>ImageContent</code>.</p>"},{"location":"pi_llm/howto/add-image-input/#overview","title":"Overview","text":"<p>Models that support image input (those with <code>\"image\"</code> in <code>model.input_types</code>) can process <code>ImageContent</code> blocks alongside text. Images are sent as base64-encoded data with a MIME type.</p>"},{"location":"pi_llm/howto/add-image-input/#creating-an-imagecontent-block","title":"Creating an ImageContent block","text":"<pre><code>from pi_llm import ImageContent\n\nimage = ImageContent(\n    data=\"&lt;base64-encoded image data&gt;\",\n    mime_type=\"image/jpeg\",\n)\n</code></pre>"},{"location":"pi_llm/howto/add-image-input/#including-images-in-a-usermessage","title":"Including images in a UserMessage","text":"<pre><code>from pi_llm import UserMessage, TextContent, ImageContent\n\nmessage = UserMessage(\n    content=[\n        TextContent(text=\"Describe this image:\"),\n        ImageContent(data=base64_data, mime_type=\"image/png\"),\n    ],\n    timestamp=int(time.time() * 1000),\n)\n</code></pre>"},{"location":"pi_llm/howto/add-image-input/#full-example","title":"Full example","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/howto/add-image-input/#next-steps","title":"Next steps","text":"<ul> <li>Messages &amp; Context -- Content block types</li> <li>Streaming -- Making LLM calls with image context</li> </ul>"},{"location":"pi_llm/howto/handle-errors/","title":"Handle Errors","text":"<p>This guide covers error handling in pi-llm: detecting errors in the event stream, handling context overflow, and using cancellation to abort requests.</p>"},{"location":"pi_llm/howto/handle-errors/#overview","title":"Overview","text":"<p>Errors in pi-llm surface through three mechanisms:</p> <ul> <li><code>ErrorEvent</code> -- Emitted when streaming ends due to an error or cancellation.</li> <li><code>is_context_overflow()</code> -- Utility to detect context window overflow errors.</li> <li><code>cancel_event</code> -- An <code>asyncio.Event</code> passed via options to abort in-flight requests.</li> </ul>"},{"location":"pi_llm/howto/handle-errors/#errorevent","title":"ErrorEvent","text":"<p>When an error occurs during streaming, the event stream emits an <code>ErrorEvent</code> instead of a <code>DoneEvent</code>:</p> <pre><code>from pi_llm import ErrorEvent\n\nasync for event in event_stream:\n    if isinstance(event, ErrorEvent):\n        print(f\"Error: {event.error.error_message}\")\n        print(f\"Reason: {event.reason}\")  # \"error\" or \"aborted\"\n</code></pre>"},{"location":"pi_llm/howto/handle-errors/#context-overflow-detection","title":"Context overflow detection","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/howto/handle-errors/#cancellation","title":"Cancellation","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/howto/handle-errors/#next-steps","title":"Next steps","text":"<ul> <li>Events -- The ErrorEvent type</li> <li>Streaming -- Cancellation via options</li> </ul>"},{"location":"pi_llm/howto/handle-tool-calls/","title":"Handle Tool Calls","text":"<p>This guide walks through the complete workflow of defining a tool, streaming a response, handling tool calls, and returning results to the LLM.</p>"},{"location":"pi_llm/howto/handle-tool-calls/#overview","title":"Overview","text":"<p>The tool call workflow has four steps:</p> <ol> <li>Define a <code>Tool</code> with a JSON Schema for its parameters.</li> <li>Include the tool in the <code>Context</code> and stream a response.</li> <li>When the LLM emits a <code>ToolCallEndEvent</code>, execute the tool.</li> <li>Return the result as a <code>ToolResultMessage</code> and continue the conversation.</li> </ol>"},{"location":"pi_llm/howto/handle-tool-calls/#step-by-step","title":"Step-by-step","text":"<p>Content coming soon.</p>"},{"location":"pi_llm/howto/handle-tool-calls/#next-steps","title":"Next steps","text":"<ul> <li>Tools -- Tool definition reference</li> <li>Events -- The tool call event family</li> <li>Handle Errors -- Error handling in tool workflows</li> </ul>"},{"location":"pi_llm/reference/","title":"pi_llm API Reference","text":"<ul> <li><code>pi_llm</code></li> <li><code>pi_llm.api_registry</code></li> <li><code>pi_llm.env_api_keys</code></li> <li><code>pi_llm.model_pricing</code></li> <li><code>pi_llm.models</code></li> <li><code>pi_llm.providers</code></li> <li><code>pi_llm.providers.faux</code></li> <li><code>pi_llm.providers.openai_responses</code></li> <li><code>pi_llm.providers.register_builtins</code></li> <li><code>pi_llm.providers.simple_options</code></li> <li><code>pi_llm.stream</code></li> <li><code>pi_llm.types</code></li> <li><code>pi_llm.utils</code></li> <li><code>pi_llm.utils.event_stream</code></li> <li><code>pi_llm.utils.overflow</code></li> <li><code>pi_llm.utils.validation</code></li> </ul>"},{"location":"pi_llm/reference/pi_llm/","title":"<code>pi_llm</code>","text":""},{"location":"pi_llm/reference/pi_llm/#pi_llm","title":"<code>pi_llm</code>","text":"<p>pi_llm \u2014 Unified LLM provider abstraction.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.AssistantContentItem","title":"<code>AssistantContentItem = Annotated[TextContent | ThinkingContent | ToolCall, Field(discriminator='type')]</code>  <code>module-attribute</code>","text":"<p>Content block within an assistant message (text, thinking, or tool call).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.AssistantMessageEvent","title":"<code>AssistantMessageEvent = StartEvent | TextStartEvent | TextDeltaEvent | TextEndEvent | ThinkingStartEvent | ThinkingDeltaEvent | ThinkingEndEvent | ToolCallStartEvent | ToolCallDeltaEvent | ToolCallEndEvent | DoneEvent | ErrorEvent</code>  <code>module-attribute</code>","text":"<p>Union of all streaming event types.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.CacheRetention","title":"<code>CacheRetention = Literal['none', 'short', 'long']</code>  <code>module-attribute</code>","text":"<p>Prompt cache retention preference.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.Message","title":"<code>Message = Annotated[UserMessage | AssistantMessage | ToolResultMessage, Field(discriminator='role')]</code>  <code>module-attribute</code>","text":"<p>Discriminated union of all message types (user, assistant, toolResult).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.StopReason","title":"<code>StopReason = Literal['stop', 'length', 'toolUse', 'error', 'aborted']</code>  <code>module-attribute</code>","text":"<p>Why an LLM response ended.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ThinkingLevel","title":"<code>ThinkingLevel = Literal['minimal', 'low', 'medium', 'high', 'xhigh']</code>  <code>module-attribute</code>","text":"<p>Extended thinking / reasoning effort level.</p> <p>Note: <code>\"off\"</code> is only available in the agent layer (see <code>AgentThinkingLevel</code>).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ToolResultContentItem","title":"<code>ToolResultContentItem = Annotated[TextContent | ImageContent, Field(discriminator='type')]</code>  <code>module-attribute</code>","text":"<p>Content block within a tool result message (text or image).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.UserContentItem","title":"<code>UserContentItem = Annotated[TextContent | ImageContent, Field(discriminator='type')]</code>  <code>module-attribute</code>","text":"<p>Content block within a user message (text or image).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.OpenAIResponsesOptions","title":"<code>OpenAIResponsesOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None, reasoning_effort=None, reasoning_summary=None, service_tier=None)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>StreamOptions</code></p> <p>Extended options for the OpenAI Responses API.</p> <p>Attributes:</p> Name Type Description <code>reasoning_effort</code> <code>str | None</code> <p>Reasoning effort level (e.g. <code>\"medium\"</code>).</p> <code>reasoning_summary</code> <code>str | None</code> <p>Reasoning summary mode (e.g. <code>\"auto\"</code>).</p> <code>service_tier</code> <code>str | None</code> <p>Service tier (e.g. <code>\"flex\"</code>, <code>\"priority\"</code>).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.AssistantMessage","title":"<code>AssistantMessage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A response from the LLM assistant.</p> <p>All fields have defaults to support incremental construction during streaming. The message is progressively built up as events arrive.</p> <p>Attributes:</p> Name Type Description <code>role</code> <code>Literal['assistant']</code> <p>Always <code>\"assistant\"</code>.</p> <code>content</code> <code>list[AssistantContentItem]</code> <p>List of content blocks (text, thinking, tool calls).</p> <code>api</code> <code>str</code> <p>API identifier (e.g. <code>\"openai-responses\"</code>).</p> <code>provider</code> <code>str</code> <p>Provider name (e.g. <code>\"openai\"</code>).</p> <code>model</code> <code>str</code> <p>Model identifier used for this response.</p> <code>response_id</code> <code>str | None</code> <p>Provider-assigned response ID.</p> <code>usage</code> <code>Usage</code> <p>Token usage and cost breakdown.</p> <code>stop_reason</code> <code>StopReason</code> <p>Why the response ended.</p> <code>error_message</code> <code>str | None</code> <p>Error description if <code>stop_reason</code> is <code>\"error\"</code>.</p> <code>timestamp</code> <code>int</code> <p>Unix timestamp in milliseconds.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.BaseModelWithAliases","title":"<code>BaseModelWithAliases</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base for all Pydantic models. Accepts both snake_case and camelCase.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.Context","title":"<code>Context</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Conversation context passed to the LLM.</p> <p>A serializable snapshot of the current conversation state including system instructions, message history, and available tools.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <code>str</code> <p>System-level instructions for the LLM.</p> <code>messages</code> <code>list[Message]</code> <p>Conversation history.</p> <code>tools</code> <code>list[Tool]</code> <p>Tools available to the LLM.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.CostBreakdown","title":"<code>CostBreakdown</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Dollar cost breakdown for a single LLM request.</p> <p>Attributes:</p> Name Type Description <code>input</code> <code>float</code> <p>Cost for input tokens.</p> <code>output</code> <code>float</code> <p>Cost for output tokens.</p> <code>cache_read</code> <code>float</code> <p>Cost for tokens read from prompt cache.</p> <code>cache_write</code> <code>float</code> <p>Cost for tokens written to prompt cache.</p> <code>total</code> <code>float</code> <p>Sum of all cost components.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.DoneEvent","title":"<code>DoneEvent(reason, message)</code>  <code>dataclass</code>","text":"<p>Emitted when streaming completes successfully.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ErrorEvent","title":"<code>ErrorEvent(reason, error)</code>  <code>dataclass</code>","text":"<p>Emitted when streaming ends due to an error or cancellation.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ImageContent","title":"<code>ImageContent</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A base64-encoded image within a message.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['image']</code> <p>Always <code>\"image\"</code>.</p> <code>data</code> <code>str</code> <p>Base64-encoded image data.</p> <code>mime_type</code> <code>str</code> <p>MIME type (e.g. <code>\"image/jpeg\"</code>).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.Model","title":"<code>Model</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>An LLM model descriptor.</p> <p>Constructed via <code>get_model()</code> or <code>fetch_models()</code>. Contains all metadata needed to make API calls: endpoint, pricing, capabilities.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Model identifier (e.g. <code>\"gpt-4o\"</code>).</p> <code>name</code> <code>str</code> <p>Display name.</p> <code>api</code> <code>str</code> <p>API backend identifier (e.g. <code>\"openai-responses\"</code>).</p> <code>provider</code> <code>str</code> <p>Provider name (e.g. <code>\"openai\"</code>).</p> <code>base_url</code> <code>str</code> <p>API base URL.</p> <code>reasoning</code> <code>bool</code> <p>Whether the model supports extended thinking.</p> <code>input_types</code> <code>list[str]</code> <p>Supported input modalities (e.g. <code>[\"text\", \"image\"]</code>).</p> <code>cost</code> <code>ModelCost | None</code> <p>Pricing per million tokens, or <code>None</code> if unknown.</p> <code>context_window</code> <code>int</code> <p>Maximum context window size in tokens.</p> <code>max_tokens</code> <code>int</code> <p>Maximum output tokens per request.</p> <code>headers</code> <code>dict[str, str] | None</code> <p>Custom HTTP headers to include in API requests.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ModelCost","title":"<code>ModelCost</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Cost per million tokens for a model.</p> <p>Attributes:</p> Name Type Description <code>input</code> <code>float</code> <p>Cost per 1M input tokens.</p> <code>output</code> <code>float</code> <p>Cost per 1M output tokens.</p> <code>cache_read</code> <code>float</code> <p>Cost per 1M cached-read tokens.</p> <code>cache_write</code> <code>float</code> <p>Cost per 1M cached-write tokens.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.SimpleStreamOptions","title":"<code>SimpleStreamOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None, reasoning=None, thinking_budgets=None)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>StreamOptions</code></p> <p>Options for the simplified streaming API (<code>stream_simple</code> / <code>complete_simple</code>).</p> <p>Extends <code>StreamOptions</code> with reasoning support.</p> <p>Attributes:</p> Name Type Description <code>reasoning</code> <code>ThinkingLevel | None</code> <p>Extended thinking level (<code>None</code> to disable).</p> <code>thinking_budgets</code> <code>dict[str, int] | None</code> <p>Custom token budgets per thinking level.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.StartEvent","title":"<code>StartEvent(partial)</code>  <code>dataclass</code>","text":"<p>Emitted when streaming begins.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.StreamOptions","title":"<code>StreamOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None)</code>  <code>dataclass</code>","text":"<p>Options for provider-level streaming calls.</p> <p>Attributes:</p> Name Type Description <code>temperature</code> <code>float | None</code> <p>Sampling temperature (<code>None</code> for provider default).</p> <code>max_tokens</code> <code>int | None</code> <p>Maximum output tokens.</p> <code>api_key</code> <code>str | None</code> <p>API key override (falls back to environment variable).</p> <code>cancel_event</code> <code>Event | None</code> <p>Event to signal request cancellation.</p> <code>cache_retention</code> <code>CacheRetention | None</code> <p>Prompt caching preference.</p> <code>session_id</code> <code>str | None</code> <p>Session ID for prompt caching.</p> <code>on_payload</code> <code>Callable[[Any, Model], Any] | None</code> <p>Hook called with raw API params before the request.</p> <code>headers</code> <code>dict[str, str] | None</code> <p>Additional HTTP headers.</p> <code>max_retry_delay_ms</code> <code>int | None</code> <p>Cap on retry backoff in milliseconds.</p> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary metadata passed to the provider.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.TextContent","title":"<code>TextContent</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A block of text content within a message.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['text']</code> <p>Always <code>\"text\"</code>.</p> <code>text</code> <code>str</code> <p>The text content.</p> <code>text_signature</code> <code>str | None</code> <p>Opaque signature for cross-provider handoff.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.TextDeltaEvent","title":"<code>TextDeltaEvent(content_index, delta, partial)</code>  <code>dataclass</code>","text":"<p>Emitted for each incremental chunk of text content.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.TextEndEvent","title":"<code>TextEndEvent(content_index, content, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a text content block finishes streaming.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.TextStartEvent","title":"<code>TextStartEvent(content_index, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a new text content block begins streaming.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ThinkingContent","title":"<code>ThinkingContent</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A block of model reasoning / \"thinking\" content.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['thinking']</code> <p>Always <code>\"thinking\"</code>.</p> <code>thinking</code> <code>str</code> <p>The reasoning text.</p> <code>thinking_signature</code> <code>str | None</code> <p>Opaque signature for cross-provider handoff.</p> <code>redacted</code> <code>bool | None</code> <p>Whether the content was redacted by the provider.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ThinkingDeltaEvent","title":"<code>ThinkingDeltaEvent(content_index, delta, partial)</code>  <code>dataclass</code>","text":"<p>Emitted for each incremental chunk of thinking content.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ThinkingEndEvent","title":"<code>ThinkingEndEvent(content_index, content, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a thinking/reasoning block finishes streaming.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ThinkingStartEvent","title":"<code>ThinkingStartEvent(content_index, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a thinking/reasoning block begins streaming.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.Tool","title":"<code>Tool</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A tool definition that can be passed to an LLM.</p> <p>Tools enable LLMs to interact with external systems by defining a name, description, and JSON Schema for parameters.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Unique identifier for the tool.</p> <code>description</code> <code>str</code> <p>Human-readable description the LLM uses to decide when to call this tool.</p> <code>parameters</code> <code>dict[str, Any]</code> <p>JSON Schema object describing the tool's parameters.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.Tool.from_pydantic","title":"<code>from_pydantic(name, description, model_class)</code>  <code>classmethod</code>","text":"<p>Create a Tool from a Pydantic model class.</p> <p>The model's JSON schema is used as the tool's parameter schema.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Tool name.</p> required <code>description</code> <code>str</code> <p>Tool description for the LLM.</p> required <code>model_class</code> <code>type[BaseModel]</code> <p>Pydantic model whose schema defines the parameters.</p> required <p>Returns:</p> Type Description <code>Tool</code> <p>A new Tool instance.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ToolCall","title":"<code>ToolCall</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>An LLM request to invoke a tool.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['toolCall']</code> <p>Always <code>\"toolCall\"</code>.</p> <code>id</code> <code>str</code> <p>Unique identifier for this tool call.</p> <code>name</code> <code>str</code> <p>Name of the tool to invoke.</p> <code>arguments</code> <code>dict[str, Any]</code> <p>Parsed arguments matching the tool's JSON Schema.</p> <code>thought_signature</code> <code>str | None</code> <p>Opaque signature for reasoning context.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ToolCallDeltaEvent","title":"<code>ToolCallDeltaEvent(content_index, delta, partial)</code>  <code>dataclass</code>","text":"<p>Emitted for each incremental chunk of tool call arguments.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ToolCallEndEvent","title":"<code>ToolCallEndEvent(content_index, tool_call, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call finishes streaming and arguments are complete.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ToolCallStartEvent","title":"<code>ToolCallStartEvent(content_index, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call begins streaming.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.ToolResultMessage","title":"<code>ToolResultMessage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>The result of executing a tool, sent back to the LLM.</p> <p>Attributes:</p> Name Type Description <code>role</code> <code>Literal['toolResult']</code> <p>Always <code>\"toolResult\"</code>.</p> <code>tool_call_id</code> <code>str</code> <p>ID of the <code>ToolCall</code> this result responds to.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool that was executed.</p> <code>content</code> <code>list[ToolResultContentItem]</code> <p>Result content blocks (text or images).</p> <code>details</code> <code>Any | None</code> <p>Arbitrary metadata about the execution.</p> <code>is_error</code> <code>bool</code> <p>Whether the tool execution failed.</p> <code>timestamp</code> <code>int</code> <p>Unix timestamp in milliseconds.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.Usage","title":"<code>Usage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Token usage for a single LLM request.</p> <p>Attributes:</p> Name Type Description <code>input</code> <code>int</code> <p>Number of input tokens.</p> <code>output</code> <code>int</code> <p>Number of output tokens.</p> <code>cache_read</code> <code>int</code> <p>Tokens read from prompt cache.</p> <code>cache_write</code> <code>int</code> <p>Tokens written to prompt cache.</p> <code>total_tokens</code> <code>int</code> <p>Sum of all token counts.</p> <code>cost</code> <code>CostBreakdown</code> <p>Dollar cost breakdown (populated by <code>calculate_cost</code>).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.UserMessage","title":"<code>UserMessage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A message from the user.</p> <p>Attributes:</p> Name Type Description <code>role</code> <code>Literal['user']</code> <p>Always <code>\"user\"</code>.</p> <code>content</code> <code>str | list[UserContentItem]</code> <p>Plain text string or a list of content blocks.</p> <code>timestamp</code> <code>int</code> <p>Unix timestamp in milliseconds.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.AssistantMessageEventStream","title":"<code>AssistantMessageEventStream()</code>","text":"<p>               Bases: <code>EventStream[AssistantMessageEvent, AssistantMessage]</code></p> <p>Event stream specialized for assistant message streaming.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.EventStream","title":"<code>EventStream(is_complete, extract_result)</code>","text":"<p>               Bases: <code>Generic[T, R]</code></p> <p>Push-based async iterable stream with a final result.</p> <p>The producer calls <code>push(event)</code> to emit events and <code>end()</code> to close the stream. The consumer uses <code>async for event in stream</code> to iterate over events and <code>await stream.result()</code> to get the final result.</p> <p>Parameters:</p> Name Type Description Default <code>is_complete</code> <code>Callable[[T], bool]</code> <p>Predicate that returns <code>True</code> for the terminal event.</p> required <code>extract_result</code> <code>Callable[[T], R]</code> <p>Extracts the final result from the terminal event.</p> required"},{"location":"pi_llm/reference/pi_llm/#pi_llm.EventStream.push","title":"<code>push(event)</code>","text":"<p>Push an event into the stream. Silently drops events after <code>end()</code>.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.EventStream.end","title":"<code>end(result=None)</code>","text":"<p>Signal end of stream. Sets result if not already set.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.EventStream.__aiter__","title":"<code>__aiter__()</code>  <code>async</code>","text":"<p>Async iterate over events until sentinel.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.EventStream.result","title":"<code>result()</code>  <code>async</code>","text":"<p>Await the final result. Resolves on completion event or end(result).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.clear_api_providers","title":"<code>clear_api_providers()</code>","text":"<p>Remove all registered providers.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.get_api_provider","title":"<code>get_api_provider(api)</code>","text":"<p>Look up a registered provider by API name.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.get_api_providers","title":"<code>get_api_providers()</code>","text":"<p>List all registered providers.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.register_api_provider","title":"<code>register_api_provider(api, stream, stream_simple, source_id=None)</code>","text":"<p>Register an API provider with stream implementations.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>str</code> <p>API name (e.g. <code>\"openai-responses\"</code>).</p> required <code>stream</code> <code>StreamFn</code> <p>Provider-specific stream function.</p> required <code>stream_simple</code> <code>StreamSimpleFn</code> <p>Simplified stream function with reasoning support.</p> required <code>source_id</code> <code>str | None</code> <p>Optional identifier for bulk unregistration.</p> <code>None</code>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.unregister_api_providers","title":"<code>unregister_api_providers(source_id)</code>","text":"<p>Remove all providers registered with the given source ID.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.get_env_api_key","title":"<code>get_env_api_key(provider)</code>","text":"<p>Get API key for provider from environment variables.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.calculate_cost","title":"<code>calculate_cost(model, usage)</code>","text":"<p>Calculate dollar cost from token usage and model pricing.</p> <p>Mutates <code>usage.cost</code> in-place. Does nothing if the model has no pricing.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>Model with pricing information.</p> required <code>usage</code> <code>Usage</code> <p>Token usage to calculate cost for.</p> required"},{"location":"pi_llm/reference/pi_llm/#pi_llm.fetch_models","title":"<code>fetch_models(provider='openai', api_key=None, base_url=None)</code>  <code>async</code>","text":"<p>Fetch available models from the provider's API.</p> <p>Each returned model is enriched with local pricing and metadata. This is optional \u2014 <code>get_model()</code> works without calling this.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name (default <code>\"openai\"</code>).</p> <code>'openai'</code> <code>api_key</code> <code>str | None</code> <p>API key (falls back to <code>OPENAI_API_KEY</code> env var).</p> <code>None</code> <code>base_url</code> <code>str | None</code> <p>Override the API base URL.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Model]</code> <p>List of <code>Model</code> instances available from the provider.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.get_model","title":"<code>get_model(provider, model_id, **overrides)</code>","text":"<p>Construct a Model from known metadata. Works offline.</p> <p>Unknown models get sensible defaults (128k context, 16k max_tokens, no pricing).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name (e.g. <code>\"openai\"</code>).</p> required <code>model_id</code> <code>str</code> <p>Model identifier (e.g. <code>\"gpt-4o\"</code>).</p> required <code>**overrides</code> <code>Any</code> <p>Override any <code>Model</code> field (e.g. <code>base_url</code>).</p> <code>{}</code> <p>Returns:</p> Type Description <code>Model</code> <p>A fully configured <code>Model</code> instance.</p> Example <p>model = get_model(\"openai\", \"gpt-4o\") model.context_window 128000</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.models_are_equal","title":"<code>models_are_equal(a, b)</code>","text":"<p>Check if two models are the same by comparing id and provider.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.supports_xhigh","title":"<code>supports_xhigh(model)</code>","text":"<p>Check if a model supports the <code>\"xhigh\"</code> thinking level.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.adjust_max_tokens_for_thinking","title":"<code>adjust_max_tokens_for_thinking(base_max_tokens, model_max_tokens, reasoning_level, custom_budgets=None)</code>","text":"<p>Calculate token limits for thinking-enabled models.</p> <p>Returns:</p> Type Description <code>tuple[int, int]</code> <p>A <code>(max_tokens, thinking_budget)</code> tuple.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.build_base_options","title":"<code>build_base_options(model, options=None, api_key=None)</code>","text":"<p>Map <code>SimpleStreamOptions</code> to base <code>StreamOptions</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model (used for default max_tokens).</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options to convert.</p> <code>None</code> <code>api_key</code> <code>str | None</code> <p>API key override.</p> <code>None</code> <p>Returns:</p> Type Description <code>StreamOptions</code> <p>A <code>StreamOptions</code> instance.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.clamp_reasoning","title":"<code>clamp_reasoning(effort)</code>","text":"<p>Clamp <code>\"xhigh\"</code> to <code>\"high\"</code> for models that don't support it.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.register_builtin_providers","title":"<code>register_builtin_providers()</code>","text":"<p>Register built-in API providers.</p> <p>Currently registers the OpenAI Responses API provider. Must be called before <code>stream_simple()</code> or <code>complete_simple()</code> will work.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.reset_api_providers","title":"<code>reset_api_providers()</code>","text":"<p>Clear all providers and re-register builtins.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.stream_openai_responses","title":"<code>stream_openai_responses(model, context, options=None)</code>","text":"<p>Stream from the OpenAI Responses API.</p> <p>Returns an event stream immediately and processes the API response in a background asyncio task.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>Model to use for the request.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>OpenAIResponsesOptions | None</code> <p>OpenAI-specific options.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessageEventStream</code> <p>An async iterable event stream of <code>AssistantMessageEvent</code>.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.stream_simple_openai_responses","title":"<code>stream_simple_openai_responses(model, context, options=None)</code>","text":"<p>Simplified entry point with automatic option building.</p> <p>Validates the API key, maps <code>SimpleStreamOptions</code> to <code>OpenAIResponsesOptions</code>, and delegates to <code>stream_openai_responses</code>.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.faux_assistant_message","title":"<code>faux_assistant_message(content, *, stop_reason='stop', error_message=None, response_id=None, timestamp=None)</code>","text":"<p>Build a complete <code>AssistantMessage</code> for scripted responses.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str | FauxContentBlock | list[FauxContentBlock]</code> <p>Text string, single content block, or list of blocks.</p> required <code>stop_reason</code> <code>str</code> <p>Stop reason (default <code>\"stop\"</code>).</p> <code>'stop'</code> <code>error_message</code> <code>str | None</code> <p>Error description if stop_reason is <code>\"error\"</code>.</p> <code>None</code> <code>response_id</code> <code>str | None</code> <p>Provider-assigned response ID.</p> <code>None</code> <code>timestamp</code> <code>int | None</code> <p>Unix timestamp in ms (defaults to current time).</p> <code>None</code>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.faux_text","title":"<code>faux_text(text)</code>","text":"<p>Create a <code>TextContent</code> block for scripted responses.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.faux_thinking","title":"<code>faux_thinking(thinking)</code>","text":"<p>Create a <code>ThinkingContent</code> block for scripted responses.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.faux_tool_call","title":"<code>faux_tool_call(name, arguments, *, id=None)</code>","text":"<p>Create a <code>ToolCall</code> block for scripted responses.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.register_faux_provider","title":"<code>register_faux_provider(options=None)</code>","text":"<p>Register a deterministic mock LLM provider.</p> <p>Returns a <code>FauxProviderRegistration</code> that lets you queue scripted <code>AssistantMessage</code> responses. Each call to the registered stream function pops the next response and simulates streaming events.</p> <p>Parameters:</p> Name Type Description Default <code>options</code> <code>RegisterFauxProviderOptions | None</code> <p>Configuration (API name, models, token speed, etc.).</p> <code>None</code> <p>Returns:</p> Type Description <code>FauxProviderRegistration</code> <p>A registration handle for managing responses and models.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.complete","title":"<code>complete(model, context, options=None)</code>  <code>async</code>","text":"<p>Non-streaming convenience wrapper around <code>stream()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>StreamOptions | None</code> <p>Provider-specific streaming options.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessage</code> <p>The completed assistant message.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.complete_simple","title":"<code>complete_simple(model, context, options=None)</code>  <code>async</code>","text":"<p>Non-streaming convenience wrapper around <code>stream_simple()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options including reasoning level.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessage</code> <p>The completed assistant message.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.stream_simple","title":"<code>stream_simple(model, context, options=None)</code>","text":"<p>Stream a response with automatic reasoning support.</p> <p>This is the recommended entry point for most use cases. It handles reasoning level mapping and option building automatically.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context (system prompt, messages, tools).</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options including reasoning level.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessageEventStream</code> <p>An async iterable event stream.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no provider is registered for the model's API.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.create_assistant_message_event_stream","title":"<code>create_assistant_message_event_stream()</code>","text":"<p>Factory function for AssistantMessageEventStream.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.is_context_overflow","title":"<code>is_context_overflow(message, context_window=None)</code>","text":"<p>Check if an assistant message represents a context overflow error.</p> <p>Handles two cases: 1. Error-based overflow: Most providers return stop_reason \"error\" with a    specific error message pattern. 2. Silent overflow: Some providers (e.g. z.ai) accept overflow silently.    For these, check if usage.input exceeds the context window.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.parse_streaming_json","title":"<code>parse_streaming_json(partial)</code>","text":"<p>Parse potentially incomplete JSON from streaming tool call arguments.</p> <p>Always returns a valid object \u2014 never raises.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.sanitize_surrogates","title":"<code>sanitize_surrogates(text)</code>","text":"<p>Replace lone Unicode surrogates with the replacement character (U+FFFD).</p> <p>Valid emoji and paired surrogates are preserved. Lone surrogates (which break JSON serialization in many API providers) are replaced.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.short_hash","title":"<code>short_hash(value)</code>","text":"<p>Generate a short deterministic hash (12-char hex).</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.validate_tool_arguments","title":"<code>validate_tool_arguments(tool, tool_call)</code>","text":"<p>Validate tool call arguments against JSON Schema.</p> <p>Returns a deep copy of validated arguments. Raises ValueError with formatted message on validation failure.</p>"},{"location":"pi_llm/reference/pi_llm/#pi_llm.validate_tool_call","title":"<code>validate_tool_call(tools, tool_call)</code>","text":"<p>Find a tool by name and validate the call's arguments.</p> <p>Parameters:</p> Name Type Description Default <code>tools</code> <code>list[Tool]</code> <p>Available tools to search.</p> required <code>tool_call</code> <code>ToolCall</code> <p>The tool call to validate.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A deep copy of the validated arguments.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the tool is not found or arguments are invalid.</p>"},{"location":"pi_llm/reference/pi_llm_api_registry/","title":"<code>pi_llm.api_registry</code>","text":""},{"location":"pi_llm/reference/pi_llm_api_registry/#pi_llm.api_registry","title":"<code>api_registry</code>","text":"<p>API provider registry.</p> <p>Module-level registry mapping API name strings to provider implementations. Providers are registered at startup via <code>register_builtin_providers()</code>.</p>"},{"location":"pi_llm/reference/pi_llm_api_registry/#pi_llm.api_registry.register_api_provider","title":"<code>register_api_provider(api, stream, stream_simple, source_id=None)</code>","text":"<p>Register an API provider with stream implementations.</p> <p>Parameters:</p> Name Type Description Default <code>api</code> <code>str</code> <p>API name (e.g. <code>\"openai-responses\"</code>).</p> required <code>stream</code> <code>StreamFn</code> <p>Provider-specific stream function.</p> required <code>stream_simple</code> <code>StreamSimpleFn</code> <p>Simplified stream function with reasoning support.</p> required <code>source_id</code> <code>str | None</code> <p>Optional identifier for bulk unregistration.</p> <code>None</code>"},{"location":"pi_llm/reference/pi_llm_api_registry/#pi_llm.api_registry.get_api_provider","title":"<code>get_api_provider(api)</code>","text":"<p>Look up a registered provider by API name.</p>"},{"location":"pi_llm/reference/pi_llm_api_registry/#pi_llm.api_registry.get_api_providers","title":"<code>get_api_providers()</code>","text":"<p>List all registered providers.</p>"},{"location":"pi_llm/reference/pi_llm_api_registry/#pi_llm.api_registry.unregister_api_providers","title":"<code>unregister_api_providers(source_id)</code>","text":"<p>Remove all providers registered with the given source ID.</p>"},{"location":"pi_llm/reference/pi_llm_api_registry/#pi_llm.api_registry.clear_api_providers","title":"<code>clear_api_providers()</code>","text":"<p>Remove all registered providers.</p>"},{"location":"pi_llm/reference/pi_llm_env_api_keys/","title":"<code>pi_llm.env_api_keys</code>","text":""},{"location":"pi_llm/reference/pi_llm_env_api_keys/#pi_llm.env_api_keys","title":"<code>env_api_keys</code>","text":"<p>Provider to environment variable mapping.</p> <p>Maps provider names to their conventional environment variable names for API key resolution.</p>"},{"location":"pi_llm/reference/pi_llm_env_api_keys/#pi_llm.env_api_keys.get_env_api_key","title":"<code>get_env_api_key(provider)</code>","text":"<p>Get API key for provider from environment variables.</p>"},{"location":"pi_llm/reference/pi_llm_model_pricing/","title":"<code>pi_llm.model_pricing</code>","text":""},{"location":"pi_llm/reference/pi_llm_model_pricing/#pi_llm.model_pricing","title":"<code>model_pricing</code>","text":"<p>Known model pricing and metadata.</p> <p>Sourced from the TypeScript model catalog (models.generated.ts). All models listed here use the <code>openai-responses</code> API via the <code>openai</code> provider.</p>"},{"location":"pi_llm/reference/pi_llm_models/","title":"<code>pi_llm.models</code>","text":""},{"location":"pi_llm/reference/pi_llm_models/#pi_llm.models","title":"<code>models</code>","text":"<p>Dynamic model catalog.</p> <p>Functions for constructing, fetching, and comparing LLM models.</p>"},{"location":"pi_llm/reference/pi_llm_models/#pi_llm.models.get_model","title":"<code>get_model(provider, model_id, **overrides)</code>","text":"<p>Construct a Model from known metadata. Works offline.</p> <p>Unknown models get sensible defaults (128k context, 16k max_tokens, no pricing).</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name (e.g. <code>\"openai\"</code>).</p> required <code>model_id</code> <code>str</code> <p>Model identifier (e.g. <code>\"gpt-4o\"</code>).</p> required <code>**overrides</code> <code>Any</code> <p>Override any <code>Model</code> field (e.g. <code>base_url</code>).</p> <code>{}</code> <p>Returns:</p> Type Description <code>Model</code> <p>A fully configured <code>Model</code> instance.</p> Example <p>model = get_model(\"openai\", \"gpt-4o\") model.context_window 128000</p>"},{"location":"pi_llm/reference/pi_llm_models/#pi_llm.models.fetch_models","title":"<code>fetch_models(provider='openai', api_key=None, base_url=None)</code>  <code>async</code>","text":"<p>Fetch available models from the provider's API.</p> <p>Each returned model is enriched with local pricing and metadata. This is optional \u2014 <code>get_model()</code> works without calling this.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>Provider name (default <code>\"openai\"</code>).</p> <code>'openai'</code> <code>api_key</code> <code>str | None</code> <p>API key (falls back to <code>OPENAI_API_KEY</code> env var).</p> <code>None</code> <code>base_url</code> <code>str | None</code> <p>Override the API base URL.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Model]</code> <p>List of <code>Model</code> instances available from the provider.</p>"},{"location":"pi_llm/reference/pi_llm_models/#pi_llm.models.calculate_cost","title":"<code>calculate_cost(model, usage)</code>","text":"<p>Calculate dollar cost from token usage and model pricing.</p> <p>Mutates <code>usage.cost</code> in-place. Does nothing if the model has no pricing.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>Model with pricing information.</p> required <code>usage</code> <code>Usage</code> <p>Token usage to calculate cost for.</p> required"},{"location":"pi_llm/reference/pi_llm_models/#pi_llm.models.supports_xhigh","title":"<code>supports_xhigh(model)</code>","text":"<p>Check if a model supports the <code>\"xhigh\"</code> thinking level.</p>"},{"location":"pi_llm/reference/pi_llm_models/#pi_llm.models.models_are_equal","title":"<code>models_are_equal(a, b)</code>","text":"<p>Check if two models are the same by comparing id and provider.</p>"},{"location":"pi_llm/reference/pi_llm_providers/","title":"<code>pi_llm.providers</code>","text":""},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers","title":"<code>providers</code>","text":"<p>pi_llm.providers \u2014 Provider implementations and helpers.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.OpenAIResponsesOptions","title":"<code>OpenAIResponsesOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None, reasoning_effort=None, reasoning_summary=None, service_tier=None)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>StreamOptions</code></p> <p>Extended options for the OpenAI Responses API.</p> <p>Attributes:</p> Name Type Description <code>reasoning_effort</code> <code>str | None</code> <p>Reasoning effort level (e.g. <code>\"medium\"</code>).</p> <code>reasoning_summary</code> <code>str | None</code> <p>Reasoning summary mode (e.g. <code>\"auto\"</code>).</p> <code>service_tier</code> <code>str | None</code> <p>Service tier (e.g. <code>\"flex\"</code>, <code>\"priority\"</code>).</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.stream_openai_responses","title":"<code>stream_openai_responses(model, context, options=None)</code>","text":"<p>Stream from the OpenAI Responses API.</p> <p>Returns an event stream immediately and processes the API response in a background asyncio task.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>Model to use for the request.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>OpenAIResponsesOptions | None</code> <p>OpenAI-specific options.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessageEventStream</code> <p>An async iterable event stream of <code>AssistantMessageEvent</code>.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.stream_simple_openai_responses","title":"<code>stream_simple_openai_responses(model, context, options=None)</code>","text":"<p>Simplified entry point with automatic option building.</p> <p>Validates the API key, maps <code>SimpleStreamOptions</code> to <code>OpenAIResponsesOptions</code>, and delegates to <code>stream_openai_responses</code>.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.register_builtin_providers","title":"<code>register_builtin_providers()</code>","text":"<p>Register built-in API providers.</p> <p>Currently registers the OpenAI Responses API provider. Must be called before <code>stream_simple()</code> or <code>complete_simple()</code> will work.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.reset_api_providers","title":"<code>reset_api_providers()</code>","text":"<p>Clear all providers and re-register builtins.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.adjust_max_tokens_for_thinking","title":"<code>adjust_max_tokens_for_thinking(base_max_tokens, model_max_tokens, reasoning_level, custom_budgets=None)</code>","text":"<p>Calculate token limits for thinking-enabled models.</p> <p>Returns:</p> Type Description <code>tuple[int, int]</code> <p>A <code>(max_tokens, thinking_budget)</code> tuple.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.build_base_options","title":"<code>build_base_options(model, options=None, api_key=None)</code>","text":"<p>Map <code>SimpleStreamOptions</code> to base <code>StreamOptions</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model (used for default max_tokens).</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options to convert.</p> <code>None</code> <code>api_key</code> <code>str | None</code> <p>API key override.</p> <code>None</code> <p>Returns:</p> Type Description <code>StreamOptions</code> <p>A <code>StreamOptions</code> instance.</p>"},{"location":"pi_llm/reference/pi_llm_providers/#pi_llm.providers.clamp_reasoning","title":"<code>clamp_reasoning(effort)</code>","text":"<p>Clamp <code>\"xhigh\"</code> to <code>\"high\"</code> for models that don't support it.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/","title":"<code>pi_llm.providers.faux</code>","text":""},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux","title":"<code>faux</code>","text":"<p>Deterministic mock LLM provider for testing.</p> <p>Provides <code>register_faux_provider()</code> which registers a fake API provider that returns scripted responses, simulating streaming events without hitting any real LLM API. Useful for unit and integration tests.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxContentBlock","title":"<code>FauxContentBlock = TextContent | ThinkingContent | ToolCall</code>  <code>module-attribute</code>","text":"<p>Content block types that can appear in a faux assistant message.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxResponseFactory","title":"<code>FauxResponseFactory = Callable[[Context, StreamOptions | None, dict[str, int], Model], 'AssistantMessage']</code>  <code>module-attribute</code>","text":"<p>Callable <code>(context, options, state, model) -&gt; AssistantMessage</code> that builds a response dynamically. Can also be async.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxResponseStep","title":"<code>FauxResponseStep = AssistantMessage | FauxResponseFactory</code>  <code>module-attribute</code>","text":"<p>A scripted response: either a static message or a factory function.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxModelDefinition","title":"<code>FauxModelDefinition(id, name=None, reasoning=False, input_types=(lambda: ['text', 'image'])(), cost=None, context_window=128000, max_tokens=16384)</code>  <code>dataclass</code>","text":"<p>Definition for a mock model.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.RegisterFauxProviderOptions","title":"<code>RegisterFauxProviderOptions(api=None, provider=None, models=None, tokens_per_second=None, token_size_min=None, token_size_max=None)</code>  <code>dataclass</code>","text":"<p>Options for <code>register_faux_provider()</code>.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxProviderRegistration","title":"<code>FauxProviderRegistration(api, models, state, *, _set_responses, _append_responses, _get_pending, _unregister)</code>","text":"<p>Handle returned by <code>register_faux_provider()</code>.</p> <p>Use this to configure scripted responses and inspect call state.</p> <p>Attributes:</p> Name Type Description <code>api</code> <p>The API name this provider was registered under.</p> <code>models</code> <p>List of mock <code>Model</code> instances.</p> <code>state</code> <p>Dict with <code>callCount</code> tracking total stream calls.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxProviderRegistration.get_model","title":"<code>get_model(model_id=None)</code>","text":"<p>Get a mock model by ID, or the first model if no ID given.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxProviderRegistration.set_responses","title":"<code>set_responses(responses)</code>","text":"<p>Replace the scripted response queue.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxProviderRegistration.append_responses","title":"<code>append_responses(responses)</code>","text":"<p>Add responses to the end of the queue.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxProviderRegistration.get_pending_response_count","title":"<code>get_pending_response_count()</code>","text":"<p>Return the number of queued responses remaining.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.FauxProviderRegistration.unregister","title":"<code>unregister()</code>","text":"<p>Remove this provider from the registry.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.faux_text","title":"<code>faux_text(text)</code>","text":"<p>Create a <code>TextContent</code> block for scripted responses.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.faux_thinking","title":"<code>faux_thinking(thinking)</code>","text":"<p>Create a <code>ThinkingContent</code> block for scripted responses.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.faux_tool_call","title":"<code>faux_tool_call(name, arguments, *, id=None)</code>","text":"<p>Create a <code>ToolCall</code> block for scripted responses.</p>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.faux_assistant_message","title":"<code>faux_assistant_message(content, *, stop_reason='stop', error_message=None, response_id=None, timestamp=None)</code>","text":"<p>Build a complete <code>AssistantMessage</code> for scripted responses.</p> <p>Parameters:</p> Name Type Description Default <code>content</code> <code>str | FauxContentBlock | list[FauxContentBlock]</code> <p>Text string, single content block, or list of blocks.</p> required <code>stop_reason</code> <code>str</code> <p>Stop reason (default <code>\"stop\"</code>).</p> <code>'stop'</code> <code>error_message</code> <code>str | None</code> <p>Error description if stop_reason is <code>\"error\"</code>.</p> <code>None</code> <code>response_id</code> <code>str | None</code> <p>Provider-assigned response ID.</p> <code>None</code> <code>timestamp</code> <code>int | None</code> <p>Unix timestamp in ms (defaults to current time).</p> <code>None</code>"},{"location":"pi_llm/reference/pi_llm_providers_faux/#pi_llm.providers.faux.register_faux_provider","title":"<code>register_faux_provider(options=None)</code>","text":"<p>Register a deterministic mock LLM provider.</p> <p>Returns a <code>FauxProviderRegistration</code> that lets you queue scripted <code>AssistantMessage</code> responses. Each call to the registered stream function pops the next response and simulates streaming events.</p> <p>Parameters:</p> Name Type Description Default <code>options</code> <code>RegisterFauxProviderOptions | None</code> <p>Configuration (API name, models, token speed, etc.).</p> <code>None</code> <p>Returns:</p> Type Description <code>FauxProviderRegistration</code> <p>A registration handle for managing responses and models.</p>"},{"location":"pi_llm/reference/pi_llm_providers_openai_responses/","title":"<code>pi_llm.providers.openai_responses</code>","text":""},{"location":"pi_llm/reference/pi_llm_providers_openai_responses/#pi_llm.providers.openai_responses","title":"<code>openai_responses</code>","text":"<p>OpenAI Responses API provider.</p> <p>Implements the streaming interface for OpenAI's Responses API.</p>"},{"location":"pi_llm/reference/pi_llm_providers_openai_responses/#pi_llm.providers.openai_responses.OpenAIResponsesOptions","title":"<code>OpenAIResponsesOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None, reasoning_effort=None, reasoning_summary=None, service_tier=None)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>StreamOptions</code></p> <p>Extended options for the OpenAI Responses API.</p> <p>Attributes:</p> Name Type Description <code>reasoning_effort</code> <code>str | None</code> <p>Reasoning effort level (e.g. <code>\"medium\"</code>).</p> <code>reasoning_summary</code> <code>str | None</code> <p>Reasoning summary mode (e.g. <code>\"auto\"</code>).</p> <code>service_tier</code> <code>str | None</code> <p>Service tier (e.g. <code>\"flex\"</code>, <code>\"priority\"</code>).</p>"},{"location":"pi_llm/reference/pi_llm_providers_openai_responses/#pi_llm.providers.openai_responses.stream_openai_responses","title":"<code>stream_openai_responses(model, context, options=None)</code>","text":"<p>Stream from the OpenAI Responses API.</p> <p>Returns an event stream immediately and processes the API response in a background asyncio task.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>Model to use for the request.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>OpenAIResponsesOptions | None</code> <p>OpenAI-specific options.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessageEventStream</code> <p>An async iterable event stream of <code>AssistantMessageEvent</code>.</p>"},{"location":"pi_llm/reference/pi_llm_providers_openai_responses/#pi_llm.providers.openai_responses.stream_simple_openai_responses","title":"<code>stream_simple_openai_responses(model, context, options=None)</code>","text":"<p>Simplified entry point with automatic option building.</p> <p>Validates the API key, maps <code>SimpleStreamOptions</code> to <code>OpenAIResponsesOptions</code>, and delegates to <code>stream_openai_responses</code>.</p>"},{"location":"pi_llm/reference/pi_llm_providers_register_builtins/","title":"<code>pi_llm.providers.register_builtins</code>","text":""},{"location":"pi_llm/reference/pi_llm_providers_register_builtins/#pi_llm.providers.register_builtins","title":"<code>register_builtins</code>","text":"<p>Built-in provider registration.</p> <p>Call <code>register_builtin_providers()</code> once at startup before using the streaming API.</p>"},{"location":"pi_llm/reference/pi_llm_providers_register_builtins/#pi_llm.providers.register_builtins.register_builtin_providers","title":"<code>register_builtin_providers()</code>","text":"<p>Register built-in API providers.</p> <p>Currently registers the OpenAI Responses API provider. Must be called before <code>stream_simple()</code> or <code>complete_simple()</code> will work.</p>"},{"location":"pi_llm/reference/pi_llm_providers_register_builtins/#pi_llm.providers.register_builtins.reset_api_providers","title":"<code>reset_api_providers()</code>","text":"<p>Clear all providers and re-register builtins.</p>"},{"location":"pi_llm/reference/pi_llm_providers_simple_options/","title":"<code>pi_llm.providers.simple_options</code>","text":""},{"location":"pi_llm/reference/pi_llm_providers_simple_options/#pi_llm.providers.simple_options","title":"<code>simple_options</code>","text":"<p>Option building helpers for providers.</p> <p>Maps <code>SimpleStreamOptions</code> to provider-specific options, handles reasoning level clamping, and adjusts token budgets for thinking-enabled models.</p>"},{"location":"pi_llm/reference/pi_llm_providers_simple_options/#pi_llm.providers.simple_options.build_base_options","title":"<code>build_base_options(model, options=None, api_key=None)</code>","text":"<p>Map <code>SimpleStreamOptions</code> to base <code>StreamOptions</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model (used for default max_tokens).</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options to convert.</p> <code>None</code> <code>api_key</code> <code>str | None</code> <p>API key override.</p> <code>None</code> <p>Returns:</p> Type Description <code>StreamOptions</code> <p>A <code>StreamOptions</code> instance.</p>"},{"location":"pi_llm/reference/pi_llm_providers_simple_options/#pi_llm.providers.simple_options.clamp_reasoning","title":"<code>clamp_reasoning(effort)</code>","text":"<p>Clamp <code>\"xhigh\"</code> to <code>\"high\"</code> for models that don't support it.</p>"},{"location":"pi_llm/reference/pi_llm_providers_simple_options/#pi_llm.providers.simple_options.adjust_max_tokens_for_thinking","title":"<code>adjust_max_tokens_for_thinking(base_max_tokens, model_max_tokens, reasoning_level, custom_budgets=None)</code>","text":"<p>Calculate token limits for thinking-enabled models.</p> <p>Returns:</p> Type Description <code>tuple[int, int]</code> <p>A <code>(max_tokens, thinking_budget)</code> tuple.</p>"},{"location":"pi_llm/reference/pi_llm_stream/","title":"<code>pi_llm.stream</code>","text":""},{"location":"pi_llm/reference/pi_llm_stream/#pi_llm.stream","title":"<code>stream</code>","text":"<p>Top-level streaming API.</p> <p>The four functions here are the primary interface for making LLM calls. Use <code>stream_simple</code> / <code>complete_simple</code> for most use cases. Use <code>stream</code> / <code>complete</code> when you need provider-specific options.</p>"},{"location":"pi_llm/reference/pi_llm_stream/#pi_llm.stream.stream","title":"<code>stream(model, context, options=None)</code>","text":"<p>Stream a response using provider-specific options.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context (system prompt, messages, tools).</p> required <code>options</code> <code>StreamOptions | None</code> <p>Provider-specific streaming options.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessageEventStream</code> <p>An async iterable event stream. Use <code>async for event in stream</code></p> <code>AssistantMessageEventStream</code> <p>to consume events, or <code>await stream.result()</code> for the final message.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no provider is registered for the model's API.</p>"},{"location":"pi_llm/reference/pi_llm_stream/#pi_llm.stream.complete","title":"<code>complete(model, context, options=None)</code>  <code>async</code>","text":"<p>Non-streaming convenience wrapper around <code>stream()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>StreamOptions | None</code> <p>Provider-specific streaming options.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessage</code> <p>The completed assistant message.</p>"},{"location":"pi_llm/reference/pi_llm_stream/#pi_llm.stream.stream_simple","title":"<code>stream_simple(model, context, options=None)</code>","text":"<p>Stream a response with automatic reasoning support.</p> <p>This is the recommended entry point for most use cases. It handles reasoning level mapping and option building automatically.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context (system prompt, messages, tools).</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options including reasoning level.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessageEventStream</code> <p>An async iterable event stream.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no provider is registered for the model's API.</p>"},{"location":"pi_llm/reference/pi_llm_stream/#pi_llm.stream.complete_simple","title":"<code>complete_simple(model, context, options=None)</code>  <code>async</code>","text":"<p>Non-streaming convenience wrapper around <code>stream_simple()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Model</code> <p>The model to use.</p> required <code>context</code> <code>Context</code> <p>Conversation context.</p> required <code>options</code> <code>SimpleStreamOptions | None</code> <p>Simplified options including reasoning level.</p> <code>None</code> <p>Returns:</p> Type Description <code>AssistantMessage</code> <p>The completed assistant message.</p>"},{"location":"pi_llm/reference/pi_llm_types/","title":"<code>pi_llm.types</code>","text":""},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types","title":"<code>types</code>","text":"<p>Core type definitions for pi_llm.</p> <p>Pydantic models, type aliases, streaming events, and configuration dataclasses used throughout the pi_llm library.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.StopReason","title":"<code>StopReason = Literal['stop', 'length', 'toolUse', 'error', 'aborted']</code>  <code>module-attribute</code>","text":"<p>Why an LLM response ended.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ThinkingLevel","title":"<code>ThinkingLevel = Literal['minimal', 'low', 'medium', 'high', 'xhigh']</code>  <code>module-attribute</code>","text":"<p>Extended thinking / reasoning effort level.</p> <p>Note: <code>\"off\"</code> is only available in the agent layer (see <code>AgentThinkingLevel</code>).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.CacheRetention","title":"<code>CacheRetention = Literal['none', 'short', 'long']</code>  <code>module-attribute</code>","text":"<p>Prompt cache retention preference.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.UserContentItem","title":"<code>UserContentItem = Annotated[TextContent | ImageContent, Field(discriminator='type')]</code>  <code>module-attribute</code>","text":"<p>Content block within a user message (text or image).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.AssistantContentItem","title":"<code>AssistantContentItem = Annotated[TextContent | ThinkingContent | ToolCall, Field(discriminator='type')]</code>  <code>module-attribute</code>","text":"<p>Content block within an assistant message (text, thinking, or tool call).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ToolResultContentItem","title":"<code>ToolResultContentItem = Annotated[TextContent | ImageContent, Field(discriminator='type')]</code>  <code>module-attribute</code>","text":"<p>Content block within a tool result message (text or image).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.Message","title":"<code>Message = Annotated[UserMessage | AssistantMessage | ToolResultMessage, Field(discriminator='role')]</code>  <code>module-attribute</code>","text":"<p>Discriminated union of all message types (user, assistant, toolResult).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.AssistantMessageEvent","title":"<code>AssistantMessageEvent = StartEvent | TextStartEvent | TextDeltaEvent | TextEndEvent | ThinkingStartEvent | ThinkingDeltaEvent | ThinkingEndEvent | ToolCallStartEvent | ToolCallDeltaEvent | ToolCallEndEvent | DoneEvent | ErrorEvent</code>  <code>module-attribute</code>","text":"<p>Union of all streaming event types.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.BaseModelWithAliases","title":"<code>BaseModelWithAliases</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base for all Pydantic models. Accepts both snake_case and camelCase.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.CostBreakdown","title":"<code>CostBreakdown</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Dollar cost breakdown for a single LLM request.</p> <p>Attributes:</p> Name Type Description <code>input</code> <code>float</code> <p>Cost for input tokens.</p> <code>output</code> <code>float</code> <p>Cost for output tokens.</p> <code>cache_read</code> <code>float</code> <p>Cost for tokens read from prompt cache.</p> <code>cache_write</code> <code>float</code> <p>Cost for tokens written to prompt cache.</p> <code>total</code> <code>float</code> <p>Sum of all cost components.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.Usage","title":"<code>Usage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Token usage for a single LLM request.</p> <p>Attributes:</p> Name Type Description <code>input</code> <code>int</code> <p>Number of input tokens.</p> <code>output</code> <code>int</code> <p>Number of output tokens.</p> <code>cache_read</code> <code>int</code> <p>Tokens read from prompt cache.</p> <code>cache_write</code> <code>int</code> <p>Tokens written to prompt cache.</p> <code>total_tokens</code> <code>int</code> <p>Sum of all token counts.</p> <code>cost</code> <code>CostBreakdown</code> <p>Dollar cost breakdown (populated by <code>calculate_cost</code>).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ModelCost","title":"<code>ModelCost</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Cost per million tokens for a model.</p> <p>Attributes:</p> Name Type Description <code>input</code> <code>float</code> <p>Cost per 1M input tokens.</p> <code>output</code> <code>float</code> <p>Cost per 1M output tokens.</p> <code>cache_read</code> <code>float</code> <p>Cost per 1M cached-read tokens.</p> <code>cache_write</code> <code>float</code> <p>Cost per 1M cached-write tokens.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.TextContent","title":"<code>TextContent</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A block of text content within a message.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['text']</code> <p>Always <code>\"text\"</code>.</p> <code>text</code> <code>str</code> <p>The text content.</p> <code>text_signature</code> <code>str | None</code> <p>Opaque signature for cross-provider handoff.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ThinkingContent","title":"<code>ThinkingContent</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A block of model reasoning / \"thinking\" content.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['thinking']</code> <p>Always <code>\"thinking\"</code>.</p> <code>thinking</code> <code>str</code> <p>The reasoning text.</p> <code>thinking_signature</code> <code>str | None</code> <p>Opaque signature for cross-provider handoff.</p> <code>redacted</code> <code>bool | None</code> <p>Whether the content was redacted by the provider.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ImageContent","title":"<code>ImageContent</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A base64-encoded image within a message.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['image']</code> <p>Always <code>\"image\"</code>.</p> <code>data</code> <code>str</code> <p>Base64-encoded image data.</p> <code>mime_type</code> <code>str</code> <p>MIME type (e.g. <code>\"image/jpeg\"</code>).</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ToolCall","title":"<code>ToolCall</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>An LLM request to invoke a tool.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>Literal['toolCall']</code> <p>Always <code>\"toolCall\"</code>.</p> <code>id</code> <code>str</code> <p>Unique identifier for this tool call.</p> <code>name</code> <code>str</code> <p>Name of the tool to invoke.</p> <code>arguments</code> <code>dict[str, Any]</code> <p>Parsed arguments matching the tool's JSON Schema.</p> <code>thought_signature</code> <code>str | None</code> <p>Opaque signature for reasoning context.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.UserMessage","title":"<code>UserMessage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A message from the user.</p> <p>Attributes:</p> Name Type Description <code>role</code> <code>Literal['user']</code> <p>Always <code>\"user\"</code>.</p> <code>content</code> <code>str | list[UserContentItem]</code> <p>Plain text string or a list of content blocks.</p> <code>timestamp</code> <code>int</code> <p>Unix timestamp in milliseconds.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.AssistantMessage","title":"<code>AssistantMessage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A response from the LLM assistant.</p> <p>All fields have defaults to support incremental construction during streaming. The message is progressively built up as events arrive.</p> <p>Attributes:</p> Name Type Description <code>role</code> <code>Literal['assistant']</code> <p>Always <code>\"assistant\"</code>.</p> <code>content</code> <code>list[AssistantContentItem]</code> <p>List of content blocks (text, thinking, tool calls).</p> <code>api</code> <code>str</code> <p>API identifier (e.g. <code>\"openai-responses\"</code>).</p> <code>provider</code> <code>str</code> <p>Provider name (e.g. <code>\"openai\"</code>).</p> <code>model</code> <code>str</code> <p>Model identifier used for this response.</p> <code>response_id</code> <code>str | None</code> <p>Provider-assigned response ID.</p> <code>usage</code> <code>Usage</code> <p>Token usage and cost breakdown.</p> <code>stop_reason</code> <code>StopReason</code> <p>Why the response ended.</p> <code>error_message</code> <code>str | None</code> <p>Error description if <code>stop_reason</code> is <code>\"error\"</code>.</p> <code>timestamp</code> <code>int</code> <p>Unix timestamp in milliseconds.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ToolResultMessage","title":"<code>ToolResultMessage</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>The result of executing a tool, sent back to the LLM.</p> <p>Attributes:</p> Name Type Description <code>role</code> <code>Literal['toolResult']</code> <p>Always <code>\"toolResult\"</code>.</p> <code>tool_call_id</code> <code>str</code> <p>ID of the <code>ToolCall</code> this result responds to.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool that was executed.</p> <code>content</code> <code>list[ToolResultContentItem]</code> <p>Result content blocks (text or images).</p> <code>details</code> <code>Any | None</code> <p>Arbitrary metadata about the execution.</p> <code>is_error</code> <code>bool</code> <p>Whether the tool execution failed.</p> <code>timestamp</code> <code>int</code> <p>Unix timestamp in milliseconds.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.Tool","title":"<code>Tool</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>A tool definition that can be passed to an LLM.</p> <p>Tools enable LLMs to interact with external systems by defining a name, description, and JSON Schema for parameters.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Unique identifier for the tool.</p> <code>description</code> <code>str</code> <p>Human-readable description the LLM uses to decide when to call this tool.</p> <code>parameters</code> <code>dict[str, Any]</code> <p>JSON Schema object describing the tool's parameters.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.Tool.from_pydantic","title":"<code>from_pydantic(name, description, model_class)</code>  <code>classmethod</code>","text":"<p>Create a Tool from a Pydantic model class.</p> <p>The model's JSON schema is used as the tool's parameter schema.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Tool name.</p> required <code>description</code> <code>str</code> <p>Tool description for the LLM.</p> required <code>model_class</code> <code>type[BaseModel]</code> <p>Pydantic model whose schema defines the parameters.</p> required <p>Returns:</p> Type Description <code>Tool</code> <p>A new Tool instance.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.Model","title":"<code>Model</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>An LLM model descriptor.</p> <p>Constructed via <code>get_model()</code> or <code>fetch_models()</code>. Contains all metadata needed to make API calls: endpoint, pricing, capabilities.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Model identifier (e.g. <code>\"gpt-4o\"</code>).</p> <code>name</code> <code>str</code> <p>Display name.</p> <code>api</code> <code>str</code> <p>API backend identifier (e.g. <code>\"openai-responses\"</code>).</p> <code>provider</code> <code>str</code> <p>Provider name (e.g. <code>\"openai\"</code>).</p> <code>base_url</code> <code>str</code> <p>API base URL.</p> <code>reasoning</code> <code>bool</code> <p>Whether the model supports extended thinking.</p> <code>input_types</code> <code>list[str]</code> <p>Supported input modalities (e.g. <code>[\"text\", \"image\"]</code>).</p> <code>cost</code> <code>ModelCost | None</code> <p>Pricing per million tokens, or <code>None</code> if unknown.</p> <code>context_window</code> <code>int</code> <p>Maximum context window size in tokens.</p> <code>max_tokens</code> <code>int</code> <p>Maximum output tokens per request.</p> <code>headers</code> <code>dict[str, str] | None</code> <p>Custom HTTP headers to include in API requests.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.Context","title":"<code>Context</code>","text":"<p>               Bases: <code>BaseModelWithAliases</code></p> <p>Conversation context passed to the LLM.</p> <p>A serializable snapshot of the current conversation state including system instructions, message history, and available tools.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <code>str</code> <p>System-level instructions for the LLM.</p> <code>messages</code> <code>list[Message]</code> <p>Conversation history.</p> <code>tools</code> <code>list[Tool]</code> <p>Tools available to the LLM.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.StreamOptions","title":"<code>StreamOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None)</code>  <code>dataclass</code>","text":"<p>Options for provider-level streaming calls.</p> <p>Attributes:</p> Name Type Description <code>temperature</code> <code>float | None</code> <p>Sampling temperature (<code>None</code> for provider default).</p> <code>max_tokens</code> <code>int | None</code> <p>Maximum output tokens.</p> <code>api_key</code> <code>str | None</code> <p>API key override (falls back to environment variable).</p> <code>cancel_event</code> <code>Event | None</code> <p>Event to signal request cancellation.</p> <code>cache_retention</code> <code>CacheRetention | None</code> <p>Prompt caching preference.</p> <code>session_id</code> <code>str | None</code> <p>Session ID for prompt caching.</p> <code>on_payload</code> <code>Callable[[Any, Model], Any] | None</code> <p>Hook called with raw API params before the request.</p> <code>headers</code> <code>dict[str, str] | None</code> <p>Additional HTTP headers.</p> <code>max_retry_delay_ms</code> <code>int | None</code> <p>Cap on retry backoff in milliseconds.</p> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary metadata passed to the provider.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.SimpleStreamOptions","title":"<code>SimpleStreamOptions(temperature=None, max_tokens=None, api_key=None, cancel_event=None, cache_retention=None, session_id=None, on_payload=None, headers=None, max_retry_delay_ms=None, metadata=None, reasoning=None, thinking_budgets=None)</code>  <code>dataclass</code>","text":"<p>               Bases: <code>StreamOptions</code></p> <p>Options for the simplified streaming API (<code>stream_simple</code> / <code>complete_simple</code>).</p> <p>Extends <code>StreamOptions</code> with reasoning support.</p> <p>Attributes:</p> Name Type Description <code>reasoning</code> <code>ThinkingLevel | None</code> <p>Extended thinking level (<code>None</code> to disable).</p> <code>thinking_budgets</code> <code>dict[str, int] | None</code> <p>Custom token budgets per thinking level.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.StartEvent","title":"<code>StartEvent(partial)</code>  <code>dataclass</code>","text":"<p>Emitted when streaming begins.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.TextStartEvent","title":"<code>TextStartEvent(content_index, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a new text content block begins streaming.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.TextDeltaEvent","title":"<code>TextDeltaEvent(content_index, delta, partial)</code>  <code>dataclass</code>","text":"<p>Emitted for each incremental chunk of text content.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.TextEndEvent","title":"<code>TextEndEvent(content_index, content, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a text content block finishes streaming.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ThinkingStartEvent","title":"<code>ThinkingStartEvent(content_index, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a thinking/reasoning block begins streaming.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ThinkingDeltaEvent","title":"<code>ThinkingDeltaEvent(content_index, delta, partial)</code>  <code>dataclass</code>","text":"<p>Emitted for each incremental chunk of thinking content.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ThinkingEndEvent","title":"<code>ThinkingEndEvent(content_index, content, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a thinking/reasoning block finishes streaming.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ToolCallStartEvent","title":"<code>ToolCallStartEvent(content_index, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call begins streaming.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ToolCallDeltaEvent","title":"<code>ToolCallDeltaEvent(content_index, delta, partial)</code>  <code>dataclass</code>","text":"<p>Emitted for each incremental chunk of tool call arguments.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ToolCallEndEvent","title":"<code>ToolCallEndEvent(content_index, tool_call, partial)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call finishes streaming and arguments are complete.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.DoneEvent","title":"<code>DoneEvent(reason, message)</code>  <code>dataclass</code>","text":"<p>Emitted when streaming completes successfully.</p>"},{"location":"pi_llm/reference/pi_llm_types/#pi_llm.types.ErrorEvent","title":"<code>ErrorEvent(reason, error)</code>  <code>dataclass</code>","text":"<p>Emitted when streaming ends due to an error or cancellation.</p>"},{"location":"pi_llm/reference/pi_llm_utils/","title":"<code>pi_llm.utils</code>","text":""},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils","title":"<code>utils</code>","text":"<p>pi_llm.utils \u2014 Streaming infrastructure and utility functions.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.AssistantMessageEventStream","title":"<code>AssistantMessageEventStream()</code>","text":"<p>               Bases: <code>EventStream[AssistantMessageEvent, AssistantMessage]</code></p> <p>Event stream specialized for assistant message streaming.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.EventStream","title":"<code>EventStream(is_complete, extract_result)</code>","text":"<p>               Bases: <code>Generic[T, R]</code></p> <p>Push-based async iterable stream with a final result.</p> <p>The producer calls <code>push(event)</code> to emit events and <code>end()</code> to close the stream. The consumer uses <code>async for event in stream</code> to iterate over events and <code>await stream.result()</code> to get the final result.</p> <p>Parameters:</p> Name Type Description Default <code>is_complete</code> <code>Callable[[T], bool]</code> <p>Predicate that returns <code>True</code> for the terminal event.</p> required <code>extract_result</code> <code>Callable[[T], R]</code> <p>Extracts the final result from the terminal event.</p> required"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.EventStream.push","title":"<code>push(event)</code>","text":"<p>Push an event into the stream. Silently drops events after <code>end()</code>.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.EventStream.end","title":"<code>end(result=None)</code>","text":"<p>Signal end of stream. Sets result if not already set.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.EventStream.__aiter__","title":"<code>__aiter__()</code>  <code>async</code>","text":"<p>Async iterate over events until sentinel.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.EventStream.result","title":"<code>result()</code>  <code>async</code>","text":"<p>Await the final result. Resolves on completion event or end(result).</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.create_assistant_message_event_stream","title":"<code>create_assistant_message_event_stream()</code>","text":"<p>Factory function for AssistantMessageEventStream.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.short_hash","title":"<code>short_hash(value)</code>","text":"<p>Generate a short deterministic hash (12-char hex).</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.parse_streaming_json","title":"<code>parse_streaming_json(partial)</code>","text":"<p>Parse potentially incomplete JSON from streaming tool call arguments.</p> <p>Always returns a valid object \u2014 never raises.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.is_context_overflow","title":"<code>is_context_overflow(message, context_window=None)</code>","text":"<p>Check if an assistant message represents a context overflow error.</p> <p>Handles two cases: 1. Error-based overflow: Most providers return stop_reason \"error\" with a    specific error message pattern. 2. Silent overflow: Some providers (e.g. z.ai) accept overflow silently.    For these, check if usage.input exceeds the context window.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.sanitize_surrogates","title":"<code>sanitize_surrogates(text)</code>","text":"<p>Replace lone Unicode surrogates with the replacement character (U+FFFD).</p> <p>Valid emoji and paired surrogates are preserved. Lone surrogates (which break JSON serialization in many API providers) are replaced.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.validate_tool_arguments","title":"<code>validate_tool_arguments(tool, tool_call)</code>","text":"<p>Validate tool call arguments against JSON Schema.</p> <p>Returns a deep copy of validated arguments. Raises ValueError with formatted message on validation failure.</p>"},{"location":"pi_llm/reference/pi_llm_utils/#pi_llm.utils.validate_tool_call","title":"<code>validate_tool_call(tools, tool_call)</code>","text":"<p>Find a tool by name and validate the call's arguments.</p> <p>Parameters:</p> Name Type Description Default <code>tools</code> <code>list[Tool]</code> <p>Available tools to search.</p> required <code>tool_call</code> <code>ToolCall</code> <p>The tool call to validate.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A deep copy of the validated arguments.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the tool is not found or arguments are invalid.</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/","title":"<code>pi_llm.utils.event_stream</code>","text":""},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream","title":"<code>event_stream</code>","text":"<p>Push-based async iterable event stream.</p> <p>Provides <code>EventStream</code>, a generic producer/consumer bridge for streaming LLM responses as typed events.</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.EventStream","title":"<code>EventStream(is_complete, extract_result)</code>","text":"<p>               Bases: <code>Generic[T, R]</code></p> <p>Push-based async iterable stream with a final result.</p> <p>The producer calls <code>push(event)</code> to emit events and <code>end()</code> to close the stream. The consumer uses <code>async for event in stream</code> to iterate over events and <code>await stream.result()</code> to get the final result.</p> <p>Parameters:</p> Name Type Description Default <code>is_complete</code> <code>Callable[[T], bool]</code> <p>Predicate that returns <code>True</code> for the terminal event.</p> required <code>extract_result</code> <code>Callable[[T], R]</code> <p>Extracts the final result from the terminal event.</p> required"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.EventStream.push","title":"<code>push(event)</code>","text":"<p>Push an event into the stream. Silently drops events after <code>end()</code>.</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.EventStream.end","title":"<code>end(result=None)</code>","text":"<p>Signal end of stream. Sets result if not already set.</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.EventStream.__aiter__","title":"<code>__aiter__()</code>  <code>async</code>","text":"<p>Async iterate over events until sentinel.</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.EventStream.result","title":"<code>result()</code>  <code>async</code>","text":"<p>Await the final result. Resolves on completion event or end(result).</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.AssistantMessageEventStream","title":"<code>AssistantMessageEventStream()</code>","text":"<p>               Bases: <code>EventStream[AssistantMessageEvent, AssistantMessage]</code></p> <p>Event stream specialized for assistant message streaming.</p>"},{"location":"pi_llm/reference/pi_llm_utils_event_stream/#pi_llm.utils.event_stream.create_assistant_message_event_stream","title":"<code>create_assistant_message_event_stream()</code>","text":"<p>Factory function for AssistantMessageEventStream.</p>"},{"location":"pi_llm/reference/pi_llm_utils_overflow/","title":"<code>pi_llm.utils.overflow</code>","text":""},{"location":"pi_llm/reference/pi_llm_utils_overflow/#pi_llm.utils.overflow","title":"<code>overflow</code>","text":"<p>Context window overflow detection across providers.</p> <p>Detects when an LLM response indicates the input exceeded the model's context window, covering 20+ provider-specific error patterns.</p>"},{"location":"pi_llm/reference/pi_llm_utils_overflow/#pi_llm.utils.overflow.is_context_overflow","title":"<code>is_context_overflow(message, context_window=None)</code>","text":"<p>Check if an assistant message represents a context overflow error.</p> <p>Handles two cases: 1. Error-based overflow: Most providers return stop_reason \"error\" with a    specific error message pattern. 2. Silent overflow: Some providers (e.g. z.ai) accept overflow silently.    For these, check if usage.input exceeds the context window.</p>"},{"location":"pi_llm/reference/pi_llm_utils_validation/","title":"<code>pi_llm.utils.validation</code>","text":""},{"location":"pi_llm/reference/pi_llm_utils_validation/#pi_llm.utils.validation","title":"<code>validation</code>","text":"<p>Tool call argument validation using jsonschema.</p> <p>Validates tool call arguments against their JSON Schema definitions.</p>"},{"location":"pi_llm/reference/pi_llm_utils_validation/#pi_llm.utils.validation.validate_tool_call","title":"<code>validate_tool_call(tools, tool_call)</code>","text":"<p>Find a tool by name and validate the call's arguments.</p> <p>Parameters:</p> Name Type Description Default <code>tools</code> <code>list[Tool]</code> <p>Available tools to search.</p> required <code>tool_call</code> <code>ToolCall</code> <p>The tool call to validate.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A deep copy of the validated arguments.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the tool is not found or arguments are invalid.</p>"},{"location":"pi_llm/reference/pi_llm_utils_validation/#pi_llm.utils.validation.validate_tool_arguments","title":"<code>validate_tool_arguments(tool, tool_call)</code>","text":"<p>Validate tool call arguments against JSON Schema.</p> <p>Returns a deep copy of validated arguments. Raises ValueError with formatted message on validation failure.</p>"},{"location":"pi_llm_agent/","title":"pi-llm-agent","text":"<p>General-purpose agent framework with tool execution and event streaming.</p> <p>Built on pi-llm, pi-llm-agent provides a stateful <code>Agent</code> class that manages the multi-turn loop of prompting an LLM, executing tool calls, and repeating until the task is done.</p>"},{"location":"pi_llm_agent/#key-concepts","title":"Key Concepts","text":"<ul> <li>Agent Lifecycle \u2014 The Agent class, state, and lifecycle methods</li> <li>Agent Loop \u2014 The low-level multi-turn execution engine</li> <li>Events \u2014 The 10 agent event types</li> <li>Tools \u2014 Creating tools with <code>AgentTool</code></li> <li>Hooks \u2014 <code>before_tool_call</code> and <code>after_tool_call</code></li> <li>Steering &amp; Follow-up \u2014 Interrupt and extend agent runs</li> <li>Cancellation \u2014 Cooperative cancellation</li> </ul>"},{"location":"pi_llm_agent/#how-to-guides","title":"How-to Guides","text":"<ul> <li>Build a Research Agent</li> <li>Custom Message Types</li> <li>Control Tool Execution</li> </ul>"},{"location":"pi_llm_agent/#api-reference","title":"API Reference","text":"<p>See the auto-generated API Reference for complete documentation of all public types and functions.</p>"},{"location":"pi_llm_agent/quickstart/","title":"pi-llm-agent Quickstart","text":"<p>This guide walks you through building your first agent: creating tools, configuring the agent, subscribing to events, and sending prompts.</p>"},{"location":"pi_llm_agent/quickstart/#installation","title":"Installation","text":"<pre><code>pip install pi-llm-agent\n</code></pre> <p>pi-llm-agent depends on pi-llm, which will be installed automatically.</p>"},{"location":"pi_llm_agent/quickstart/#your-first-agent","title":"Your first agent","text":"<p>An <code>Agent</code> manages the multi-turn loop: it sends a prompt to the LLM, executes any tool calls, feeds the results back, and repeats until the task is done.</p> <pre><code>import asyncio\nimport os\n\nfrom pi_llm_agent import (\n    Agent,\n    AgentOptions,\n    AgentTool,\n    AgentToolResult,\n    InitialAgentState,\n)\nfrom pi_llm import TextContent, get_model, stream_simple\nfrom pi_llm.providers import register_builtin_providers\n\nregister_builtin_providers()\n\n\n# 1. Define a tool\nclass GreetTool(AgentTool):\n    def __init__(self):\n        super().__init__(\n            name=\"greet\",\n            label=\"Greet\",\n            description=\"Generate a greeting for a person\",\n            parameters={\n                \"type\": \"object\",\n                \"properties\": {\n                    \"name\": {\"type\": \"string\", \"description\": \"Person's name\"},\n                },\n                \"required\": [\"name\"],\n            },\n        )\n\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        name = params[\"name\"]\n        return AgentToolResult(\n            content=[TextContent(text=f\"Hello, {name}! Welcome.\")]\n        )\n\n\n# 2. Create the agent\nagent = Agent(AgentOptions(\n    initial_state=InitialAgentState(\n        model=get_model(\"openai\", \"gpt-4o\"),\n        system_prompt=\"You are a helpful assistant. Use the greet tool when asked to greet someone.\",\n        tools=[GreetTool()],\n    ),\n    get_api_key=lambda provider: os.environ.get(\"OPENAI_API_KEY\"),\n))\n\n\n# 3. Subscribe to events\ndef on_event(event, cancellation):\n    match event.type:\n        case \"message_update\":\n            # Incremental text from the LLM\n            inner = event.assistant_message_event\n            if hasattr(inner, \"delta\") and inner.type == \"text_delta\":\n                print(inner.delta, end=\"\", flush=True)\n        case \"tool_execution_start\":\n            print(f\"\\n[Calling tool: {event.tool_name}]\")\n        case \"tool_execution_end\":\n            print(f\"[Tool result: {event.result.content[0].text}]\")\n        case \"agent_end\":\n            print(\"\\n[Agent finished]\")\n\n\nagent.subscribe(on_event)\n\n\n# 4. Send a prompt\nasync def main():\n    await agent.prompt(\"Please greet Alice and Bob.\")\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"pi_llm_agent/quickstart/#how-it-works","title":"How it works","text":"<ol> <li><code>agent.prompt(\"...\")</code> adds a <code>UserMessage</code> to the transcript, then runs the agent loop.</li> <li>The agent loop calls the LLM via <code>stream_simple</code>, streaming events.</li> <li>When the LLM emits tool calls, the agent executes them (in parallel by default).</li> <li>Tool results are appended to the transcript and the LLM is called again.</li> <li>This repeats until the LLM responds with text only (no tool calls).</li> <li>Events are emitted at every stage so subscribers can react in real time.</li> </ol>"},{"location":"pi_llm_agent/quickstart/#reading-agent-state","title":"Reading agent state","text":"<p>The <code>agent.state</code> object gives you access to the current conversation:</p> <pre><code># After prompt completes:\nprint(f\"Total messages: {len(agent.state.messages)}\")\nprint(f\"Is streaming: {agent.state.is_streaming}\")\nprint(f\"Model: {agent.state.model.id}\")\n</code></pre>"},{"location":"pi_llm_agent/quickstart/#using-from_function-for-simple-tools","title":"Using from_function for simple tools","text":"<p>For tools that do not need a class, use the factory method:</p> <pre><code>from pi_llm_agent import AgentTool, AgentToolResult\nfrom pi_llm import TextContent\n\n\nasync def reverse_text(tool_call_id, params, cancellation=None, on_update=None):\n    text = params[\"text\"]\n    return AgentToolResult(\n        content=[TextContent(text=text[::-1])]\n    )\n\n\nreverse_tool = AgentTool.from_function(\n    name=\"reverse_text\",\n    label=\"Reverse Text\",\n    description=\"Reverse a string of text\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"text\": {\"type\": \"string\", \"description\": \"Text to reverse\"},\n        },\n        \"required\": [\"text\"],\n    },\n    fn=reverse_text,\n)\n</code></pre>"},{"location":"pi_llm_agent/quickstart/#cancellation","title":"Cancellation","text":"<p>Cancel a running agent with <code>agent.abort()</code>:</p> <pre><code>import asyncio\n\nasync def main():\n    # Start the prompt in a task\n    task = asyncio.create_task(agent.prompt(\"Write a very long essay.\"))\n\n    # Cancel after 2 seconds\n    await asyncio.sleep(2)\n    agent.abort()\n\n    await task  # The prompt finishes with an \"aborted\" stop reason\n</code></pre>"},{"location":"pi_llm_agent/quickstart/#steering-and-follow-up","title":"Steering and follow-up","text":"<p>Inject messages into the conversation while the agent is running:</p> <pre><code># Steering: injected between turns (before the next LLM call)\nagent.steer(UserMessage(\n    content=\"Focus on the technical aspects.\",\n    timestamp=int(time.time() * 1000),\n))\n\n# Follow-up: processed after the current run completes\nagent.follow_up(UserMessage(\n    content=\"Now summarize what you found.\",\n    timestamp=int(time.time() * 1000),\n))\n</code></pre>"},{"location":"pi_llm_agent/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- The Agent class, state, and lifecycle</li> <li>Events -- The 10 agent event types</li> <li>Tools -- Creating tools with AgentTool</li> <li>Build a Research Agent -- End-to-end tutorial</li> </ul>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/","title":"Agent Lifecycle","text":"<p>The <code>Agent</code> class is the primary public API for pi-llm-agent. It wraps the low-level agent loop, managing conversation state, tool execution, event dispatch, and message queueing.</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#creating-an-agent","title":"Creating an agent","text":"<p>An agent is created with <code>AgentOptions</code>, which configures its initial state and behavior:</p> <pre><code>from pi_llm_agent import Agent, AgentOptions, InitialAgentState\nfrom pi_llm import get_model\n\nagent = Agent(AgentOptions(\n    initial_state=InitialAgentState(\n        model=get_model(\"openai\", \"gpt-4o\"),\n        system_prompt=\"You are a helpful assistant.\",\n        tools=[my_tool],\n        thinking_level=\"off\",\n    ),\n    get_api_key=lambda provider: os.environ.get(\"OPENAI_API_KEY\"),\n))\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#initialagentstate","title":"InitialAgentState","text":"<p>Sets the starting conditions:</p> Field Type Default Description <code>system_prompt</code> <code>str</code> <code>\"\"</code> System instructions for the LLM <code>model</code> <code>Model \\| None</code> <code>None</code> LLM model to use <code>thinking_level</code> <code>AgentThinkingLevel</code> <code>\"off\"</code> Reasoning level (<code>\"off\"</code>, <code>\"minimal\"</code>, ..., <code>\"xhigh\"</code>) <code>tools</code> <code>list[AgentTool] \\| None</code> <code>None</code> Available tools <code>messages</code> <code>list[Any] \\| None</code> <code>None</code> Pre-existing conversation history"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#agentoptions","title":"AgentOptions","text":"<p>Configures agent behavior:</p> Field Type Default Description <code>initial_state</code> <code>InitialAgentState \\| None</code> <code>None</code> Starting state <code>convert_to_llm</code> <code>Callable</code> <code>None</code> Custom message filter <code>transform_context</code> <code>Callable</code> <code>None</code> Pre-process messages before LLM call <code>stream_fn</code> <code>StreamFn</code> <code>None</code> Custom streaming function <code>get_api_key</code> <code>Callable</code> <code>None</code> Dynamic API key resolver <code>before_tool_call</code> <code>BeforeToolCallHook</code> <code>None</code> Pre-execution hook <code>after_tool_call</code> <code>AfterToolCallHook</code> <code>None</code> Post-execution hook <code>steering_mode</code> <code>QueueMode</code> <code>\"one-at-a-time\"</code> How steering messages drain <code>follow_up_mode</code> <code>QueueMode</code> <code>\"one-at-a-time\"</code> How follow-up messages drain <code>tool_execution</code> <code>ToolExecutionMode</code> <code>\"parallel\"</code> Sequential or parallel tool execution"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#agent-state","title":"Agent state","text":"<p>Access the agent's mutable state via <code>agent.state</code>:</p> <pre><code>state = agent.state\n\n# Conversation data\nstate.system_prompt      # str\nstate.model              # Model\nstate.thinking_level     # AgentThinkingLevel\nstate.tools              # list[AgentTool]\nstate.messages           # list[Any]\n\n# Runtime state (read-only, managed by the agent)\nstate.is_streaming       # bool -- True while processing\nstate.streaming_message  # The partial message being built, or None\nstate.pending_tool_calls # set[str] -- Tool call IDs in progress\nstate.error_message      # str | None -- Last error\n</code></pre> <p>You can modify <code>system_prompt</code>, <code>model</code>, <code>thinking_level</code>, <code>tools</code>, and <code>messages</code> between agent runs. Changes to <code>tools</code> and <code>messages</code> are copy-on-assign to prevent accidental mutation.</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#lifecycle-methods","title":"Lifecycle methods","text":""},{"location":"pi_llm_agent/concepts/agent-lifecycle/#prompt","title":"prompt()","text":"<p>Send a prompt to the agent. Accepts a string, a message object, or a list of messages:</p> <pre><code># String prompt (automatically wrapped in UserMessage)\nawait agent.prompt(\"Hello!\")\n\n# With images\nfrom pi_llm import ImageContent\nawait agent.prompt(\"Describe this image.\", images=[image_content])\n\n# Raw message or list of messages\nawait agent.prompt(user_message)\nawait agent.prompt([msg1, msg2])\n</code></pre> <p>Raises <code>RuntimeError</code> if the agent is already processing.</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#continue_","title":"continue_()","text":"<p>Resume processing from the current conversation state without adding a new message:</p> <pre><code>await agent.continue_()\n</code></pre> <p>The last message must be <code>user</code> or <code>toolResult</code> (not <code>assistant</code>). If the last message is <code>assistant</code>, the method drains steering or follow-up queues instead.</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#abort","title":"abort()","text":"<p>Cancel the current processing run:</p> <pre><code>agent.abort()\n</code></pre> <p>Uses cooperative cancellation via <code>CancellationToken</code>. The agent finishes with an <code>\"aborted\"</code> stop reason.</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#wait_for_idle","title":"wait_for_idle()","text":"<p>Wait until the agent finishes processing:</p> <pre><code>await agent.wait_for_idle()\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#reset","title":"reset()","text":"<p>Clear all messages, state, and queues:</p> <pre><code>agent.reset()\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#event-subscription","title":"Event subscription","text":"<p>Subscribe to agent events with a sync or async callback:</p> <pre><code>def on_event(event, cancellation):\n    print(event.type)\n\nunsubscribe = agent.subscribe(on_event)\n\n# Later, stop receiving events:\nunsubscribe()\n</code></pre> <p>The listener receives <code>(AgentEvent, CancellationToken)</code>. See Events for the full event catalog.</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#message-queueing","title":"Message queueing","text":"<p>The agent provides two message queues for injecting messages into the conversation:</p>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#steering","title":"Steering","text":"<p>Steering messages are injected between turns (after the LLM responds, before the next call):</p> <pre><code>agent.steer(message)\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#follow-up","title":"Follow-up","text":"<p>Follow-up messages are processed after the current agent run completes:</p> <pre><code>agent.follow_up(message)\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#queue-modes","title":"Queue modes","text":"<p>Both queues support two drain modes:</p> <ul> <li><code>\"one-at-a-time\"</code> (default) -- Process one queued message per turn.</li> <li><code>\"all\"</code> -- Process all queued messages at once.</li> </ul> <pre><code>agent.steering_mode = \"all\"\nagent.follow_up_mode = \"one-at-a-time\"\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#queue-management","title":"Queue management","text":"<pre><code>agent.clear_steering_queue()\nagent.clear_follow_up_queue()\nagent.clear_all_queues()\nagent.has_queued_messages()  # bool\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#lifecycle-flow","title":"Lifecycle flow","text":"<pre><code>Agent created\n    |\n    v\nprompt(\"Hello!\") ---------&gt; RuntimeError if already processing\n    |\n    v\n[idle_event cleared]\n[CancellationToken created]\n[state.is_streaming = True]\n    |\n    v\nrun_agent_loop()\n    |-- Turn 1: LLM call -&gt; tool execution -&gt; results\n    |-- [steering queue polled]\n    |-- Turn 2: LLM call -&gt; text response\n    |-- [follow-up queue polled]\n    |-- (continue if follow-ups...)\n    |\n    v\n[AgentEndEvent emitted]\n[state.is_streaming = False]\n[CancellationToken = None]\n[idle_event set]\n</code></pre>"},{"location":"pi_llm_agent/concepts/agent-lifecycle/#next-steps","title":"Next steps","text":"<ul> <li>Agent Loop -- The low-level multi-turn engine</li> <li>Events -- The 10 agent event types</li> <li>Tools -- Creating tools with AgentTool</li> <li>Steering &amp; Follow-up -- Queue modes and injection</li> </ul>"},{"location":"pi_llm_agent/concepts/agent-loop/","title":"Agent Loop","text":"<p>The agent loop is the low-level multi-turn execution engine that powers the <code>Agent</code> class in pi-llm-agent. It manages the cycle of calling the LLM, executing tool calls, and repeating until the task is complete.</p>"},{"location":"pi_llm_agent/concepts/agent-loop/#overview","title":"Overview","text":"<p>The loop operates at a lower level than the <code>Agent</code> class. While <code>Agent</code> provides state management, event dispatch, and message queueing, the agent loop focuses purely on the turn-by-turn execution logic.</p>"},{"location":"pi_llm_agent/concepts/agent-loop/#key-functions","title":"Key functions","text":"<ul> <li><code>run_agent_loop(messages, context, config, event_sink, cancellation, stream_fn)</code> -- Run the loop with new input messages.</li> <li><code>run_agent_loop_continue(context, config, event_sink, cancellation, stream_fn)</code> -- Continue from existing context.</li> <li><code>agent_loop()</code> / <code>agent_loop_continue()</code> -- Lower-level async generators.</li> </ul>"},{"location":"pi_llm_agent/concepts/agent-loop/#turn-structure","title":"Turn structure","text":"<p>A single turn consists of:</p> <ol> <li>Build context from system prompt, messages, and tools.</li> <li>Call the LLM via the <code>stream_fn</code>.</li> <li>Consume the event stream, building the assistant message.</li> <li>If the message contains tool calls, execute them and append results.</li> <li>Poll for steering messages.</li> <li>If there are pending tool results or steering messages, start a new turn.</li> </ol>"},{"location":"pi_llm_agent/concepts/agent-loop/#agentloopconfig","title":"AgentLoopConfig","text":"<p>Content coming soon.</p>"},{"location":"pi_llm_agent/concepts/agent-loop/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- The Agent class that wraps the loop</li> <li>Events -- Events emitted during the loop</li> <li>Steering &amp; Follow-up -- Message injection between turns</li> </ul>"},{"location":"pi_llm_agent/concepts/cancellation/","title":"Cancellation","text":"<p>pi-llm-agent uses cooperative cancellation via <code>CancellationToken</code>. Tokens are passed to event listeners and tool <code>execute()</code> methods, allowing graceful shutdown at any point in the agent loop.</p>"},{"location":"pi_llm_agent/concepts/cancellation/#overview","title":"Overview","text":"<ul> <li><code>CancellationToken</code> -- A token that can be checked and cancelled.</li> <li><code>agent.abort()</code> -- Cancel the current agent run.</li> <li><code>cancellation.is_cancelled</code> -- Check if cancellation has been requested.</li> <li><code>cancellation.cancel()</code> -- Request cancellation.</li> </ul>"},{"location":"pi_llm_agent/concepts/cancellation/#using-cancellationtoken-in-tools","title":"Using CancellationToken in tools","text":"<pre><code>class LongRunningTool(AgentTool):\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        for i in range(100):\n            if cancellation and cancellation.is_cancelled:\n                return AgentToolResult(\n                    content=[TextContent(text=\"Cancelled\")]\n                )\n            await asyncio.sleep(0.1)\n        return AgentToolResult(content=[TextContent(text=\"Done\")])\n</code></pre>"},{"location":"pi_llm_agent/concepts/cancellation/#aborting-the-agent","title":"Aborting the agent","text":"<pre><code>agent.abort()  # Cancels the current run\nawait agent.wait_for_idle()  # Wait for cleanup\n</code></pre>"},{"location":"pi_llm_agent/concepts/cancellation/#cancellationtoken-in-event-listeners","title":"CancellationToken in event listeners","text":"<p>Content coming soon.</p>"},{"location":"pi_llm_agent/concepts/cancellation/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- The abort() and wait_for_idle() methods</li> <li>Tools -- Using cancellation in tool execute()</li> </ul>"},{"location":"pi_llm_agent/concepts/events/","title":"Events","text":"<p>The agent emits events at every stage of processing. There are 10 event types, grouped into four families: agent lifecycle, turn lifecycle, message streaming, and tool execution.</p>"},{"location":"pi_llm_agent/concepts/events/#event-type-overview","title":"Event type overview","text":"Event Type string Description <code>AgentStartEvent</code> <code>\"agent_start\"</code> Agent loop has begun <code>AgentEndEvent</code> <code>\"agent_end\"</code> Agent loop has finished <code>TurnStartEvent</code> <code>\"turn_start\"</code> A new turn is starting <code>TurnEndEvent</code> <code>\"turn_end\"</code> A turn has completed <code>AgentMessageStartEvent</code> <code>\"message_start\"</code> A message is being added <code>AgentMessageUpdateEvent</code> <code>\"message_update\"</code> Incremental streaming update <code>AgentMessageEndEvent</code> <code>\"message_end\"</code> A message is complete <code>ToolExecutionStartEvent</code> <code>\"tool_execution_start\"</code> A tool call is starting <code>ToolExecutionUpdateEvent</code> <code>\"tool_execution_update\"</code> A tool is streaming partial results <code>ToolExecutionEndEvent</code> <code>\"tool_execution_end\"</code> A tool call has completed"},{"location":"pi_llm_agent/concepts/events/#subscribing-to-events","title":"Subscribing to events","text":"<p>Use <code>agent.subscribe()</code> to receive events. The callback receives the event and a <code>CancellationToken</code>:</p> <pre><code>from pi_llm_agent import CancellationToken\n\ndef on_event(event, cancellation: CancellationToken):\n    print(f\"Event: {event.type}\")\n\nunsubscribe = agent.subscribe(on_event)\n</code></pre> <p>The callback can be sync or async:</p> <pre><code>async def on_event_async(event, cancellation):\n    await log_event(event)\n\nunsubscribe = agent.subscribe(on_event_async)\n</code></pre> <p>Call the returned function to unsubscribe:</p> <pre><code>unsubscribe()\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#agent-lifecycle-events","title":"Agent lifecycle events","text":""},{"location":"pi_llm_agent/concepts/events/#agentstartevent","title":"AgentStartEvent","text":"<p>Emitted once when the agent loop begins processing (at the start of <code>prompt()</code> or <code>continue_()</code>).</p> <pre><code>from pi_llm_agent import AgentStartEvent\n\nif isinstance(event, AgentStartEvent):\n    print(\"Agent started\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#agentendevent","title":"AgentEndEvent","text":"<p>Emitted when the agent loop finishes. Contains all messages added during the run.</p> <pre><code>from pi_llm_agent import AgentEndEvent\n\nif isinstance(event, AgentEndEvent):\n    print(f\"Agent finished. {len(event.messages)} new messages.\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#turn-lifecycle-events","title":"Turn lifecycle events","text":"<p>A \"turn\" is one LLM call plus any tool executions triggered by the response. An agent run consists of one or more turns.</p>"},{"location":"pi_llm_agent/concepts/events/#turnstartevent","title":"TurnStartEvent","text":"<p>Emitted at the start of each turn.</p>"},{"location":"pi_llm_agent/concepts/events/#turnendevent","title":"TurnEndEvent","text":"<p>Emitted at the end of a turn. Contains the assistant message and any tool results.</p> <pre><code>from pi_llm_agent import TurnEndEvent\n\nif isinstance(event, TurnEndEvent):\n    msg = event.message\n    print(f\"Turn complete. Tool results: {len(event.tool_results)}\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#message-streaming-events","title":"Message streaming events","text":"<p>These events track the lifecycle of messages being added to the conversation.</p>"},{"location":"pi_llm_agent/concepts/events/#agentmessagestartevent","title":"AgentMessageStartEvent","text":"<p>Emitted when a message begins (user messages, assistant messages being streamed, tool results).</p> <pre><code>from pi_llm_agent import AgentMessageStartEvent\n\nif isinstance(event, AgentMessageStartEvent):\n    msg = event.message\n    if hasattr(msg, \"role\"):\n        print(f\"Message starting: {msg.role}\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#agentmessageupdateevent","title":"AgentMessageUpdateEvent","text":"<p>Emitted for incremental streaming updates on assistant messages. Contains both the partial agent message and the underlying pi-llm streaming event.</p> <pre><code>from pi_llm_agent import AgentMessageUpdateEvent\nfrom pi_llm import TextDeltaEvent\n\nif isinstance(event, AgentMessageUpdateEvent):\n    inner = event.assistant_message_event\n    if isinstance(inner, TextDeltaEvent):\n        print(inner.delta, end=\"\", flush=True)\n</code></pre> <p>This is the primary event for building real-time UIs -- the <code>assistant_message_event</code> gives you access to the full pi-llm event (text deltas, thinking deltas, tool call deltas, etc.).</p>"},{"location":"pi_llm_agent/concepts/events/#agentmessageendevent","title":"AgentMessageEndEvent","text":"<p>Emitted when a message is complete and has been added to the transcript.</p> <pre><code>from pi_llm_agent import AgentMessageEndEvent\n\nif isinstance(event, AgentMessageEndEvent):\n    msg = event.message\n    print(f\"Message complete: {msg.role if hasattr(msg, 'role') else 'custom'}\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#tool-execution-events","title":"Tool execution events","text":"<p>These events bracket individual tool executions within a turn.</p>"},{"location":"pi_llm_agent/concepts/events/#toolexecutionstartevent","title":"ToolExecutionStartEvent","text":"<p>Emitted when a tool call begins execution.</p> <pre><code>from pi_llm_agent import ToolExecutionStartEvent\n\nif isinstance(event, ToolExecutionStartEvent):\n    print(f\"Executing: {event.tool_name}(id={event.tool_call_id})\")\n    print(f\"Arguments: {event.args}\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#toolexecutionupdateevent","title":"ToolExecutionUpdateEvent","text":"<p>Emitted when a tool streams a partial result via its <code>on_update</code> callback. Useful for long-running tools that report progress.</p> <pre><code>from pi_llm_agent import ToolExecutionUpdateEvent\n\nif isinstance(event, ToolExecutionUpdateEvent):\n    print(f\"[{event.tool_name}] progress: {event.partial_result}\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#toolexecutionendevent","title":"ToolExecutionEndEvent","text":"<p>Emitted when a tool call completes.</p> <pre><code>from pi_llm_agent import ToolExecutionEndEvent\n\nif isinstance(event, ToolExecutionEndEvent):\n    status = \"error\" if event.is_error else \"success\"\n    print(f\"[{event.tool_name}] {status}\")\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#event-sequence-diagrams","title":"Event sequence diagrams","text":""},{"location":"pi_llm_agent/concepts/events/#simple-text-response-no-tool-calls","title":"Simple text response (no tool calls)","text":"<pre><code>AgentStartEvent\n  TurnStartEvent\n    AgentMessageStartEvent  (user message)\n    AgentMessageEndEvent\n    AgentMessageStartEvent  (assistant message, streaming begins)\n    AgentMessageUpdateEvent (text_delta) ...\n    AgentMessageEndEvent\n  TurnEndEvent\nAgentEndEvent\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#response-with-tool-calls","title":"Response with tool calls","text":"<pre><code>AgentStartEvent\n  TurnStartEvent\n    AgentMessageStartEvent  (user message)\n    AgentMessageEndEvent\n    AgentMessageStartEvent  (assistant message with tool calls)\n    AgentMessageUpdateEvent (toolcall_delta) ...\n    AgentMessageEndEvent\n    ToolExecutionStartEvent (tool A)\n    ToolExecutionStartEvent (tool B)    -- parallel by default\n    ToolExecutionEndEvent   (tool A)\n    ToolExecutionEndEvent   (tool B)\n    AgentMessageStartEvent  (tool result A)\n    AgentMessageEndEvent\n    AgentMessageStartEvent  (tool result B)\n    AgentMessageEndEvent\n  TurnEndEvent\n  TurnStartEvent\n    AgentMessageStartEvent  (assistant message, final text)\n    AgentMessageUpdateEvent (text_delta) ...\n    AgentMessageEndEvent\n  TurnEndEvent\nAgentEndEvent\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#building-an-event-printer","title":"Building an event printer","text":"<p>A common pattern is a function that prints events for debugging:</p> <pre><code>from pi_llm_agent import (\n    AgentStartEvent,\n    AgentEndEvent,\n    AgentMessageUpdateEvent,\n    ToolExecutionStartEvent,\n    ToolExecutionEndEvent,\n)\nfrom pi_llm import TextDeltaEvent\n\n\ndef make_event_printer():\n    \"\"\"Create an event subscriber that prints agent activity.\"\"\"\n    last_text_len = 0\n\n    def on_event(event, cancellation):\n        nonlocal last_text_len\n\n        if isinstance(event, AgentStartEvent):\n            print(\"--- Agent started ---\")\n\n        elif isinstance(event, AgentMessageUpdateEvent):\n            inner = event.assistant_message_event\n            if isinstance(inner, TextDeltaEvent):\n                print(inner.delta, end=\"\", flush=True)\n\n        elif isinstance(event, ToolExecutionStartEvent):\n            print(f\"\\n[Tool: {event.tool_name}({event.args})]\")\n\n        elif isinstance(event, ToolExecutionEndEvent):\n            status = \"error\" if event.is_error else \"done\"\n            print(f\"[Tool {event.tool_name}: {status}]\")\n\n        elif isinstance(event, AgentEndEvent):\n            print(\"\\n--- Agent finished ---\")\n\n    return on_event\n\n\n# Usage\nagent.subscribe(make_event_printer())\n</code></pre>"},{"location":"pi_llm_agent/concepts/events/#the-agentevent-union","title":"The AgentEvent union","text":"<p>All 10 event types are members of the <code>AgentEvent</code> union type:</p> <pre><code>from pi_llm_agent import AgentEvent\n</code></pre> <p>You can match on the <code>type</code> string or use <code>isinstance</code> checks.</p>"},{"location":"pi_llm_agent/concepts/events/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- The Agent class and state management</li> <li>Tools -- Creating tools that generate tool execution events</li> <li>Hooks -- Intercepting tool calls with before/after hooks</li> </ul>"},{"location":"pi_llm_agent/concepts/hooks/","title":"Hooks","text":"<p>Hooks let you intercept tool calls before and after execution. Use them for authorization, logging, argument transformation, or result modification.</p>"},{"location":"pi_llm_agent/concepts/hooks/#overview","title":"Overview","text":"<p>pi-llm-agent provides two hook points:</p> <ul> <li><code>before_tool_call</code> -- Called before a tool executes. Can block execution.</li> <li><code>after_tool_call</code> -- Called after a tool executes. Can override the result.</li> </ul> <p>Both hooks are async functions configured via <code>AgentOptions</code>.</p>"},{"location":"pi_llm_agent/concepts/hooks/#beforetoolcallhook","title":"BeforeToolCallHook","text":"<p>The <code>before_tool_call</code> hook receives a <code>BeforeToolCallContext</code> and an optional <code>CancellationToken</code>. It returns a <code>BeforeToolCallResult</code> (or <code>None</code> to allow execution).</p> <pre><code>async def my_before_hook(context, cancellation):\n    if context.tool_call.name == \"dangerous_tool\":\n        return BeforeToolCallResult(block=True, reason=\"Tool is restricted\")\n    return None  # Allow execution\n</code></pre>"},{"location":"pi_llm_agent/concepts/hooks/#aftertoolcallhook","title":"AfterToolCallHook","text":"<p>The <code>after_tool_call</code> hook receives an <code>AfterToolCallContext</code> and returns an <code>AfterToolCallResult</code> (or <code>None</code> to use the original result).</p> <pre><code>async def my_after_hook(context, cancellation):\n    # Log all tool results\n    print(f\"Tool {context.tool_call.name} returned: {context.result}\")\n    return None  # Keep original result\n</code></pre>"},{"location":"pi_llm_agent/concepts/hooks/#configuration","title":"Configuration","text":"<p>Content coming soon.</p>"},{"location":"pi_llm_agent/concepts/hooks/#next-steps","title":"Next steps","text":"<ul> <li>Tools -- The AgentTool execute method</li> <li>Control Tool Execution -- Sequential vs parallel</li> <li>Agent Lifecycle -- Configuring hooks in AgentOptions</li> </ul>"},{"location":"pi_llm_agent/concepts/steering-followup/","title":"Steering &amp; Follow-up","text":"<p>Steering and follow-up queues let you inject messages into a running agent's conversation. Steering messages are delivered between turns; follow-up messages are delivered after the current run completes.</p>"},{"location":"pi_llm_agent/concepts/steering-followup/#overview","title":"Overview","text":"<ul> <li>Steering -- Use <code>agent.steer(message)</code> to inject a message between turns. The agent sees it before its next LLM call.</li> <li>Follow-up -- Use <code>agent.follow_up(message)</code> to queue a message for after the current run. The agent processes it as a new prompt.</li> </ul>"},{"location":"pi_llm_agent/concepts/steering-followup/#queue-modes","title":"Queue modes","text":"<p>Both queues support two drain modes:</p> Mode Behavior <code>\"one-at-a-time\"</code> Process one queued message per turn (default) <code>\"all\"</code> Process all queued messages at once <pre><code>agent.steering_mode = \"all\"\nagent.follow_up_mode = \"one-at-a-time\"\n</code></pre>"},{"location":"pi_llm_agent/concepts/steering-followup/#steer","title":"steer()","text":"<pre><code>agent.steer(UserMessage(\n    content=\"Focus on cost analysis.\",\n    timestamp=int(time.time() * 1000),\n))\n</code></pre>"},{"location":"pi_llm_agent/concepts/steering-followup/#follow_up","title":"follow_up()","text":"<pre><code>agent.follow_up(UserMessage(\n    content=\"Now summarize your findings.\",\n    timestamp=int(time.time() * 1000),\n))\n</code></pre>"},{"location":"pi_llm_agent/concepts/steering-followup/#queue-management","title":"Queue management","text":"<p>Content coming soon.</p>"},{"location":"pi_llm_agent/concepts/steering-followup/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- Queue configuration in AgentOptions</li> <li>Cancellation -- Aborting a run</li> <li>Agent Loop -- How messages are polled between turns</li> </ul>"},{"location":"pi_llm_agent/concepts/tools/","title":"Tools","text":"<p>In pi-llm-agent, tools are implemented as <code>AgentTool</code> subclasses. Each tool has a name, description, JSON Schema for parameters, and an async <code>execute()</code> method that performs the actual work.</p>"},{"location":"pi_llm_agent/concepts/tools/#defining-a-tool-with-a-subclass","title":"Defining a tool with a subclass","text":"<p>Subclass <code>AgentTool</code> and implement <code>execute()</code>:</p> <pre><code>from pi_llm_agent import AgentTool, AgentToolResult\nfrom pi_llm import TextContent\n\n\nclass WeatherTool(AgentTool):\n    def __init__(self):\n        super().__init__(\n            name=\"get_weather\",\n            label=\"Get Weather\",\n            description=\"Get current weather for a city\",\n            parameters={\n                \"type\": \"object\",\n                \"properties\": {\n                    \"city\": {\n                        \"type\": \"string\",\n                        \"description\": \"City name\",\n                    },\n                },\n                \"required\": [\"city\"],\n            },\n        )\n\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        city = params[\"city\"]\n        # ... call an API, do computation, etc.\n        return AgentToolResult(\n            content=[TextContent(text=f\"18C and sunny in {city}\")]\n        )\n</code></pre>"},{"location":"pi_llm_agent/concepts/tools/#constructor-parameters","title":"Constructor parameters","text":"Parameter Type Description <code>name</code> <code>str</code> Unique identifier for the tool <code>label</code> <code>str</code> Human-readable display name <code>description</code> <code>str</code> LLM-facing description of what the tool does <code>parameters</code> <code>dict</code> JSON Schema defining input parameters"},{"location":"pi_llm_agent/concepts/tools/#execute-signature","title":"execute() signature","text":"<pre><code>async def execute(\n    self,\n    tool_call_id: str,\n    params: Any,\n    cancellation: CancellationToken | None = None,\n    on_update: AgentToolUpdateCallback | None = None,\n) -&gt; AgentToolResult\n</code></pre> Parameter Description <code>tool_call_id</code> Unique ID for this invocation <code>params</code> Validated arguments matching the JSON Schema <code>cancellation</code> Token for cooperative cancellation <code>on_update</code> Callback for streaming partial results"},{"location":"pi_llm_agent/concepts/tools/#defining-a-tool-with-from_function","title":"Defining a tool with from_function","text":"<p>For simple tools, use the factory method:</p> <pre><code>from pi_llm_agent import AgentTool, AgentToolResult\nfrom pi_llm import TextContent\n\n\nasync def calculate(tool_call_id, params, cancellation=None, on_update=None):\n    expr = params[\"expression\"]\n    result = eval(expr)  # (use a safe evaluator in production!)\n    return AgentToolResult(\n        content=[TextContent(text=str(result))]\n    )\n\n\ncalc_tool = AgentTool.from_function(\n    name=\"calculate\",\n    label=\"Calculator\",\n    description=\"Evaluate a mathematical expression\",\n    parameters={\n        \"type\": \"object\",\n        \"properties\": {\n            \"expression\": {\n                \"type\": \"string\",\n                \"description\": \"Math expression to evaluate\",\n            },\n        },\n        \"required\": [\"expression\"],\n    },\n    fn=calculate,\n)\n</code></pre>"},{"location":"pi_llm_agent/concepts/tools/#agenttoolresult","title":"AgentToolResult","text":"<p>The result returned by <code>execute()</code>:</p> <pre><code>from pi_llm_agent import AgentToolResult\nfrom pi_llm import TextContent, ImageContent\n\n# Text result\nresult = AgentToolResult(\n    content=[TextContent(text=\"The answer is 42\")]\n)\n\n# Result with metadata\nresult = AgentToolResult(\n    content=[TextContent(text=\"File saved\")],\n    details={\"path\": \"/tmp/output.txt\", \"bytes\": 1024},\n)\n\n# Image result\nresult = AgentToolResult(\n    content=[ImageContent(data=base64_data, mime_type=\"image/png\")]\n)\n</code></pre> <p>The <code>content</code> field accepts a list of <code>TextContent</code> and/or <code>ImageContent</code> blocks. The <code>details</code> field is arbitrary metadata that is included in the tool result message.</p>"},{"location":"pi_llm_agent/concepts/tools/#streaming-partial-results","title":"Streaming partial results","text":"<p>Long-running tools can report progress via the <code>on_update</code> callback:</p> <pre><code>class ResearchTool(AgentTool):\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        query = params[\"query\"]\n        results = []\n\n        for i, source in enumerate(sources):\n            # Check for cancellation\n            if cancellation and cancellation.is_cancelled:\n                break\n\n            result = await fetch_source(source, query)\n            results.append(result)\n\n            # Stream progress\n            if on_update:\n                on_update(AgentToolResult(\n                    content=[TextContent(text=f\"Searched {i+1}/{len(sources)} sources\")],\n                    details={\"progress\": (i + 1) / len(sources)},\n                ))\n\n        return AgentToolResult(\n            content=[TextContent(text=\"\\n\".join(results))]\n        )\n</code></pre> <p>Each <code>on_update</code> call emits a <code>ToolExecutionUpdateEvent</code> that subscribers can observe.</p>"},{"location":"pi_llm_agent/concepts/tools/#error-handling","title":"Error handling","text":"<p>Raise any exception from <code>execute()</code> to signal failure. The agent catches it and reports the error to the LLM:</p> <pre><code>class FileTool(AgentTool):\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        path = params[\"path\"]\n        if not os.path.exists(path):\n            raise FileNotFoundError(f\"File not found: {path}\")\n        content = open(path).read()\n        return AgentToolResult(content=[TextContent(text=content)])\n</code></pre> <p>The LLM sees the error message and can retry or inform the user.</p>"},{"location":"pi_llm_agent/concepts/tools/#argument-pre-processing","title":"Argument pre-processing","text":"<p>Override <code>prepare_arguments()</code> to transform raw arguments before validation:</p> <pre><code>class FlexibleTool(AgentTool):\n    def prepare_arguments(self, args):\n        # Normalize a string argument to the expected object shape\n        if isinstance(args, str):\n            return {\"query\": args}\n        return args\n</code></pre> <p>This runs before JSON Schema validation, so the transformed arguments must match the schema.</p>"},{"location":"pi_llm_agent/concepts/tools/#converting-to-pi-llm-tool","title":"Converting to pi-llm Tool","text":"<p><code>AgentTool</code> has a <code>to_tool()</code> method that produces a pi-llm <code>Tool</code> for inclusion in LLM context. The agent calls this internally, but you can use it directly:</p> <pre><code>pi_llm_tool = my_agent_tool.to_tool()\n# Tool(name=\"...\", description=\"...\", parameters={...})\n</code></pre>"},{"location":"pi_llm_agent/concepts/tools/#registering-tools-with-the-agent","title":"Registering tools with the agent","text":"<p>Pass tools in <code>InitialAgentState</code>, or modify <code>agent.state.tools</code> between runs:</p> <pre><code># At creation\nagent = Agent(AgentOptions(\n    initial_state=InitialAgentState(\n        tools=[weather_tool, calc_tool],\n        ...\n    ),\n))\n\n# Between runs\nagent.state.tools = [weather_tool, calc_tool, new_tool]\n</code></pre>"},{"location":"pi_llm_agent/concepts/tools/#next-steps","title":"Next steps","text":"<ul> <li>Hooks -- Intercept tool calls with before/after hooks</li> <li>Events -- Tool execution events</li> <li>Control Tool Execution -- Sequential vs parallel</li> </ul>"},{"location":"pi_llm_agent/howto/build-research-agent/","title":"Build a Research Agent","text":"<p>This tutorial builds a research agent from scratch that can search the web and take notes. It demonstrates the full agent workflow: defining tools, configuring the agent, subscribing to events, and running a multi-turn research task.</p>"},{"location":"pi_llm_agent/howto/build-research-agent/#overview","title":"Overview","text":"<p>The research agent has two tools:</p> <ul> <li>WebSearchTool -- Searches the web using the Tavily API</li> <li>NoteTool -- Saves research notes to memory</li> </ul> <p>The agent loop handles everything automatically: when the LLM decides to search, the agent executes the search tool, feeds the results back, and continues until the research is complete.</p>"},{"location":"pi_llm_agent/howto/build-research-agent/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install pi-llm-agent httpx\n</code></pre> <p>You will need API keys for:</p> <ul> <li>An LLM provider (e.g., OpenAI)</li> <li>Tavily for web search (or substitute your own search API)</li> </ul>"},{"location":"pi_llm_agent/howto/build-research-agent/#step-1-define-the-websearchtool","title":"Step 1: Define the WebSearchTool","text":"<pre><code>import httpx\nfrom pi_llm_agent import AgentTool, AgentToolResult\nfrom pi_llm import TextContent\n\n\nclass WebSearchTool(AgentTool):\n    \"\"\"Search the web using the Tavily API.\"\"\"\n\n    def __init__(self, api_key: str):\n        super().__init__(\n            name=\"web_search\",\n            label=\"Web Search\",\n            description=\"Search the web for current information on a topic\",\n            parameters={\n                \"type\": \"object\",\n                \"properties\": {\n                    \"query\": {\n                        \"type\": \"string\",\n                        \"description\": \"Search query\",\n                    },\n                },\n                \"required\": [\"query\"],\n            },\n        )\n        self.api_key = api_key\n\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        query = params[\"query\"]\n\n        async with httpx.AsyncClient() as client:\n            response = await client.post(\n                \"https://api.tavily.com/search\",\n                json={\n                    \"api_key\": self.api_key,\n                    \"query\": query,\n                    \"max_results\": 5,\n                },\n            )\n            data = response.json()\n\n        # Format results as text\n        results = []\n        for item in data.get(\"results\", []):\n            results.append(\n                f\"**{item['title']}**\\n{item['url']}\\n{item.get('content', '')}\\n\"\n            )\n\n        text = f\"Search results for: {query}\\n\\n\" + \"\\n---\\n\".join(results)\n        return AgentToolResult(content=[TextContent(text=text)])\n</code></pre>"},{"location":"pi_llm_agent/howto/build-research-agent/#step-2-define-the-notetool","title":"Step 2: Define the NoteTool","text":"<pre><code>class NoteTool(AgentTool):\n    \"\"\"Save a research note.\"\"\"\n\n    def __init__(self):\n        super().__init__(\n            name=\"save_note\",\n            label=\"Save Note\",\n            description=\"Save a research note with a title and content\",\n            parameters={\n                \"type\": \"object\",\n                \"properties\": {\n                    \"title\": {\n                        \"type\": \"string\",\n                        \"description\": \"Note title\",\n                    },\n                    \"content\": {\n                        \"type\": \"string\",\n                        \"description\": \"Note content\",\n                    },\n                },\n                \"required\": [\"title\", \"content\"],\n            },\n        )\n        self.notes: list[dict] = []\n\n    async def execute(self, tool_call_id, params, cancellation=None, on_update=None):\n        note = {\"title\": params[\"title\"], \"content\": params[\"content\"]}\n        self.notes.append(note)\n        return AgentToolResult(\n            content=[TextContent(text=f\"Note saved: {note['title']}\")],\n            details=note,\n        )\n</code></pre>"},{"location":"pi_llm_agent/howto/build-research-agent/#step-3-create-an-event-printer","title":"Step 3: Create an event printer","text":"<p>Build a subscriber that shows the agent's activity in real time:</p> <pre><code>from pi_llm_agent import (\n    AgentStartEvent,\n    AgentEndEvent,\n    AgentMessageUpdateEvent,\n    ToolExecutionStartEvent,\n    ToolExecutionEndEvent,\n)\nfrom pi_llm import TextDeltaEvent, ThinkingDeltaEvent\n\n\ndef make_event_printer():\n    \"\"\"Create an event subscriber that prints agent activity.\"\"\"\n\n    def on_event(event, cancellation):\n        if isinstance(event, AgentStartEvent):\n            print(\"=\" * 60)\n            print(\"Research agent started\")\n            print(\"=\" * 60)\n\n        elif isinstance(event, AgentMessageUpdateEvent):\n            inner = event.assistant_message_event\n            if isinstance(inner, TextDeltaEvent):\n                print(inner.delta, end=\"\", flush=True)\n            elif isinstance(inner, ThinkingDeltaEvent):\n                print(f\"[thinking] {inner.delta}\", end=\"\", flush=True)\n\n        elif isinstance(event, ToolExecutionStartEvent):\n            print(f\"\\n&gt;&gt;&gt; Calling {event.tool_name}: {event.args}\")\n\n        elif isinstance(event, ToolExecutionEndEvent):\n            status = \"ERROR\" if event.is_error else \"OK\"\n            print(f\"&lt;&lt;&lt; {event.tool_name}: {status}\")\n\n        elif isinstance(event, AgentEndEvent):\n            print(\"\\n\" + \"=\" * 60)\n            print(f\"Research complete. {len(event.messages)} messages generated.\")\n            print(\"=\" * 60)\n\n    return on_event\n</code></pre>"},{"location":"pi_llm_agent/howto/build-research-agent/#step-4-wire-it-all-together","title":"Step 4: Wire it all together","text":"<pre><code>import asyncio\nimport os\n\nfrom pi_llm_agent import Agent, AgentOptions, InitialAgentState\nfrom pi_llm import get_model\nfrom pi_llm.providers import register_builtin_providers\n\n\nasync def run_research(topic: str):\n    register_builtin_providers()\n\n    # Create tools\n    note_tool = NoteTool()\n    search_tool = WebSearchTool(api_key=os.environ[\"TAVILY_API_KEY\"])\n\n    # Create agent\n    agent = Agent(AgentOptions(\n        initial_state=InitialAgentState(\n            model=get_model(\"openai\", \"gpt-4o\"),\n            system_prompt=(\n                \"You are a research assistant. When given a topic:\\n\"\n                \"1. Search the web for relevant information\\n\"\n                \"2. Save key findings as notes\\n\"\n                \"3. Provide a summary of your research\\n\\n\"\n                \"Be thorough -- search multiple angles of the topic.\"\n            ),\n            tools=[search_tool, note_tool],\n        ),\n        get_api_key=lambda provider: os.environ.get(\"OPENAI_API_KEY\"),\n    ))\n\n    # Subscribe to events\n    agent.subscribe(make_event_printer())\n\n    # Run the research\n    await agent.prompt(f\"Research the following topic: {topic}\")\n\n    # Print collected notes\n    print(\"\\n\\nCollected Notes:\")\n    for i, note in enumerate(note_tool.notes, 1):\n        print(f\"\\n{i}. {note['title']}\")\n        print(f\"   {note['content'][:200]}...\")\n\n    return agent.state.messages\n\n\nasyncio.run(run_research(\"Recent advances in quantum computing\"))\n</code></pre>"},{"location":"pi_llm_agent/howto/build-research-agent/#how-it-works","title":"How it works","text":"<ol> <li><code>agent.prompt(topic)</code> adds a <code>UserMessage</code> and starts the agent loop.</li> <li>The LLM reads the system prompt and decides to call <code>web_search</code>.</li> <li>The agent executes <code>WebSearchTool.execute()</code>, producing a <code>ToolResultMessage</code>.</li> <li>The tool result is appended to the conversation and the LLM is called again.</li> <li>The LLM may search again, save notes, or produce a final summary.</li> <li>The loop ends when the LLM responds with text only (no more tool calls).</li> <li>Events are emitted at each stage, driving the real-time event printer.</li> </ol>"},{"location":"pi_llm_agent/howto/build-research-agent/#adding-cancellation-support","title":"Adding cancellation support","text":"<p>For long research tasks, add a timeout:</p> <pre><code>async def run_with_timeout(topic: str, timeout_seconds: int = 60):\n    # ... create agent as above ...\n\n    task = asyncio.create_task(agent.prompt(f\"Research: {topic}\"))\n\n    try:\n        await asyncio.wait_for(task, timeout=timeout_seconds)\n    except asyncio.TimeoutError:\n        agent.abort()\n        print(\"\\nResearch timed out, partial results available.\")\n        await agent.wait_for_idle()\n</code></pre>"},{"location":"pi_llm_agent/howto/build-research-agent/#adding-steering","title":"Adding steering","text":"<p>Redirect the agent mid-research:</p> <pre><code>import time\nfrom pi_llm import UserMessage\n\nasync def run_with_steering(topic: str):\n    # ... create agent as above ...\n\n    async def steer_after_delay():\n        await asyncio.sleep(10)\n        agent.steer(UserMessage(\n            content=\"Focus specifically on practical applications, not theory.\",\n            timestamp=int(time.time() * 1000),\n        ))\n\n    # Run both concurrently\n    await asyncio.gather(\n        agent.prompt(f\"Research: {topic}\"),\n        steer_after_delay(),\n    )\n</code></pre> <p>The steering message is injected between turns, so the LLM sees it before its next response.</p>"},{"location":"pi_llm_agent/howto/build-research-agent/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- Understanding the Agent class</li> <li>Events -- All 10 event types</li> <li>Hooks -- Intercept tool calls</li> <li>Control Tool Execution -- Sequential vs parallel execution</li> </ul>"},{"location":"pi_llm_agent/howto/control-tool-execution/","title":"Control Tool Execution","text":"<p>This guide covers how to control whether tools run sequentially or in parallel, and how to use hooks to intercept tool calls.</p>"},{"location":"pi_llm_agent/howto/control-tool-execution/#overview","title":"Overview","text":"<p>By default, when the LLM requests multiple tool calls in a single response, pi-llm-agent executes them in parallel. You can switch to sequential execution or use hooks to add authorization, logging, or result modification.</p>"},{"location":"pi_llm_agent/howto/control-tool-execution/#execution-modes","title":"Execution modes","text":"<p>Set the mode in <code>AgentOptions</code>:</p> <pre><code>from pi_llm_agent import AgentOptions\n\n# Parallel (default) -- all tool calls run concurrently\noptions = AgentOptions(tool_execution=\"parallel\")\n\n# Sequential -- tool calls run one at a time, in order\noptions = AgentOptions(tool_execution=\"sequential\")\n</code></pre>"},{"location":"pi_llm_agent/howto/control-tool-execution/#using-hooks","title":"Using hooks","text":"<p>Hooks intercept tool calls before and after execution:</p> <pre><code>from pi_llm_agent import BeforeToolCallResult\n\nasync def before_hook(context, cancellation):\n    print(f\"About to call: {context.tool_call.name}\")\n    # Return BeforeToolCallResult(block=True) to prevent execution\n    return None\n\noptions = AgentOptions(before_tool_call=before_hook)\n</code></pre>"},{"location":"pi_llm_agent/howto/control-tool-execution/#full-example","title":"Full example","text":"<p>Content coming soon.</p>"},{"location":"pi_llm_agent/howto/control-tool-execution/#next-steps","title":"Next steps","text":"<ul> <li>Tools -- AgentTool reference</li> <li>Hooks -- Hook types and context objects</li> <li>Events -- Tool execution events</li> </ul>"},{"location":"pi_llm_agent/howto/custom-message-types/","title":"Custom Message Types","text":"<p>This guide explains how to use custom message types in the agent's conversation transcript, and how to filter them before they reach the LLM.</p>"},{"location":"pi_llm_agent/howto/custom-message-types/#overview","title":"Overview","text":"<p>The agent's message list (<code>agent.state.messages</code>) can contain any Python object, not just pi-llm message types. This is useful for application-specific messages (status updates, metadata, UI events) that should be part of the transcript but should not be sent to the LLM.</p>"},{"location":"pi_llm_agent/howto/custom-message-types/#convert_to_llm","title":"convert_to_llm","text":"<p>The <code>convert_to_llm</code> callback filters messages before each LLM call. The default implementation keeps only <code>user</code>, <code>assistant</code>, and <code>toolResult</code> messages.</p> <pre><code>def my_convert_to_llm(messages):\n    return [m for m in messages if hasattr(m, \"role\") and m.role in (\"user\", \"assistant\", \"toolResult\")]\n</code></pre>"},{"location":"pi_llm_agent/howto/custom-message-types/#transform_context","title":"transform_context","text":"<p>The <code>transform_context</code> callback provides a second stage of message processing, useful for context window management or message summarization.</p>"},{"location":"pi_llm_agent/howto/custom-message-types/#full-example","title":"Full example","text":"<p>Content coming soon.</p>"},{"location":"pi_llm_agent/howto/custom-message-types/#next-steps","title":"Next steps","text":"<ul> <li>Agent Lifecycle -- AgentOptions configuration</li> <li>Agent Loop -- How messages flow through the loop</li> </ul>"},{"location":"pi_llm_agent/reference/","title":"pi_llm_agent API Reference","text":"<ul> <li><code>pi_llm_agent</code></li> <li><code>pi_llm_agent.agent</code></li> <li><code>pi_llm_agent.agent_loop</code></li> <li><code>pi_llm_agent.cancellation</code></li> <li><code>pi_llm_agent.types</code></li> <li><code>pi_llm_agent.validation</code></li> </ul>"},{"location":"pi_llm_agent/reference/pi_llm_agent/","title":"<code>pi_llm_agent</code>","text":""},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent","title":"<code>pi_llm_agent</code>","text":"<p>pi_llm_agent \u2014 General-purpose agent framework.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AfterToolCallHook","title":"<code>AfterToolCallHook = Callable[['AfterToolCallContext', 'CancellationToken | None'], Awaitable[AfterToolCallResult | None]]</code>  <code>module-attribute</code>","text":"<p>Async hook called after each tool execution. Return a result to override content, details, or error flag.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentEvent","title":"<code>AgentEvent = Union[AgentStartEvent, AgentEndEvent, TurnStartEvent, TurnEndEvent, AgentMessageStartEvent, AgentMessageUpdateEvent, AgentMessageEndEvent, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolExecutionEndEvent]</code>  <code>module-attribute</code>","text":"<p>Union of all agent event types.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentEventSink","title":"<code>AgentEventSink = Callable[[AgentEvent], 'Awaitable[None] | None']</code>  <code>module-attribute</code>","text":"<p>Callback that receives agent events. Can be sync or async.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentMessage","title":"<code>AgentMessage = Any</code>  <code>module-attribute</code>","text":"<p>Union of LLM messages and custom application messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentThinkingLevel","title":"<code>AgentThinkingLevel = Literal['off', 'minimal', 'low', 'medium', 'high', 'xhigh']</code>  <code>module-attribute</code>","text":"<p>Extended thinking level for the agent. Includes <code>\"off\"</code> (pi_llm's <code>ThinkingLevel</code> does not).</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentToolCall","title":"<code>AgentToolCall = ToolCall</code>  <code>module-attribute</code>","text":"<p>A single tool call content block from an assistant message.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentToolUpdateCallback","title":"<code>AgentToolUpdateCallback = Callable[['AgentToolResult'], None]</code>  <code>module-attribute</code>","text":"<p>Callback passed to <code>AgentTool.execute()</code> for streaming partial updates.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.BeforeToolCallHook","title":"<code>BeforeToolCallHook = Callable[['BeforeToolCallContext', 'CancellationToken | None'], Awaitable[BeforeToolCallResult | None]]</code>  <code>module-attribute</code>","text":"<p>Async hook called before each tool execution. Return a result with <code>block=True</code> to prevent execution.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.QueueMode","title":"<code>QueueMode = Literal['all', 'one-at-a-time']</code>  <code>module-attribute</code>","text":"<p>How queued messages are drained: all at once or one per turn.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.StreamFn","title":"<code>StreamFn = Callable[[Model, Any, Any], 'AssistantMessageEventStream | Awaitable[AssistantMessageEventStream]']</code>  <code>module-attribute</code>","text":"<p>Stream function used by the agent loop. Matches the <code>stream_simple</code> signature.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.ToolExecutionMode","title":"<code>ToolExecutionMode = Literal['sequential', 'parallel']</code>  <code>module-attribute</code>","text":"<p>How tool calls within a turn are executed.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent","title":"<code>Agent(options=None)</code>","text":"<p>Stateful agent with tool execution and event streaming.</p> <p>The Agent wraps the low-level agent loop, managing the conversation transcript, tool execution lifecycle, event dispatch, and message queueing for steering and follow-up.</p> Example <p>agent = Agent(AgentOptions( ...     initial_state=InitialAgentState( ...         model=get_model(\"openai\", \"gpt-4o\"), ...         system_prompt=\"You are helpful.\", ...         tools=[my_tool], ...     ), ...     stream_fn=stream_simple, ...     get_api_key=lambda _: os.environ[\"OPENAI_API_KEY\"], ... )) agent.subscribe(lambda event, cancel: print(event.type)) await agent.prompt(\"Hello!\")</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.cancellation","title":"<code>cancellation</code>  <code>property</code>","text":"<p>The current cancellation token, or <code>None</code> if idle.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.steer","title":"<code>steer(message)</code>","text":"<p>Queue a steering message to interrupt between turns.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.follow_up","title":"<code>follow_up(message)</code>","text":"<p>Queue a follow-up message for after the current run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.clear_steering_queue","title":"<code>clear_steering_queue()</code>","text":"<p>Clear all pending steering messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.clear_follow_up_queue","title":"<code>clear_follow_up_queue()</code>","text":"<p>Clear all pending follow-up messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.clear_all_queues","title":"<code>clear_all_queues()</code>","text":"<p>Clear both steering and follow-up queues.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.has_queued_messages","title":"<code>has_queued_messages()</code>","text":"<p>Check if either queue has pending messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.abort","title":"<code>abort()</code>","text":"<p>Cancel the current processing run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.wait_for_idle","title":"<code>wait_for_idle()</code>  <code>async</code>","text":"<p>Wait until the agent finishes processing.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.reset","title":"<code>reset()</code>","text":"<p>Clear all messages, state, and queues.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.subscribe","title":"<code>subscribe(listener)</code>","text":"<p>Subscribe to agent events.</p> <p>Parameters:</p> Name Type Description Default <code>listener</code> <code>Callable[[AgentEvent, CancellationToken], Awaitable[None] | None]</code> <p>Callback receiving <code>(event, cancellation_token)</code>. Can be sync or async.</p> required <p>Returns:</p> Type Description <code>Callable[[], None]</code> <p>An unsubscribe function. Call it to stop receiving events.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.prompt","title":"<code>prompt(input, images=None)</code>  <code>async</code>","text":"<p>Send a prompt to the agent.</p> <p>Parameters:</p> Name Type Description Default <code>input</code> <code>str | Any | list[Any]</code> <p>A text string, a message object, or a list of messages.</p> required <code>images</code> <code>list[ImageContent] | None</code> <p>Optional images to include with a text prompt.</p> <code>None</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the agent is already processing.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.Agent.continue_","title":"<code>continue_()</code>  <code>async</code>","text":"<p>Continue from the current conversation state.</p> <p>Resumes processing without adding a new message. The last message must be <code>user</code> or <code>toolResult</code> (not <code>assistant</code>).</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the agent is already processing or has no messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentOptions","title":"<code>AgentOptions(initial_state=None, convert_to_llm=None, transform_context=None, stream_fn=None, get_api_key=None, on_payload=None, before_tool_call=None, after_tool_call=None, steering_mode='one-at-a-time', follow_up_mode='one-at-a-time', session_id=None, thinking_budgets=None, max_retry_delay_ms=None, tool_execution='parallel')</code>  <code>dataclass</code>","text":"<p>Options for constructing an <code>Agent</code>.</p> <p>Attributes:</p> Name Type Description <code>initial_state</code> <code>InitialAgentState | None</code> <p>Initial agent state (model, tools, system prompt).</p> <code>convert_to_llm</code> <code>Callable[[list[Any]], list[Any] | Awaitable[list[Any]]] | None</code> <p>Custom message filter for LLM calls.</p> <code>transform_context</code> <code>Callable[[list[Any], CancellationToken | None], Awaitable[list[Any]]] | None</code> <p>Pre-process messages before each LLM call.</p> <code>stream_fn</code> <code>StreamFn | None</code> <p>Custom streaming function (default: <code>stream_simple</code>).</p> <code>get_api_key</code> <code>Callable[[str], str | None | Awaitable[str | None]] | None</code> <p>Dynamic API key resolver.</p> <code>on_payload</code> <code>Callable[..., Any] | None</code> <p>Hook called with raw API params before each request.</p> <code>before_tool_call</code> <code>BeforeToolCallHook | None</code> <p>Hook called before each tool execution.</p> <code>after_tool_call</code> <code>AfterToolCallHook | None</code> <p>Hook called after each tool execution.</p> <code>steering_mode</code> <code>QueueMode</code> <p>How steering queue drains (default <code>\"one-at-a-time\"</code>).</p> <code>follow_up_mode</code> <code>QueueMode</code> <p>How follow-up queue drains (default <code>\"one-at-a-time\"</code>).</p> <code>session_id</code> <code>str | None</code> <p>Session ID for prompt caching.</p> <code>thinking_budgets</code> <code>dict[str, int] | None</code> <p>Custom token budgets per thinking level.</p> <code>max_retry_delay_ms</code> <code>int | None</code> <p>Cap on retry backoff.</p> <code>tool_execution</code> <code>ToolExecutionMode</code> <p><code>\"parallel\"</code> (default) or <code>\"sequential\"</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentState","title":"<code>AgentState(system_prompt='', model=None, thinking_level='off', tools=None, messages=None)</code>","text":"<p>Mutable agent state with copy-on-assign for tools and messages.</p> <p>Access via <code>agent.state</code>. Assigning to <code>tools</code> or <code>messages</code> creates a shallow copy, preventing external mutation.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <p>System-level instructions.</p> <code>model</code> <code>Model</code> <p>Current LLM model.</p> <code>thinking_level</code> <code>AgentThinkingLevel</code> <p>Current extended thinking level.</p> <code>tools</code> <code>list[AgentTool]</code> <p>Available tools (copy-on-assign).</p> <code>messages</code> <code>list[Any]</code> <p>Conversation transcript (copy-on-assign).</p> <code>is_streaming</code> <code>bool</code> <p>Whether the agent is currently processing.</p> <code>streaming_message</code> <code>Any | None</code> <p>Partial message being streamed, or <code>None</code>.</p> <code>pending_tool_calls</code> <code>set[str]</code> <p>Set of tool call IDs awaiting completion.</p> <code>error_message</code> <code>str | None</code> <p>Last error message, or <code>None</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.InitialAgentState","title":"<code>InitialAgentState(system_prompt='', model=None, thinking_level='off', tools=None, messages=None)</code>  <code>dataclass</code>","text":"<p>Initial state for constructing an Agent.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <code>str</code> <p>System-level instructions for the LLM.</p> <code>model</code> <code>Model | None</code> <p>LLM model to use.</p> <code>thinking_level</code> <code>AgentThinkingLevel</code> <p>Extended thinking level (default <code>\"off\"</code>).</p> <code>tools</code> <code>list[AgentTool] | None</code> <p>Available tools.</p> <code>messages</code> <code>list[Any] | None</code> <p>Pre-existing conversation history.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.PendingMessageQueue","title":"<code>PendingMessageQueue(mode)</code>","text":"<p>Message queue with configurable drain mode.</p> <p>In <code>\"all\"</code> mode, <code>drain()</code> returns all queued messages at once. In <code>\"one-at-a-time\"</code> mode, <code>drain()</code> returns only the first message.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.CancellationToken","title":"<code>CancellationToken()</code>","text":"<p>Lightweight wrapper around <code>asyncio.Event</code> for cooperative cancellation.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.CancellationToken.is_cancelled","title":"<code>is_cancelled</code>  <code>property</code>","text":"<p>Whether cancellation has been requested.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.CancellationToken.cancel","title":"<code>cancel()</code>","text":"<p>Signal cancellation (like <code>AbortController.abort()</code>).</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.CancellationToken.wait","title":"<code>wait()</code>  <code>async</code>","text":"<p>Block until cancellation is signalled.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AfterToolCallContext","title":"<code>AfterToolCallContext(assistant_message, tool_call, args, result, is_error, context)</code>  <code>dataclass</code>","text":"<p>Context passed to the <code>after_tool_call</code> hook.</p> <p>Attributes:</p> Name Type Description <code>assistant_message</code> <code>AssistantMessage</code> <p>The assistant message that requested the tool call.</p> <code>tool_call</code> <code>AgentToolCall</code> <p>The tool call that was executed.</p> <code>args</code> <code>Any</code> <p>Validated arguments that were passed to the tool.</p> <code>result</code> <code>AgentToolResult</code> <p>The tool execution result.</p> <code>is_error</code> <code>bool</code> <p>Whether the tool execution failed.</p> <code>context</code> <code>AgentContext</code> <p>Current agent context snapshot.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AfterToolCallResult","title":"<code>AfterToolCallResult(content=None, details=None, is_error=None)</code>  <code>dataclass</code>","text":"<p>Partial override returned by the <code>after_tool_call</code> hook.</p> <p>Only non-<code>None</code> fields override the original result.</p> <p>Attributes:</p> Name Type Description <code>content</code> <code>list[TextContent | ImageContent] | None</code> <p>Override result content.</p> <code>details</code> <code>Any</code> <p>Override result details.</p> <code>is_error</code> <code>bool | None</code> <p>Override error flag.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentContext","title":"<code>AgentContext(system_prompt='', messages=list(), tools=None)</code>  <code>dataclass</code>","text":"<p>Context snapshot passed into the low-level agent loop.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <code>str</code> <p>System-level instructions.</p> <code>messages</code> <code>list[Any]</code> <p>Conversation history (agent messages).</p> <code>tools</code> <code>list[AgentTool] | None</code> <p>Available tools, or <code>None</code> for no tools.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentEndEvent","title":"<code>AgentEndEvent(messages)</code>  <code>dataclass</code>","text":"<p>Emitted when the agent loop finishes. Contains all new messages.</p> <p>Attributes:</p> Name Type Description <code>messages</code> <code>list[Any]</code> <p>All messages added during this agent run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentLoopConfig","title":"<code>AgentLoopConfig(model, convert_to_llm, transform_context=None, get_api_key=None, get_steering_messages=None, get_follow_up_messages=None, before_tool_call=None, after_tool_call=None, tool_execution='parallel', reasoning=None, api_key=None, session_id=None, temperature=None, max_tokens=None, max_retry_delay_ms=None, thinking_budgets=None, on_payload=None, cache_retention=None, headers=None, metadata=None)</code>  <code>dataclass</code>","text":"<p>Configuration for the low-level agent loop.</p> <p>Attributes:</p> Name Type Description <code>model</code> <code>Model</code> <p>LLM model to use.</p> <code>convert_to_llm</code> <code>Callable[[list[Any]], Awaitable[list[Any]] | list[Any]]</code> <p>Function to filter agent messages to LLM messages.</p> <code>transform_context</code> <code>Callable[[list[Any], CancellationToken | None], Awaitable[list[Any]]] | None</code> <p>Optional pre-processing of messages before LLM call.</p> <code>get_api_key</code> <code>Callable[[str], Awaitable[str | None] | str | None] | None</code> <p>Resolve API key by provider name.</p> <code>get_steering_messages</code> <code>Callable[[], Awaitable[list[Any]]] | None</code> <p>Poll for steering messages between turns.</p> <code>get_follow_up_messages</code> <code>Callable[[], Awaitable[list[Any]]] | None</code> <p>Poll for follow-up messages after all turns.</p> <code>before_tool_call</code> <code>BeforeToolCallHook | None</code> <p>Hook called before each tool execution.</p> <code>after_tool_call</code> <code>AfterToolCallHook | None</code> <p>Hook called after each tool execution.</p> <code>tool_execution</code> <code>ToolExecutionMode</code> <p><code>\"parallel\"</code> (default) or <code>\"sequential\"</code>.</p> <code>reasoning</code> <code>AgentThinkingLevel | None</code> <p>Extended thinking level.</p> <code>api_key</code> <code>str | None</code> <p>Static API key (<code>get_api_key</code> takes precedence).</p> <code>session_id</code> <code>str | None</code> <p>Session ID for prompt caching.</p> <code>temperature</code> <code>float | None</code> <p>Sampling temperature.</p> <code>max_tokens</code> <code>int | None</code> <p>Maximum output tokens.</p> <code>max_retry_delay_ms</code> <code>int | None</code> <p>Cap on retry backoff.</p> <code>thinking_budgets</code> <code>dict[str, int] | None</code> <p>Custom token budgets per thinking level.</p> <code>on_payload</code> <code>Callable[..., Any] | None</code> <p>Hook called with raw API params.</p> <code>cache_retention</code> <code>str | None</code> <p>Prompt cache retention preference.</p> <code>headers</code> <code>dict[str, str] | None</code> <p>Additional HTTP headers.</p> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary metadata passed to the provider.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentMessageEndEvent","title":"<code>AgentMessageEndEvent(message)</code>  <code>dataclass</code>","text":"<p>Emitted when a message is complete and added to the transcript.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentMessageStartEvent","title":"<code>AgentMessageStartEvent(message)</code>  <code>dataclass</code>","text":"<p>Emitted when any message (user, assistant, toolResult) begins.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentMessageUpdateEvent","title":"<code>AgentMessageUpdateEvent(message, assistant_message_event)</code>  <code>dataclass</code>","text":"<p>Emitted for incremental assistant message streaming updates.</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>Any</code> <p>The partial assistant message being built.</p> <code>assistant_message_event</code> <code>AssistantMessageEvent</code> <p>The underlying pi_llm streaming event.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentStartEvent","title":"<code>AgentStartEvent()</code>  <code>dataclass</code>","text":"<p>Emitted when the agent loop begins processing.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentTool","title":"<code>AgentTool(name, label, description, parameters)</code>","text":"<p>Base class for tools that the agent can invoke.</p> <p>Subclass and implement <code>execute()</code>, or use the <code>from_function()</code> factory for simple cases.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Unique identifier for the tool.</p> <code>label</code> <p>Human-readable display name.</p> <code>description</code> <p>LLM-facing description of what the tool does.</p> <code>parameters</code> <p>JSON Schema defining the tool's input parameters.</p> Example <p>class WeatherTool(AgentTool): ...     def init(self): ...         super().init( ...             name=\"get_weather\", ...             label=\"Get Weather\", ...             description=\"Get current weather for a city\", ...             parameters={ ...                 \"type\": \"object\", ...                 \"properties\": {\"city\": {\"type\": \"string\"}}, ...                 \"required\": [\"city\"], ...             }, ...         ) ... ...     async def execute(self, tool_call_id, params, cancellation=None, on_update=None): ...         return AgentToolResult(content=[TextContent(text=f\"Sunny in {params['city']}\")])</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentTool.prepare_arguments","title":"<code>prepare_arguments(args)</code>","text":"<p>Optional pre-processing of raw arguments before validation.</p> <p>Override to transform arguments before they are validated against the JSON Schema. Default implementation is a passthrough.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentTool.execute","title":"<code>execute(tool_call_id, params, cancellation=None, on_update=None)</code>  <code>async</code>","text":"<p>Execute the tool with validated parameters.</p> <p>Parameters:</p> Name Type Description Default <code>tool_call_id</code> <code>str</code> <p>Unique ID for this tool call.</p> required <code>params</code> <code>Any</code> <p>Validated arguments matching the tool's JSON Schema.</p> required <code>cancellation</code> <code>CancellationToken | None</code> <p>Token to check for cooperative cancellation.</p> <code>None</code> <code>on_update</code> <code>AgentToolUpdateCallback | None</code> <p>Callback for streaming partial results.</p> <code>None</code> <p>Returns:</p> Type Description <code>AgentToolResult</code> <p>The tool execution result.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Raise any exception to signal tool failure. The agent will report it to the LLM as an error.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentTool.to_tool","title":"<code>to_tool()</code>","text":"<p>Convert to a pi_llm <code>Tool</code> for inclusion in LLM context.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentTool.from_function","title":"<code>from_function(name, label, description, parameters, fn)</code>  <code>classmethod</code>","text":"<p>Create a tool from a JSON Schema and an async function.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Tool name.</p> required <code>label</code> <code>str</code> <p>Display name.</p> required <code>description</code> <code>str</code> <p>LLM-facing description.</p> required <code>parameters</code> <code>dict[str, Any]</code> <p>JSON Schema for parameters.</p> required <code>fn</code> <code>Callable[..., Awaitable[AgentToolResult]]</code> <p>Async function with the same signature as <code>execute()</code>.</p> required <p>Returns:</p> Type Description <code>AgentTool</code> <p>A new <code>AgentTool</code> instance.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.AgentToolResult","title":"<code>AgentToolResult(content, details=None)</code>  <code>dataclass</code>","text":"<p>Result returned by a tool's <code>execute()</code> method.</p> <p>Also used for partial progress updates via <code>on_update</code>.</p> <p>Attributes:</p> Name Type Description <code>content</code> <code>list[TextContent | ImageContent]</code> <p>Result content blocks (text or images).</p> <code>details</code> <code>Any</code> <p>Arbitrary metadata about the execution.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.BeforeToolCallContext","title":"<code>BeforeToolCallContext(assistant_message, tool_call, args, context)</code>  <code>dataclass</code>","text":"<p>Context passed to the <code>before_tool_call</code> hook.</p> <p>Attributes:</p> Name Type Description <code>assistant_message</code> <code>AssistantMessage</code> <p>The assistant message that requested the tool call.</p> <code>tool_call</code> <code>AgentToolCall</code> <p>The tool call being preflighted.</p> <code>args</code> <code>Any</code> <p>Validated arguments (after <code>prepare_arguments</code> and schema validation).</p> <code>context</code> <code>AgentContext</code> <p>Current agent context snapshot.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.BeforeToolCallResult","title":"<code>BeforeToolCallResult(block=False, reason=None)</code>  <code>dataclass</code>","text":"<p>Result from the <code>before_tool_call</code> hook.</p> <p>Attributes:</p> Name Type Description <code>block</code> <code>bool</code> <p>Set to <code>True</code> to prevent tool execution.</p> <code>reason</code> <code>str | None</code> <p>Human-readable reason for blocking (reported to the LLM).</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.ToolExecutionEndEvent","title":"<code>ToolExecutionEndEvent(tool_call_id, tool_name, result, is_error)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call completes execution.</p> <p>Attributes:</p> Name Type Description <code>tool_call_id</code> <code>str</code> <p>Unique ID of the tool call.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool.</p> <code>result</code> <code>Any</code> <p>The final tool result.</p> <code>is_error</code> <code>bool</code> <p>Whether the execution failed.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.ToolExecutionStartEvent","title":"<code>ToolExecutionStartEvent(tool_call_id, tool_name, args)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call begins execution.</p> <p>Attributes:</p> Name Type Description <code>tool_call_id</code> <code>str</code> <p>Unique ID of the tool call.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool being executed.</p> <code>args</code> <code>Any</code> <p>Raw arguments from the LLM.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.ToolExecutionUpdateEvent","title":"<code>ToolExecutionUpdateEvent(tool_call_id, tool_name, args, partial_result)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool streams a partial result via <code>on_update</code>.</p> <p>Attributes:</p> Name Type Description <code>tool_call_id</code> <code>str</code> <p>Unique ID of the tool call.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool.</p> <code>args</code> <code>Any</code> <p>Arguments passed to the tool.</p> <code>partial_result</code> <code>Any</code> <p>The partial result from <code>on_update</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.TurnEndEvent","title":"<code>TurnEndEvent(message, tool_results)</code>  <code>dataclass</code>","text":"<p>Emitted at the end of a turn (one LLM call + tool executions).</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>Any</code> <p>The assistant message from this turn.</p> <code>tool_results</code> <code>list[ToolResultMessage]</code> <p>Tool result messages produced in this turn.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.TurnStartEvent","title":"<code>TurnStartEvent()</code>  <code>dataclass</code>","text":"<p>Emitted at the start of each conversation turn.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.default_convert_to_llm","title":"<code>default_convert_to_llm(messages)</code>","text":"<p>Default message filter: passes through user, assistant, and toolResult messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.agent_loop_continue","title":"<code>agent_loop_continue(context, config, cancellation=None, stream_fn=None)</code>","text":"<p>Continue the agent loop from existing context. Returns an event stream.</p> <p>The last message in context must be <code>user</code> or <code>toolResult</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.run_agent_loop","title":"<code>run_agent_loop(prompts, context, config, emit, cancellation=None, stream_fn=None)</code>  <code>async</code>","text":"<p>Start the agent loop with prompt messages.</p> <p>This is the direct async entry point (no background task). Used by the <code>Agent</code> class internally.</p> <p>Returns:</p> Type Description <code>list[Any]</code> <p>List of all new messages added during the run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.run_agent_loop_continue","title":"<code>run_agent_loop_continue(context, config, emit, cancellation=None, stream_fn=None)</code>  <code>async</code>","text":"<p>Continue the agent loop from existing context (direct async).</p> <p>Returns:</p> Type Description <code>list[Any]</code> <p>List of all new messages added during the run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent/#pi_llm_agent.validate_agent_tool_arguments","title":"<code>validate_agent_tool_arguments(tool, tool_call)</code>","text":"<p>Validate tool call arguments against the agent tool's JSON Schema.</p> <p>Converts <code>AgentTool</code> to a pi_llm <code>Tool</code> and delegates to <code>pi_llm.utils.validation.validate_tool_arguments</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/","title":"<code>pi_llm_agent.agent</code>","text":""},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent","title":"<code>agent</code>","text":"<p>Stateful Agent wrapper \u2014 the primary public API.</p> <p>The <code>Agent</code> class manages conversation state, tool execution, event dispatch, and message queueing. It wraps the low-level agent loop.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.PendingMessageQueue","title":"<code>PendingMessageQueue(mode)</code>","text":"<p>Message queue with configurable drain mode.</p> <p>In <code>\"all\"</code> mode, <code>drain()</code> returns all queued messages at once. In <code>\"one-at-a-time\"</code> mode, <code>drain()</code> returns only the first message.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.InitialAgentState","title":"<code>InitialAgentState(system_prompt='', model=None, thinking_level='off', tools=None, messages=None)</code>  <code>dataclass</code>","text":"<p>Initial state for constructing an Agent.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <code>str</code> <p>System-level instructions for the LLM.</p> <code>model</code> <code>Model | None</code> <p>LLM model to use.</p> <code>thinking_level</code> <code>AgentThinkingLevel</code> <p>Extended thinking level (default <code>\"off\"</code>).</p> <code>tools</code> <code>list[AgentTool] | None</code> <p>Available tools.</p> <code>messages</code> <code>list[Any] | None</code> <p>Pre-existing conversation history.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.AgentOptions","title":"<code>AgentOptions(initial_state=None, convert_to_llm=None, transform_context=None, stream_fn=None, get_api_key=None, on_payload=None, before_tool_call=None, after_tool_call=None, steering_mode='one-at-a-time', follow_up_mode='one-at-a-time', session_id=None, thinking_budgets=None, max_retry_delay_ms=None, tool_execution='parallel')</code>  <code>dataclass</code>","text":"<p>Options for constructing an <code>Agent</code>.</p> <p>Attributes:</p> Name Type Description <code>initial_state</code> <code>InitialAgentState | None</code> <p>Initial agent state (model, tools, system prompt).</p> <code>convert_to_llm</code> <code>Callable[[list[Any]], list[Any] | Awaitable[list[Any]]] | None</code> <p>Custom message filter for LLM calls.</p> <code>transform_context</code> <code>Callable[[list[Any], CancellationToken | None], Awaitable[list[Any]]] | None</code> <p>Pre-process messages before each LLM call.</p> <code>stream_fn</code> <code>StreamFn | None</code> <p>Custom streaming function (default: <code>stream_simple</code>).</p> <code>get_api_key</code> <code>Callable[[str], str | None | Awaitable[str | None]] | None</code> <p>Dynamic API key resolver.</p> <code>on_payload</code> <code>Callable[..., Any] | None</code> <p>Hook called with raw API params before each request.</p> <code>before_tool_call</code> <code>BeforeToolCallHook | None</code> <p>Hook called before each tool execution.</p> <code>after_tool_call</code> <code>AfterToolCallHook | None</code> <p>Hook called after each tool execution.</p> <code>steering_mode</code> <code>QueueMode</code> <p>How steering queue drains (default <code>\"one-at-a-time\"</code>).</p> <code>follow_up_mode</code> <code>QueueMode</code> <p>How follow-up queue drains (default <code>\"one-at-a-time\"</code>).</p> <code>session_id</code> <code>str | None</code> <p>Session ID for prompt caching.</p> <code>thinking_budgets</code> <code>dict[str, int] | None</code> <p>Custom token budgets per thinking level.</p> <code>max_retry_delay_ms</code> <code>int | None</code> <p>Cap on retry backoff.</p> <code>tool_execution</code> <code>ToolExecutionMode</code> <p><code>\"parallel\"</code> (default) or <code>\"sequential\"</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.AgentState","title":"<code>AgentState(system_prompt='', model=None, thinking_level='off', tools=None, messages=None)</code>","text":"<p>Mutable agent state with copy-on-assign for tools and messages.</p> <p>Access via <code>agent.state</code>. Assigning to <code>tools</code> or <code>messages</code> creates a shallow copy, preventing external mutation.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <p>System-level instructions.</p> <code>model</code> <code>Model</code> <p>Current LLM model.</p> <code>thinking_level</code> <code>AgentThinkingLevel</code> <p>Current extended thinking level.</p> <code>tools</code> <code>list[AgentTool]</code> <p>Available tools (copy-on-assign).</p> <code>messages</code> <code>list[Any]</code> <p>Conversation transcript (copy-on-assign).</p> <code>is_streaming</code> <code>bool</code> <p>Whether the agent is currently processing.</p> <code>streaming_message</code> <code>Any | None</code> <p>Partial message being streamed, or <code>None</code>.</p> <code>pending_tool_calls</code> <code>set[str]</code> <p>Set of tool call IDs awaiting completion.</p> <code>error_message</code> <code>str | None</code> <p>Last error message, or <code>None</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent","title":"<code>Agent(options=None)</code>","text":"<p>Stateful agent with tool execution and event streaming.</p> <p>The Agent wraps the low-level agent loop, managing the conversation transcript, tool execution lifecycle, event dispatch, and message queueing for steering and follow-up.</p> Example <p>agent = Agent(AgentOptions( ...     initial_state=InitialAgentState( ...         model=get_model(\"openai\", \"gpt-4o\"), ...         system_prompt=\"You are helpful.\", ...         tools=[my_tool], ...     ), ...     stream_fn=stream_simple, ...     get_api_key=lambda _: os.environ[\"OPENAI_API_KEY\"], ... )) agent.subscribe(lambda event, cancel: print(event.type)) await agent.prompt(\"Hello!\")</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.cancellation","title":"<code>cancellation</code>  <code>property</code>","text":"<p>The current cancellation token, or <code>None</code> if idle.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.steer","title":"<code>steer(message)</code>","text":"<p>Queue a steering message to interrupt between turns.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.follow_up","title":"<code>follow_up(message)</code>","text":"<p>Queue a follow-up message for after the current run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.clear_steering_queue","title":"<code>clear_steering_queue()</code>","text":"<p>Clear all pending steering messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.clear_follow_up_queue","title":"<code>clear_follow_up_queue()</code>","text":"<p>Clear all pending follow-up messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.clear_all_queues","title":"<code>clear_all_queues()</code>","text":"<p>Clear both steering and follow-up queues.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.has_queued_messages","title":"<code>has_queued_messages()</code>","text":"<p>Check if either queue has pending messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.abort","title":"<code>abort()</code>","text":"<p>Cancel the current processing run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.wait_for_idle","title":"<code>wait_for_idle()</code>  <code>async</code>","text":"<p>Wait until the agent finishes processing.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.reset","title":"<code>reset()</code>","text":"<p>Clear all messages, state, and queues.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.subscribe","title":"<code>subscribe(listener)</code>","text":"<p>Subscribe to agent events.</p> <p>Parameters:</p> Name Type Description Default <code>listener</code> <code>Callable[[AgentEvent, CancellationToken], Awaitable[None] | None]</code> <p>Callback receiving <code>(event, cancellation_token)</code>. Can be sync or async.</p> required <p>Returns:</p> Type Description <code>Callable[[], None]</code> <p>An unsubscribe function. Call it to stop receiving events.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.prompt","title":"<code>prompt(input, images=None)</code>  <code>async</code>","text":"<p>Send a prompt to the agent.</p> <p>Parameters:</p> Name Type Description Default <code>input</code> <code>str | Any | list[Any]</code> <p>A text string, a message object, or a list of messages.</p> required <code>images</code> <code>list[ImageContent] | None</code> <p>Optional images to include with a text prompt.</p> <code>None</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the agent is already processing.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.Agent.continue_","title":"<code>continue_()</code>  <code>async</code>","text":"<p>Continue from the current conversation state.</p> <p>Resumes processing without adding a new message. The last message must be <code>user</code> or <code>toolResult</code> (not <code>assistant</code>).</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the agent is already processing or has no messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent/#pi_llm_agent.agent.default_convert_to_llm","title":"<code>default_convert_to_llm(messages)</code>","text":"<p>Default message filter: passes through user, assistant, and toolResult messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent_loop/","title":"<code>pi_llm_agent.agent_loop</code>","text":""},{"location":"pi_llm_agent/reference/pi_llm_agent_agent_loop/#pi_llm_agent.agent_loop","title":"<code>agent_loop</code>","text":"<p>Agent loop \u2014 the core execution engine.</p> <p>Implements the multi-turn loop: prompt \u2192 LLM \u2192 tool calls \u2192 repeat.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent_loop/#pi_llm_agent.agent_loop.agent_loop","title":"<code>agent_loop(prompts, context, config, cancellation=None, stream_fn=None)</code>","text":"<p>Start the agent loop and return an event stream.</p> <p>Runs in a background asyncio task. Use <code>async for event in stream</code> to consume events.</p> <p>Parameters:</p> Name Type Description Default <code>prompts</code> <code>list[Any]</code> <p>Initial prompt messages.</p> required <code>context</code> <code>AgentContext</code> <p>Agent context snapshot.</p> required <code>config</code> <code>AgentLoopConfig</code> <p>Loop configuration.</p> required <code>cancellation</code> <code>CancellationToken | None</code> <p>Optional cancellation token.</p> <code>None</code> <code>stream_fn</code> <code>StreamFn | None</code> <p>Optional custom stream function.</p> <code>None</code> <p>Returns:</p> Type Description <code>EventStream[AgentEvent, list[Any]]</code> <p>An async iterable <code>EventStream</code> of <code>AgentEvent</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent_loop/#pi_llm_agent.agent_loop.agent_loop_continue","title":"<code>agent_loop_continue(context, config, cancellation=None, stream_fn=None)</code>","text":"<p>Continue the agent loop from existing context. Returns an event stream.</p> <p>The last message in context must be <code>user</code> or <code>toolResult</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent_loop/#pi_llm_agent.agent_loop.run_agent_loop","title":"<code>run_agent_loop(prompts, context, config, emit, cancellation=None, stream_fn=None)</code>  <code>async</code>","text":"<p>Start the agent loop with prompt messages.</p> <p>This is the direct async entry point (no background task). Used by the <code>Agent</code> class internally.</p> <p>Returns:</p> Type Description <code>list[Any]</code> <p>List of all new messages added during the run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_agent_loop/#pi_llm_agent.agent_loop.run_agent_loop_continue","title":"<code>run_agent_loop_continue(context, config, emit, cancellation=None, stream_fn=None)</code>  <code>async</code>","text":"<p>Continue the agent loop from existing context (direct async).</p> <p>Returns:</p> Type Description <code>list[Any]</code> <p>List of all new messages added during the run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_cancellation/","title":"<code>pi_llm_agent.cancellation</code>","text":""},{"location":"pi_llm_agent/reference/pi_llm_agent_cancellation/#pi_llm_agent.cancellation","title":"<code>cancellation</code>","text":"<p>Cooperative cancellation token \u2014 Python equivalent of AbortSignal.</p> <p>Passed to tool execute() and hook callbacks so they can check for and respond to cancellation requests from the agent loop.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_cancellation/#pi_llm_agent.cancellation.CancellationToken","title":"<code>CancellationToken()</code>","text":"<p>Lightweight wrapper around <code>asyncio.Event</code> for cooperative cancellation.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_cancellation/#pi_llm_agent.cancellation.CancellationToken.is_cancelled","title":"<code>is_cancelled</code>  <code>property</code>","text":"<p>Whether cancellation has been requested.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_cancellation/#pi_llm_agent.cancellation.CancellationToken.cancel","title":"<code>cancel()</code>","text":"<p>Signal cancellation (like <code>AbortController.abort()</code>).</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_cancellation/#pi_llm_agent.cancellation.CancellationToken.wait","title":"<code>wait()</code>  <code>async</code>","text":"<p>Block until cancellation is signalled.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/","title":"<code>pi_llm_agent.types</code>","text":""},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types","title":"<code>types</code>","text":"<p>Agent type definitions.</p> <p>Tools, events, hooks, loop configuration, and type aliases for the agent framework.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentThinkingLevel","title":"<code>AgentThinkingLevel = Literal['off', 'minimal', 'low', 'medium', 'high', 'xhigh']</code>  <code>module-attribute</code>","text":"<p>Extended thinking level for the agent. Includes <code>\"off\"</code> (pi_llm's <code>ThinkingLevel</code> does not).</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.ToolExecutionMode","title":"<code>ToolExecutionMode = Literal['sequential', 'parallel']</code>  <code>module-attribute</code>","text":"<p>How tool calls within a turn are executed.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.QueueMode","title":"<code>QueueMode = Literal['all', 'one-at-a-time']</code>  <code>module-attribute</code>","text":"<p>How queued messages are drained: all at once or one per turn.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentToolCall","title":"<code>AgentToolCall = ToolCall</code>  <code>module-attribute</code>","text":"<p>A single tool call content block from an assistant message.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentMessage","title":"<code>AgentMessage = Any</code>  <code>module-attribute</code>","text":"<p>Union of LLM messages and custom application messages.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentToolUpdateCallback","title":"<code>AgentToolUpdateCallback = Callable[['AgentToolResult'], None]</code>  <code>module-attribute</code>","text":"<p>Callback passed to <code>AgentTool.execute()</code> for streaming partial updates.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.BeforeToolCallHook","title":"<code>BeforeToolCallHook = Callable[['BeforeToolCallContext', 'CancellationToken | None'], Awaitable[BeforeToolCallResult | None]]</code>  <code>module-attribute</code>","text":"<p>Async hook called before each tool execution. Return a result with <code>block=True</code> to prevent execution.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AfterToolCallHook","title":"<code>AfterToolCallHook = Callable[['AfterToolCallContext', 'CancellationToken | None'], Awaitable[AfterToolCallResult | None]]</code>  <code>module-attribute</code>","text":"<p>Async hook called after each tool execution. Return a result to override content, details, or error flag.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.StreamFn","title":"<code>StreamFn = Callable[[Model, Any, Any], 'AssistantMessageEventStream | Awaitable[AssistantMessageEventStream]']</code>  <code>module-attribute</code>","text":"<p>Stream function used by the agent loop. Matches the <code>stream_simple</code> signature.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentEvent","title":"<code>AgentEvent = Union[AgentStartEvent, AgentEndEvent, TurnStartEvent, TurnEndEvent, AgentMessageStartEvent, AgentMessageUpdateEvent, AgentMessageEndEvent, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolExecutionEndEvent]</code>  <code>module-attribute</code>","text":"<p>Union of all agent event types.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentEventSink","title":"<code>AgentEventSink = Callable[[AgentEvent], 'Awaitable[None] | None']</code>  <code>module-attribute</code>","text":"<p>Callback that receives agent events. Can be sync or async.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentToolResult","title":"<code>AgentToolResult(content, details=None)</code>  <code>dataclass</code>","text":"<p>Result returned by a tool's <code>execute()</code> method.</p> <p>Also used for partial progress updates via <code>on_update</code>.</p> <p>Attributes:</p> Name Type Description <code>content</code> <code>list[TextContent | ImageContent]</code> <p>Result content blocks (text or images).</p> <code>details</code> <code>Any</code> <p>Arbitrary metadata about the execution.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentTool","title":"<code>AgentTool(name, label, description, parameters)</code>","text":"<p>Base class for tools that the agent can invoke.</p> <p>Subclass and implement <code>execute()</code>, or use the <code>from_function()</code> factory for simple cases.</p> <p>Attributes:</p> Name Type Description <code>name</code> <p>Unique identifier for the tool.</p> <code>label</code> <p>Human-readable display name.</p> <code>description</code> <p>LLM-facing description of what the tool does.</p> <code>parameters</code> <p>JSON Schema defining the tool's input parameters.</p> Example <p>class WeatherTool(AgentTool): ...     def init(self): ...         super().init( ...             name=\"get_weather\", ...             label=\"Get Weather\", ...             description=\"Get current weather for a city\", ...             parameters={ ...                 \"type\": \"object\", ...                 \"properties\": {\"city\": {\"type\": \"string\"}}, ...                 \"required\": [\"city\"], ...             }, ...         ) ... ...     async def execute(self, tool_call_id, params, cancellation=None, on_update=None): ...         return AgentToolResult(content=[TextContent(text=f\"Sunny in {params['city']}\")])</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentTool.prepare_arguments","title":"<code>prepare_arguments(args)</code>","text":"<p>Optional pre-processing of raw arguments before validation.</p> <p>Override to transform arguments before they are validated against the JSON Schema. Default implementation is a passthrough.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentTool.execute","title":"<code>execute(tool_call_id, params, cancellation=None, on_update=None)</code>  <code>async</code>","text":"<p>Execute the tool with validated parameters.</p> <p>Parameters:</p> Name Type Description Default <code>tool_call_id</code> <code>str</code> <p>Unique ID for this tool call.</p> required <code>params</code> <code>Any</code> <p>Validated arguments matching the tool's JSON Schema.</p> required <code>cancellation</code> <code>CancellationToken | None</code> <p>Token to check for cooperative cancellation.</p> <code>None</code> <code>on_update</code> <code>AgentToolUpdateCallback | None</code> <p>Callback for streaming partial results.</p> <code>None</code> <p>Returns:</p> Type Description <code>AgentToolResult</code> <p>The tool execution result.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>Raise any exception to signal tool failure. The agent will report it to the LLM as an error.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentTool.to_tool","title":"<code>to_tool()</code>","text":"<p>Convert to a pi_llm <code>Tool</code> for inclusion in LLM context.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentTool.from_function","title":"<code>from_function(name, label, description, parameters, fn)</code>  <code>classmethod</code>","text":"<p>Create a tool from a JSON Schema and an async function.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Tool name.</p> required <code>label</code> <code>str</code> <p>Display name.</p> required <code>description</code> <code>str</code> <p>LLM-facing description.</p> required <code>parameters</code> <code>dict[str, Any]</code> <p>JSON Schema for parameters.</p> required <code>fn</code> <code>Callable[..., Awaitable[AgentToolResult]]</code> <p>Async function with the same signature as <code>execute()</code>.</p> required <p>Returns:</p> Type Description <code>AgentTool</code> <p>A new <code>AgentTool</code> instance.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentContext","title":"<code>AgentContext(system_prompt='', messages=list(), tools=None)</code>  <code>dataclass</code>","text":"<p>Context snapshot passed into the low-level agent loop.</p> <p>Attributes:</p> Name Type Description <code>system_prompt</code> <code>str</code> <p>System-level instructions.</p> <code>messages</code> <code>list[Any]</code> <p>Conversation history (agent messages).</p> <code>tools</code> <code>list[AgentTool] | None</code> <p>Available tools, or <code>None</code> for no tools.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.BeforeToolCallContext","title":"<code>BeforeToolCallContext(assistant_message, tool_call, args, context)</code>  <code>dataclass</code>","text":"<p>Context passed to the <code>before_tool_call</code> hook.</p> <p>Attributes:</p> Name Type Description <code>assistant_message</code> <code>AssistantMessage</code> <p>The assistant message that requested the tool call.</p> <code>tool_call</code> <code>AgentToolCall</code> <p>The tool call being preflighted.</p> <code>args</code> <code>Any</code> <p>Validated arguments (after <code>prepare_arguments</code> and schema validation).</p> <code>context</code> <code>AgentContext</code> <p>Current agent context snapshot.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.BeforeToolCallResult","title":"<code>BeforeToolCallResult(block=False, reason=None)</code>  <code>dataclass</code>","text":"<p>Result from the <code>before_tool_call</code> hook.</p> <p>Attributes:</p> Name Type Description <code>block</code> <code>bool</code> <p>Set to <code>True</code> to prevent tool execution.</p> <code>reason</code> <code>str | None</code> <p>Human-readable reason for blocking (reported to the LLM).</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AfterToolCallContext","title":"<code>AfterToolCallContext(assistant_message, tool_call, args, result, is_error, context)</code>  <code>dataclass</code>","text":"<p>Context passed to the <code>after_tool_call</code> hook.</p> <p>Attributes:</p> Name Type Description <code>assistant_message</code> <code>AssistantMessage</code> <p>The assistant message that requested the tool call.</p> <code>tool_call</code> <code>AgentToolCall</code> <p>The tool call that was executed.</p> <code>args</code> <code>Any</code> <p>Validated arguments that were passed to the tool.</p> <code>result</code> <code>AgentToolResult</code> <p>The tool execution result.</p> <code>is_error</code> <code>bool</code> <p>Whether the tool execution failed.</p> <code>context</code> <code>AgentContext</code> <p>Current agent context snapshot.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AfterToolCallResult","title":"<code>AfterToolCallResult(content=None, details=None, is_error=None)</code>  <code>dataclass</code>","text":"<p>Partial override returned by the <code>after_tool_call</code> hook.</p> <p>Only non-<code>None</code> fields override the original result.</p> <p>Attributes:</p> Name Type Description <code>content</code> <code>list[TextContent | ImageContent] | None</code> <p>Override result content.</p> <code>details</code> <code>Any</code> <p>Override result details.</p> <code>is_error</code> <code>bool | None</code> <p>Override error flag.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentStartEvent","title":"<code>AgentStartEvent()</code>  <code>dataclass</code>","text":"<p>Emitted when the agent loop begins processing.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentEndEvent","title":"<code>AgentEndEvent(messages)</code>  <code>dataclass</code>","text":"<p>Emitted when the agent loop finishes. Contains all new messages.</p> <p>Attributes:</p> Name Type Description <code>messages</code> <code>list[Any]</code> <p>All messages added during this agent run.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.TurnStartEvent","title":"<code>TurnStartEvent()</code>  <code>dataclass</code>","text":"<p>Emitted at the start of each conversation turn.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.TurnEndEvent","title":"<code>TurnEndEvent(message, tool_results)</code>  <code>dataclass</code>","text":"<p>Emitted at the end of a turn (one LLM call + tool executions).</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>Any</code> <p>The assistant message from this turn.</p> <code>tool_results</code> <code>list[ToolResultMessage]</code> <p>Tool result messages produced in this turn.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentMessageStartEvent","title":"<code>AgentMessageStartEvent(message)</code>  <code>dataclass</code>","text":"<p>Emitted when any message (user, assistant, toolResult) begins.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentMessageUpdateEvent","title":"<code>AgentMessageUpdateEvent(message, assistant_message_event)</code>  <code>dataclass</code>","text":"<p>Emitted for incremental assistant message streaming updates.</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>Any</code> <p>The partial assistant message being built.</p> <code>assistant_message_event</code> <code>AssistantMessageEvent</code> <p>The underlying pi_llm streaming event.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentMessageEndEvent","title":"<code>AgentMessageEndEvent(message)</code>  <code>dataclass</code>","text":"<p>Emitted when a message is complete and added to the transcript.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.ToolExecutionStartEvent","title":"<code>ToolExecutionStartEvent(tool_call_id, tool_name, args)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call begins execution.</p> <p>Attributes:</p> Name Type Description <code>tool_call_id</code> <code>str</code> <p>Unique ID of the tool call.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool being executed.</p> <code>args</code> <code>Any</code> <p>Raw arguments from the LLM.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.ToolExecutionUpdateEvent","title":"<code>ToolExecutionUpdateEvent(tool_call_id, tool_name, args, partial_result)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool streams a partial result via <code>on_update</code>.</p> <p>Attributes:</p> Name Type Description <code>tool_call_id</code> <code>str</code> <p>Unique ID of the tool call.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool.</p> <code>args</code> <code>Any</code> <p>Arguments passed to the tool.</p> <code>partial_result</code> <code>Any</code> <p>The partial result from <code>on_update</code>.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.ToolExecutionEndEvent","title":"<code>ToolExecutionEndEvent(tool_call_id, tool_name, result, is_error)</code>  <code>dataclass</code>","text":"<p>Emitted when a tool call completes execution.</p> <p>Attributes:</p> Name Type Description <code>tool_call_id</code> <code>str</code> <p>Unique ID of the tool call.</p> <code>tool_name</code> <code>str</code> <p>Name of the tool.</p> <code>result</code> <code>Any</code> <p>The final tool result.</p> <code>is_error</code> <code>bool</code> <p>Whether the execution failed.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_types/#pi_llm_agent.types.AgentLoopConfig","title":"<code>AgentLoopConfig(model, convert_to_llm, transform_context=None, get_api_key=None, get_steering_messages=None, get_follow_up_messages=None, before_tool_call=None, after_tool_call=None, tool_execution='parallel', reasoning=None, api_key=None, session_id=None, temperature=None, max_tokens=None, max_retry_delay_ms=None, thinking_budgets=None, on_payload=None, cache_retention=None, headers=None, metadata=None)</code>  <code>dataclass</code>","text":"<p>Configuration for the low-level agent loop.</p> <p>Attributes:</p> Name Type Description <code>model</code> <code>Model</code> <p>LLM model to use.</p> <code>convert_to_llm</code> <code>Callable[[list[Any]], Awaitable[list[Any]] | list[Any]]</code> <p>Function to filter agent messages to LLM messages.</p> <code>transform_context</code> <code>Callable[[list[Any], CancellationToken | None], Awaitable[list[Any]]] | None</code> <p>Optional pre-processing of messages before LLM call.</p> <code>get_api_key</code> <code>Callable[[str], Awaitable[str | None] | str | None] | None</code> <p>Resolve API key by provider name.</p> <code>get_steering_messages</code> <code>Callable[[], Awaitable[list[Any]]] | None</code> <p>Poll for steering messages between turns.</p> <code>get_follow_up_messages</code> <code>Callable[[], Awaitable[list[Any]]] | None</code> <p>Poll for follow-up messages after all turns.</p> <code>before_tool_call</code> <code>BeforeToolCallHook | None</code> <p>Hook called before each tool execution.</p> <code>after_tool_call</code> <code>AfterToolCallHook | None</code> <p>Hook called after each tool execution.</p> <code>tool_execution</code> <code>ToolExecutionMode</code> <p><code>\"parallel\"</code> (default) or <code>\"sequential\"</code>.</p> <code>reasoning</code> <code>AgentThinkingLevel | None</code> <p>Extended thinking level.</p> <code>api_key</code> <code>str | None</code> <p>Static API key (<code>get_api_key</code> takes precedence).</p> <code>session_id</code> <code>str | None</code> <p>Session ID for prompt caching.</p> <code>temperature</code> <code>float | None</code> <p>Sampling temperature.</p> <code>max_tokens</code> <code>int | None</code> <p>Maximum output tokens.</p> <code>max_retry_delay_ms</code> <code>int | None</code> <p>Cap on retry backoff.</p> <code>thinking_budgets</code> <code>dict[str, int] | None</code> <p>Custom token budgets per thinking level.</p> <code>on_payload</code> <code>Callable[..., Any] | None</code> <p>Hook called with raw API params.</p> <code>cache_retention</code> <code>str | None</code> <p>Prompt cache retention preference.</p> <code>headers</code> <code>dict[str, str] | None</code> <p>Additional HTTP headers.</p> <code>metadata</code> <code>dict[str, Any] | None</code> <p>Arbitrary metadata passed to the provider.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_validation/","title":"<code>pi_llm_agent.validation</code>","text":""},{"location":"pi_llm_agent/reference/pi_llm_agent_validation/#pi_llm_agent.validation","title":"<code>validation</code>","text":"<p>Tool argument validation for the agent package.</p>"},{"location":"pi_llm_agent/reference/pi_llm_agent_validation/#pi_llm_agent.validation.validate_agent_tool_arguments","title":"<code>validate_agent_tool_arguments(tool, tool_call)</code>","text":"<p>Validate tool call arguments against the agent tool's JSON Schema.</p> <p>Converts <code>AgentTool</code> to a pi_llm <code>Tool</code> and delegates to <code>pi_llm.utils.validation.validate_tool_arguments</code>.</p>"}]}